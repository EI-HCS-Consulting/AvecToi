# Handoff — AvecToi
_Généré le : 2026-09-09 (cycle #418, #420-421, clos)_

## État global du projet

**Stack :** React Native + Expo SDK 51+, Expo Router, Supabase (BDD/Auth/Storage/Realtime/Edge Functions), EAS Build + EAS Update (channels development/preview/production, `expo-updates`, publication OTA automatique sur push `main` via `.github/workflows/eas-update-preview.yml`). expo-notifications, expo-calendar, expo-image-picker, expo-sharing + react-native-share (partage groupé). Resend (transactionnel + SMTP Supabase Auth, sous-domaine `notifications.avectoi.care`) branché sur les Edge Functions. Stripe côté web uniquement. Supabase CLI bloqué sur cette machine (App Control Policy Windows) : déploiement des migrations/Edge Functions exclusivement via le Dashboard Supabase, jamais auto-appliqué par Claude.

**⚠️ Règle Git STRICTE (CLAUDE.md) :** chaque modification, même un correctif ponctuel, part sur une branche neuve (depuis `origin/main` à jour) et se termine par sa propre PR poussée sur GitHub — jamais de nouveau commit ajouté à une branche dont la PR est déjà mergée. **Autorisation permanente : commit + push + PR dès qu'une tâche de code est terminée et vérifiée, sans attendre que l'utilisateur le redemande.** **L'utilisateur merge lui-même, puis teste** (le merge déclenche la publication OTA — sauf nouvelle dépendance native, qui nécessite un `eas build` + réinstallation complète). Toujours vérifier l'état réel d'une PR via `gh pr view <n> --json state,url` plutôt que de supposer.

**⚠️ Convention UI (CLAUDE.md, "Conventions UI") :** toute nouvelle confirmation utilise `ConfirmModal` (jamais `Alert.alert` natif) ; tout nouveau bouton reprend les couleurs/emoji/style déjà en usage à l'écran ; toute notification éphémère réutilise le pattern `showToast` existant.

**Repo GitHub :** `https://github.com/EI-HCS-Consulting/AvecToi`, branche `main` protégée par ruleset (PR obligatoire).

**PR ouverte, en attente de test/merge :** aucune. #418, #420, #421 toutes mergées — cycle code entièrement clos. Ce cycle-ci ne livre que de la documentation.

**Livré — snapshot par domaine (V1 + V2 archivé) :**
- **Socle V1** (points 1-10 CLAUDE.md) : auth admin, accès visiteur par lien/QR/code dossier, calendrier + créneaux + réservation + PIN, Souvenirs, Nouvelles, Entraide + Soutien, 6 thèmes, "Prochaine disponibilité", ajout calendrier natif Android, notifications + emails Resend.
- **RGPD & Freemium/Premium** : purge différenciée par plan (Freemium 30j non prolongeable / Premium 60j + 30j renouvelable) + gate J-7 ; cap Freemium en essai de 7 jours glissants depuis la 1ère Visite (`lib/freemiumCap.ts`, scan quotidien pg_cron `check-freemium-expiry-daily`) ; prix Premium 7,99 € (paiement unique, Stripe côté web uniquement) ; 5 fonctionnalités Premium (rôle Intervenant, vue Hebdo, besoin 🆘 SOS Relais, Documents types, Chronologie/livret + prolongation RGPD) réparties entre masquage complet et popup `ConfirmModal` selon l'écran (le gate Intervenant reste sur l'ancien style `Alert` bloquante, non harmonisé — voir rappels).
- **Reconnaissance visiteur cross-device** : identité vérifiée côté serveur par prénom + nom + PIN (table `visitor_profiles`, 4 RPC `SECURITY DEFINER`), reset PIN par l'admin (fiche visiteur) ou en self-service visiteur (table `pin_reset_requests`), historique symétrique dans « 🔔 Mes alertes ».
- **Co-administration temporaire** : un admin désigne un visiteur ayant proposé du relais comme co-admin (accès Admin complet hors facturation/RGPD/gestion des autres co-admins), acceptation par email + code à 6 chiffres, tableau de bord `app/(admin)/coadmins.tsx` en 2 sections « 🆘 Besoins SOS » / « 🛡️ Co-Administrateurs », barre de progression relais partagée (`components/RelaisDayProgress.tsx`) réutilisée à 3 emplacements, accès Paramètres via onglet caché monté dans les `Tabs` du visiteur.
- **Entraide** : assistant « Publier » en popups chaînées par catégorie, besoin de relais ponctuel avec répartition par sous-périodes, Transport (lien Maps, propositions, droits de validation), Courses (produits récurrents, cochage attribué/verrouillé, multi-contributeurs), tri Urgent/proximité + regroupement fermés, badges « New », sélection multiple, checklists suggérées avec choix de destination de publication. **Cycle #416-417** : bibliothèque passée à **15 modèles** — la carte « 🏠 Soin à domicile » ouvre désormais un sous-menu de 5 situations (🧹 ménage, 🍽️ portage de repas, 📞 télésurveillance, 🛏️ matériel médical, 📋 mise en place générale) au lieu de publier directement son contenu d'origine, avec la **même présentation côté Mon Compte → Mes Checklists** (`components/MyChecklist.tsx` réplique désormais le pattern sous-menu de `components/Entraide.tsx`) ; certains items (ex. formulaire national d'aides à l'autonomie) proposent plusieurs liens officiels au lieu d'un seul (`lienExterne` accepte objet unique ou tableau, normalisé par `checklistItemLinks()`, introduit par **PR #418**, pas #417 malgré l'attribution du cycle précédent — voir §1). **Ce cycle (#418)** : le mur d'Entraide affichait encore un seul lien par item (boucle manquante côté `Entraide.tsx`) alors que Mon Compte → Mes Checklists en affichait déjà plusieurs — corrigé, les deux écrans sont maintenant alignés y compris pour un item déjà publié.
- **Nouvelles/Soutien** : indicateur « New » (barre bleue = identité connectée, badge/cadre rouge = non-lu — deux mécanismes indépendants depuis #371), fiabilité en arrière-plan (filet `AppState`), permissions admin/visiteur harmonisées.
- **Calendrier/Visites** : `HomeCalendarScreen` commun admin/visiteur, Mensuel/Hebdo (Hebdo Premium), réservations récurrentes, regroupement par créneau, cascade accompagnant sur modification.
- **Mon Compte** : Mes souvenirs (page personnelle), Mes documents (courriers pré-remplis), Mes engagements de relais, Mes alertes (avec Archives repliables), Mon code PIN.
- **Rôle Intervenant** : masqué derrière `INTERVENANT_ROLE_ENABLED=false` depuis #291 (pas supprimé — code archivé dans `Développement V2/`), Premium.
- **Identité visuelle (logo)** : **Ce cycle (#420-421)**, sur remontée utilisateur (contours en escalier visibles sur un screenshot). PR #420 : réencodage de `assets/icon.png`, `icon-sans-512.png`, `splash.png` avec anti-aliasing propre sur les silhouettes des personnages (dilatation de couleur + downsample/upsample pour éviter tout halo) ; `adaptive-icon.png` volontairement non touché (le lissage y créait un halo gris visible autour de la marge de sécurité transparente Android, non vérifiable sans device — limite connue). PR #421 : suite à #420, l'anneau décoratif superposé à la photo du patient (`icon-sans-512.png`, utilisé par `SpaceHeader.tsx` uniquement quand une photo patient existe) a son calendrier+horloge descendu d'environ 50px pour commencer entre les bras des deux personnages au lieu de les chevaucher, dégageant davantage la photo au centre. Aucun changement de code, aucune migration — uniquement des assets binaires ; pas de section dédiée dans `Documentation Fonctionnalités.docx` (organisé par écran/fonctionnalité, pas par asset visuel), non modifiée pour ce point.

**Connu, non bloquant :**
- Branche distante `feat/entraide-creer-checklist` (PR #140, déjà mergée) porte un commit orphelin (`e9472c9`) — nettoyage jamais confirmé, sans conséquence fonctionnelle.
- Trois implémentations distinctes du désengagement/désinscription coexistent volontairement (`performUnclaim` dans `Entraide.tsx`, `lib/taskDisengage.ts` dans les deux `account.tsx`) — convention du repo, à garder en tête si une future demande vise à tout unifier.
- **Migrations manuelles au statut d'application non reconfirmé** : `supabase/migrations/20260909_personal_checklist_context_widen2.sql` (#417, **nouveau ce cycle** — élargit la contrainte CHECK `personal_checklist_items_checklist_context_check` aux 4 nouveaux contextes `telesurveillance`/`materiel_medical`/`aide_domicile_menage`/`portage_repas` ; sans cette exécution manuelle, la publication privée d'une de ces 4 sous-checklists échoue encore en production avec l'erreur "violates check constraint"), `20260821_visitor_profiles_relation.sql` (#278), `20260827_task_relais_coverage.sql` (#305), `20260830_deleted_content_seen.sql` (#337), `20260901_claimed_bought_at.sql` (#354), `20260901_visitor_claim_reset_pin.sql` (#366), `20260902_pin_reset_requests_resolved_at.sql` (#367). **Confirmées appliquées** : `20260827_reservation_change_history_seen.sql`, `20260902_freemium_7day_window.sql` (dont la fonction trigger avait le bug `column "timestamp"`, corrigé par #386), `20260904_rgpd_purge_premium_upgrade_extension.sql`. Le job pg_cron `check-freemium-expiry-daily` n'a toujours pas tourné sur un espace réellement expiré en conditions réelles — comportement de bout en bout (email + `cap_email_sent_at`) à surveiller au premier espace qui atteint sa 7ᵉ journée.
- **Ruleset GitHub `main-protection`** : le check de statut "Vercel" a été retiré manuellement par l'utilisateur (nécessaire suite à #375) — Claude Code ne peut pas modifier les rulesets lui-même (`gh api ... --method PUT` bloqué par le classifieur de sécurité auto-mode).
- **`adaptive-icon.png`** (icône adaptative Android) : volontairement non retouché par #420 — l'anti-aliasing y créerait un halo gris visible autour de sa marge de sécurité transparente, effet non vérifiable sans device réel. Limite connue, laissée de côté.
- **`logo.png`** (racine du dépôt, ajouté par ce cycle) : copie de `assets/icon-sans-512.png` (version finale post-#421), à la demande explicite de l'utilisateur — référence rapide / usage externe (site web, docs), n'est pas un asset consommé par le code de l'app.
- **`CHECKLISTS_SUGGEREES.md`** (inventaire complet du contenu des checklists, à la racine du dépôt) daté 09/09/2026 mais **non régénéré depuis l'ajout du sous-menu « Soin à domicile »** (#416) : sa checklist n°3 reflète encore l'ancien contenu plat (6 items), sans les 4 nouveaux modèles ni la structure en sous-menu — à régénérer si son usage redevient actif.
- **Documentation à jour** : `Documentation/Documentation Fonctionnalités.docx` synchronisée jusqu'à la PR #418 (**V1.34**, 09/09/2026, livrée par ce cycle documentaire) — sections « Checklists toutes prêtes (admin) » (Entraide, table 5.9) et « Checklist personnelle » (Mon Compte admin, table 5.11) retouchées : correction de l'attribution PR (le mécanisme de liens officiels multiples vient de la **PR #418**, pas de #416-417 comme écrit par le cycle précédent) et précision sur la parité mur d'Entraide / Mon Compte désormais réelle. PR #420-421 (qualité visuelle du logo) : **aucune section du docx concernée** — document organisé par écran/fonctionnalité, pas par asset visuel, aucune section ne décrit le logo/`SpaceHeader` à corriger. `PRD_AvecToi_v1_4.md` **non modifié ce cycle**, reste à **v1.32** : #418 est un correctif de parité d'affichage (pas une nouvelle capacité produit, le contenu multi-liens était déjà en v1.32) et #420-421 sont de la qualité visuelle d'asset, pas une fonctionnalité — sous le seuil "portée produit" du critère 5ter, comme le cycle #412-414 (non structurant, doc uniquement dans ce fichier).

**En cours / pas commencé (reporté, à reprendre sur demande) :**
- `fix/souvenirs-rls-and-news-delete` : branche stale, 2 migrations RLS jamais exécutées en prod (`AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Isolation Supabase (séparer l'instance prod du site web historique) : plan dans `ISOLATION_SUPABASE.md`, à réévaluer main­tenant que l'ancien site Vercel qui la motivait a été supprimé (#375) — ne pas présumer le plan caduc sans le redemander à l'utilisateur.
- `docs/spec-web-upgrade` : en attente d'une décision utilisateur.
- Points 13-14 (EAS Build APK signé, fiche Play Store) : pas commencés.
- Site marketing `avectoi.care` (dépôt séparé, Next.js, Infomaniak) : toujours pas scaffoldé. DNS/Resend opérationnels pour `support@avectoi.care`, PRD revu, reste à confirmer les accès Infomaniak pour le déploiement Git.
- Gate Intervenant (§3.9 docx) toujours en `Alert` bloquante, non harmonisé avec le style masquage/`ConfirmModal` introduit par #377-378 — à signaler si une future PR touche ce chantier.
- Nettoyage code mort post-assistant « Publier » (`!editTask &&` dans `taskForm`), à reproposer explicitement sur demande.
- `CHECKLISTS_SUGGEREES.md` à régénérer pour refléter le sous-menu « Soin à domicile » et les 4 nouveaux modèles (voir « Connu, non bloquant » ci-dessus).
- `AUDIT_RLS_TAILLE_CODE_MORT.md`, `PRD_ClaudeCode_Site_avectoi_care_v3.md`, `eslint.config.js`, `AvecToi-Site/` : fichiers non suivis présents dans l'arbre de travail, non touchés par ce cycle, jamais stagés (exclusion permanente).

## Historique cumulé
- Jusqu'au 17/08/2026 (PR #7-224) : socle V1 complet (auth, calendrier/créneaux/PIN, Souvenirs, Nouvelles, Entraide/Soutien, thèmes, notifications/emails), refonte intervenant Phase A/B, RGPD 90j + purge auto + sauvegarde schéma, checklists perso, EAS Update automatisé, panneau admin Calendrier + permissions Nouvelles, planning intervenants multi-espaces.
- 17/08 → 27/08/2026 (PR #225-301) : "Mes souvenirs", checklists proche-aidant guidées (11 modèles), Mes documents (courriers pré-remplis auto), liste de courses temps réel, besoin de relais ponctuel, réservations récurrentes, lien visiteur-patient, RGPD 60j+30j, retrait du rôle Intervenant en V1 (masqué, archivé V2), calendrier `HomeCalendarScreen` unifié admin/visiteur.
- 27/08 → 30/08/2026 (PR #303-345) : chantier SOS Relais (répartition par sous-périodes, alertes centralisées « Mes alertes »), assistant « Publier » Entraide en popups chaînées, Transport (lien Maps, propositions, validation), badges de non-lu (cadre orange puis indicateur « New »), une dizaine de correctifs UX groupés après tests utilisateur.
- 31/08 → 02/09/2026 (PR #346-371) : indicateur « New » stabilisé (fiabilité `AppState` en arrière-plan), tag Urgent auto à J+3, reconnaissance visiteur cross-device par PIN serveur (`visitor_profiles`), reset PIN admin + self-service visiteur, badge « New »/liseret bleu découplés en deux mécanismes indépendants.
- 02/09 → 05/09/2026 (PR #373-387) : cap Freemium repositionné en essai glissant de 7 jours, nettoyage de l'ancien MVP Vercel mort du dépôt, gating Freemium/Premium aligné sur le site (7,99 €, purge RGPD différenciée par plan), flux « Mot de passe oublié ? » (fix définitif via `flowType: "pkce"`), correctif du bug bloquant `column "timestamp" does not exist` sur toute réservation de Visite.
- 04/09 → 08/09/2026 (PR #390-411, narrative PR-par-PR non détaillée dans ce fichier — voir GitHub) : chantier co-administration temporaire complet — désignation depuis une proposition de relais, acceptation email + code, tableau de bord « Besoins SOS »/« Co-Administrateurs », RPC serveur dédiées avec restrictions RGPD/facturation, badge « New » sur propositions non traitées, barre de progression relais introduite dans Mon Compte.
- 08/09 → 09/09/2026 (PR #412-414) : barre de progression relais réutilisée sur 3 emplacements (Mon Compte, tableau de bord co-admin, mur d'Entraide) via `components/RelaisDayProgress.tsx` partagé ; accès « Paramètres Co-Administrateur » stabilisé après 4 tentatives (#405-410) en onglet caché du groupe `(visitor)` plutôt qu'un overlay manuel ; correctif d'affichage (troncature de date sur les propositions de relais déjà faites).
- 09/09/2026 (PR #416-417) : bibliothèque de checklists passée à 15 modèles — la carte « 🏠 Soin à domicile » (Entraide et Mon Compte → Mes Checklists) ouvre un sous-menu de 5 situations (ménage, portage de repas, télésurveillance, matériel médical, mise en place générale) au lieu de publier directement son contenu ; correctifs de navigation post-publication et de contrainte CHECK en base pour la publication privée des 4 nouveaux contextes.
- 09/09/2026 (PR #418, #420-421) : parité d'affichage des liens officiels multiples entre le mur d'Entraide et Mon Compte → Mes Checklists (PR #418, complète le mécanisme introduit par erreur d'attribution lors du cycle #416-417) ; qualité visuelle du logo — anti-aliasing des contours des personnages (PR #420) puis repositionnement du calendrier de l'anneau décoratif pour dégager la photo du patient (PR #421).

## 1. Objectif de la session
Trois PR distinctes, déjà mergées par l'utilisateur avant le début de cette session documentaire :
- **PR #418** (titre GitHub générique « Fix/soin domicile submenu nav context check », body vide, branche `fix/soin-domicile-submenu-nav-context-check` — 2 commits `eaf4e81`/`7c67d0f`) : le mur d'Entraide (`Entraide.tsx`) n'affichait encore qu'un seul lien officiel par item (`tplLink`/`item.lienExterne` unique) alors que Mon Compte → Mes Checklists (`MyChecklist.tsx`) en affichait déjà plusieurs. **Correction d'attribution découverte pendant cette session** : le mécanisme `checklistItemLinks()` normalisant `lienExterne` (objet unique ou tableau) et sa migration sur les 4 points de rendu (Entraide.tsx ×2, MyChecklist.tsx ×2) ont en réalité été livrés par cette PR **#418**, et non par #417 comme documenté par erreur au cycle précédent (le handoff #416-417 avait pris pour argent comptant un message de commit décrivant un travail qui a fini par atterrir sur une PR différente de celle numérotée #417 sur GitHub — `gh pr view 417/418 --json commits` fait foi). #417 s'est en réalité limitée au correctif de navigation + à la contrainte CHECK + à la réplication du sous-menu dans `MyChecklist.tsx` (`git log` : commit unique `a8c3812`).
- **PR #420** (`fix/logo-contours-anti-aliasing`) : sur remontée utilisateur (contours en escalier visibles sur un screenshot du header « Visites Rose-Marie »), réencodage de `assets/icon.png`, `icon-sans-512.png`, `splash.png` avec anti-aliasing propre. `adaptive-icon.png` volontairement non touché.
- **PR #421** (`fix/logo-calendrier-position`) : suite à #420, demande explicite de descendre le calendrier de l'anneau décoratif du logo (`icon-sans-512.png`, utilisé par `SpaceHeader.tsx`) pour dégager la photo du patient au centre.

L'utilisateur a ensuite demandé d'ajouter une copie du logo final à la racine du dépôt (`logo.png`, référence rapide / usage externe) et la génération de ce handoff couvrant les 3 PR.

## 2. État actuel
- Code : PR #418, #420, #421 mergées. Aucun changement de code fonctionnel pour #420-421 (assets binaires uniquement) ; #418 modifie 3 fichiers TypeScript, aucune erreur introduite.
- `Documentation/Documentation Fonctionnalités.docx` : V1.33 → **V1.34** (09/09/2026). Deux cellules retouchées : « Checklists toutes prêtes (admin) » (table 5.9 Entraide, ligne « Checklists toutes prêtes ») et « Checklist personnelle » (table 5.11 Compte admin) — correction de l'attribution PR (liens officiels multiples → **#418**, pas #416-417) et précision que le rendu boucle désormais sur tous les liens des deux côtés, y compris pour un item déjà publié sur le mur. #420-421 (qualité du logo) : **aucune section concernée**, le docx est organisé par écran/fonctionnalité, pas par asset visuel — aucune édition forcée.
- `PRD_AvecToi_v1_4.md` : **non modifié, reste à v1.32**. #418 est un correctif de parité d'affichage (le contenu produit — liens multiples — était déjà documenté en v1.32) ; #420-421 sont de la qualité visuelle d'asset, pas une fonctionnalité produit. Jugé sous le seuil « portée produit » du critère 5ter, comme le cycle #412-414.
- `Handoff/handoff.md` : ce fichier — en-tête, « État global » (bullets Entraide/Identité visuelle, ligne Documentation à jour, 2 nouvelles entrées « Connu, non bloquant »), « Historique cumulé » complété de la ligne manquante #416-417 puis de la ligne de ce cycle, section détaillée réécrite.
- `logo.png` (racine du dépôt) : ajouté, copie de `assets/icon-sans-512.png` post-#421.

## 3. Fichiers concernés
- `components/Entraide.tsx` (#418) — import de `checklistItemLinks` ; rendu des liens migré vers une boucle `checklistItemLinks(...).map(...)` à deux endroits (item déjà publié sur le mur, sélection d'items dans le sous-menu).
- `components/MyChecklist.tsx` (#418) — même migration sur ses deux points de rendu de lien (item déjà importé, sélection dans le picker).
- `lib/checklistTemplates.ts` (#418) — `lienExterne` élargi en `{label,url} | {label,url}[]` ; nouvelle fonction exportée `checklistItemLinks()` ; `checklistItemDescription()` mis à jour ; item « Remplir le formulaire national d'aides à l'autonomie à domicile » (modèles `aide_domicile_menage`, `portage_repas`) passé à 3 liens (Service-Public, page pratique pour-les-personnes-agees.gouv.fr, PDF Cerfa 16301-01).
- `assets/icon.png`, `assets/splash.png`, `assets/icon-sans-512.png` (#420) — réencodage anti-aliasing, aucun changement de dimensions.
- `assets/icon-sans-512.png` (#421, suite à #420) — calendrier+horloge de l'anneau décoratif descendu d'environ 50px, dilatation de masque + compositing alpha (pas de `SetPixel` brut).
- `assets/adaptive-icon.png` — non touché par #420 ni #421 (limite connue, voir État global).
- `logo.png` (racine, nouveau) — copie de `assets/icon-sans-512.png`.
- `Documentation/Documentation Fonctionnalités.docx` — V1.33 → V1.34, 2 cellules retouchées (voir §2).
- Vérifié sans lien avec ce cycle : `supabase/migrations/20260909_personal_checklist_context_widen2.sql` appartient bien à #417 (`git log` : commit unique `a8c3812`), pas à #418.

## 4. Ce qui a échoué
Rien pendant cette session. Point de vigilance découvert en cours de route (pas un échec, une correction) : la numérotation PR du cycle précédent (#416-417) attribuait par erreur à #417 un travail (mécanisme `checklistItemLinks()`, ajout des 2ᵉ/3ᵉ liens officiels) qui appartient en réalité à la PR GitHub #418 — vérifié via `gh pr view 417/418 --json commits` et `git log --oneline -- lib/checklistTemplates.ts`, tous deux cohérents entre eux. Corrigé dans le docx et dans ce fichier ; le PRD n'avait pas cité de numéro de PR assez précis pour nécessiter une correction similaire.

## 5. Prochaine étape
Aucune tâche de code en attente identifiée par l'utilisateur — le cycle est clos une fois cette documentation committée/poussée. **Non testé sur un appareil réel pendant cette session** (pas d'accès `adb`/émulateur dans cet environnement) : #418 a été vérifié par relecture de code uniquement ; #420-421 (qualité du logo) n'ont pu être vérifiées que par inspection pixel des rendus (recadrages zoomés avant/après décrits dans les PR), jamais sur device — à confirmer visuellement par l'utilisateur sur l'écran d'accueil avec photo patient (`SpaceHeader.tsx`) et sur l'icône/splash de l'app au prochain build. Committer les 4 fichiers (3 doc + `logo.png`) sur une branche `docs/` neuve (depuis `origin/main`), pousser, ouvrir la PR.

**⚠️ Rappels à transmettre à l'utilisateur (reportés depuis les cycles précédents, statuts inchangés sauf mention contraire) :**
1. `supabase/migrations/20260909_personal_checklist_context_widen2.sql` (#417) — à exécuter manuellement via le Dashboard Supabase (SQL Editor) ; sans ça, publier en privé une checklist « ménage », « portage de repas », « télésurveillance » ou « matériel médical » échoue encore avec une erreur de contrainte CHECK. Inchangé, toujours non confirmé.
2. `supabase/migrations/20260821_visitor_profiles_relation.sql` (#278) — statut d'application non confirmé, inchangé.
3. `supabase/migrations/20260827_task_relais_coverage.sql` (#305) — statut d'application non reconfirmé explicitement, inchangé.
4. Symptôme (b) du cycle #307-308 — alertes SOS relais qui semblaient disparaître — reste seulement instrumenté (logs d'erreur), jamais conclusivement corrigé, inchangé.
5. `supabase/migrations/20260830_deleted_content_seen.sql` (#337) — statut d'application non confirmé, inchangé.
6. `supabase/migrations/20260901_claimed_bought_at.sql` (#354) — statut d'application non confirmé, inchangé.
7. Nettoyage du code mort post-assistant « Publier » (`!editTask &&` dans `taskForm`) — à reproposer explicitement sur demande, inchangé.
8. Le « Modifier » visible par PIN partagé sur une réservation d'autrui reste un comportement explicitement voulu (cycle #350), pas un bug — inchangé.
9. Le badge « New » ne se réinitialise qu'à un vrai redémarrage complet de l'app — inchangé.
10. L'Urgent automatique à J+3 persiste après échéance mais reste borné à ~60s par `closeOverdue` — inchangé.
11. `supabase/migrations/20260901_visitor_claim_reset_pin.sql` (#366) et `20260902_pin_reset_requests_resolved_at.sql` (#367) — statut d'application non confirmé, inchangé.
12. Badge « New » et liseret bleu sont deux mécanismes indépendants (`NewIndicator` `{isNew, mine}`) depuis #371 — inchangé.
13. `visitorSession.ts` (AsyncStorage) n'est qu'un cache de confort depuis #358, pas la source de vérité de l'identité visiteur — inchangé.
14. `supabase/migrations/20260902_freemium_7day_window.sql` (#374, corrigée #386) confirmée appliquée, mais le job `check-freemium-expiry-daily` n'a toujours pas tourné sur un espace réellement expiré en conditions réelles — comportement de bout en bout (email + `cap_email_sent_at`) à surveiller en priorité au premier espace qui atteint sa 7ᵉ journée — inchangé.
15. Le plan `ISOLATION_SUPABASE.md` mériterait d'être réévalué maintenant que l'ancien site Vercel qui partageait l'instance a été supprimé (#375) — ne pas présumer qu'il est caduc sans le redemander à l'utilisateur — inchangé.
16. Le site marketing `avectoi.care` n'est toujours pas scaffoldé — inchangé.
17. Le gate Intervenant (§3.9 docx), toujours Premium, garde l'ancien comportement d'`Alert` bloquante — non harmonisé avec les deux styles de gate (masquage / `ConfirmModal`) introduits par #377-378 — inchangé.
18. `avectoi://auth/reset-password` dépend du flux PKCE (`flowType: "pkce"`, #383) — toute autre feature future utilisant un lien email Supabase Auth en bénéficie implicitement — inchangé.
19. Toute nouvelle colonne temporelle sur `reservations` doit utiliser `created_at` (le nom réel en base) — `timestamp` n'a jamais existé et a causé le bug corrigé par #386 — vérifier `supabase/schema_backups/schema_snapshot.sql` en cas de doute plutôt que de supposer — inchangé.
20. `CHECKLISTS_SUGGEREES.md` (inventaire du contenu des checklists, à la racine du dépôt) n'a pas été régénéré depuis l'ajout du sous-menu « Soin à domicile » (#416) — sa section « Soin à domicile » reflète encore l'ancien contenu plat, sans les 4 nouveaux modèles ni la structure en sous-menu — inchangé.
21. **Nouveau** : `assets/adaptive-icon.png` n'a reçu aucun des deux correctifs de logo (#420 anti-aliasing, #421 repositionnement) — l'anti-aliasing y créerait un halo gris visible autour de sa marge de sécurité transparente Android, non vérifiable sans device. À réévaluer si l'utilisateur confirme le halo sur un build réel.
22. **Nouveau** : `logo.png` à la racine du dépôt est une copie statique de `assets/icon-sans-512.png` au moment de ce commit — elle ne se met pas à jour automatiquement si l'asset source est retouché par une future PR ; à resynchroniser manuellement si besoin.

Points en suspens à reproposer sur demande : nettoyage du code mort post-assistant « Publier » (rappel 7), nettoyage de la branche distante `feat/entraide-creer-checklist` (commit orphelin `e9472c9`), harmonisation du gate Intervenant (rappel 17), réévaluation d'`ISOLATION_SUPABASE.md` (rappel 15), scaffold du site marketing `avectoi.care`, déploiement `avectoi-site` sur Infomaniak (bloqué sans les accès de l'utilisateur), rebase de `fix/souvenirs-rls-and-news-delete`, décision `docs/spec-web-upgrade`, isolation Supabase, taille de l'app/audit code mort (`AUDIT_RLS_TAILLE_CODE_MORT.md`), régénération de `CHECKLISTS_SUGGEREES.md` (rappel 20).
