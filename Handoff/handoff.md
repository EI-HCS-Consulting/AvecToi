# Handoff — AvecToi
_Généré le : 2026-09-14 (cycle #440-450, clos)_

## État global du projet

**Stack :** React Native + Expo SDK 51+, Expo Router, Supabase (BDD/Auth/Storage/Realtime/Edge Functions), EAS Build + EAS Update (channels development/preview/production, `expo-updates`, publication OTA automatique sur push `main` via `.github/workflows/eas-update-preview.yml`). expo-notifications, expo-calendar, expo-image-picker, expo-sharing + react-native-share (partage groupé). Resend (transactionnel + SMTP Supabase Auth, sous-domaine `notifications.avectoi.care`) branché sur les Edge Functions. Stripe côté web uniquement. Supabase CLI bloqué sur cette machine (App Control Policy Windows) : déploiement des migrations/Edge Functions exclusivement via le Dashboard Supabase, jamais auto-appliqué par Claude.

**⚠️ Règle Git STRICTE (CLAUDE.md) :** chaque modification, même un correctif ponctuel, part sur une branche neuve (depuis `origin/main` à jour) et se termine par sa propre PR poussée sur GitHub — jamais de nouveau commit ajouté à une branche dont la PR est déjà mergée. **Autorisation permanente : commit + push + PR dès qu'une tâche de code est terminée et vérifiée, sans attendre que l'utilisateur le redemande.** **L'utilisateur merge lui-même, puis teste** (le merge déclenche la publication OTA — sauf nouvelle dépendance native, qui nécessite un `eas build` + réinstallation complète). Toujours vérifier l'état réel d'une PR via `gh pr view <n> --json state,url` plutôt que de supposer.

**⚠️ Convention UI (CLAUDE.md, "Conventions UI") :** toute nouvelle confirmation utilise `ConfirmModal` (jamais `Alert.alert` natif) ; tout nouveau bouton reprend les couleurs/emoji/style déjà en usage à l'écran ; toute notification éphémère réutilise le pattern `showToast` existant.

**Repo GitHub :** `https://github.com/EI-HCS-Consulting/AvecToi`, branche `main` protégée par ruleset (PR obligatoire).

**PR ouverte, en attente de test/merge :** aucune. #440 à #450 (11 PR) toutes mergées, confirmé par l'utilisateur (« mergé »). Aucune migration SQL dans ce cycle à l'exception de `20260913_task_disengage_alerts.sql` (#446, nouvelle table). Ce cycle-ci (documentaire) livre uniquement docx + PRD + ce fichier.

**Livré — snapshot par domaine (V1 + V2 archivé) :**
- **Socle V1** (points 1-10 CLAUDE.md) : auth admin, accès visiteur par lien/QR/code dossier, calendrier + créneaux + réservation + PIN, Souvenirs, Nouvelles, Entraide + Soutien, 6 thèmes, "Prochaine disponibilité", ajout calendrier natif Android, notifications + emails Resend.
- **RGPD & Freemium/Premium** : purge différenciée par plan (Freemium 30j non prolongeable / Premium 60j + 30j renouvelable) + gate J-7 ; cap Freemium en essai de 7 jours glissants depuis la 1ère Visite (`lib/freemiumCap.ts`, scan quotidien pg_cron `check-freemium-expiry-daily`) ; prix Premium 7,99 € (paiement unique, Stripe côté web uniquement) ; 5 fonctionnalités Premium (rôle Intervenant, vue Hebdo, besoin 🆘 SOS Relais, Documents types, Chronologie/livret + prolongation RGPD) réparties entre masquage complet et popup `ConfirmModal` selon l'écran (le gate Intervenant reste sur l'ancien style `Alert` bloquante, non harmonisé — voir rappels).
- **Reconnaissance visiteur cross-device** : identité vérifiée côté serveur par prénom + nom + PIN (table `visitor_profiles`, 4 RPC `SECURITY DEFINER`), reset PIN par l'admin (fiche visiteur) ou en self-service visiteur (table `pin_reset_requests`), historique symétrique dans « 🔔 Mes alertes ».
- **Co-administration temporaire** : un admin désigne un visiteur ayant proposé du relais comme co-admin (accès Admin complet hors facturation/RGPD/gestion des autres co-admins), acceptation par email + code à 6 chiffres, tableau de bord `app/(admin)/coadmins.tsx` en 2 sections « 🆘 Besoins SOS » / « 🛡️ Co-Administrateurs », barre de progression relais partagée (`components/RelaisDayProgress.tsx`) réutilisée à plusieurs emplacements, accès Paramètres via onglet caché monté dans les `Tabs` du visiteur.
- **Entraide** : assistant « Publier » en popups chaînées par catégorie, besoin de relais ponctuel avec répartition par sous-périodes, Transport (lien Maps, propositions, droits de validation), Courses (produits récurrents, cochage attribué/verrouillé, multi-contributeurs), tri Urgent/proximité + regroupement fermés, badges « New », sélection multiple, checklists suggérées avec choix de destination de publication. **Cycle #429-433** : mur d'Entraide avec photo/avatar réelle du preneur en charge, avatars groupés pour les besoins Courses multi-contributeurs. **Cycle #435-438** : popup de rappel d'échéance du jour, « Modifier le besoin » ouvert à tout auteur, cochage automatique au claim courses réintroduit, bouton « ✓ Fait » + badge « Pris en charge / partiellement ». **Ce cycle (#440, affiné #441-444)** : refonte du modèle de données Courses — cocher un article (`bought_by_prenom/nom/at`, qui a coché) et l'avoir réellement acheté (`bought`, booléen distinct, posé seulement par une action « Fait ») sont désormais **décorrélés**, corrigeant un badge « Pris en charge » affiché à tort dès qu'un article était simplement coché sans être acheté ; « Se désinscrire » libère aussi les articles cochés-mais-non-achetés par la personne qui se désinscrit (#443) ; le filet de rattrapage historique qui laissait un seul clic « ✓ Fait » absorber des articles jamais cochés par personne a été retiré, resserrant `courseFaitEligible()` (#444) ; correctif intermédiaire d'un badge « Fait » en doublon avec le bouton « Se désinscrire » (#442) et d'un cas où une liste réclamée (« claim ») restait signalée « encore ouverte » (#441). **Ce cycle (#445)** : le popup « Modifier le besoin » se simplifie en édition (par opposition à la création) — catégorie et libellé « Relais » masqués (catégorie non modifiable après publication), mention « Photo (optionnelle) » retirée du bouton photo, toggle « Urgent » en texte simple, « Me désengager » de nouveau strictement réservé à la personne ayant réellement pris en charge le besoin (`isMine`, plus tout claimant), crayon d'édition (et son raccourci par appui long) masqué une fois l'échéance dépassée. **Ce cycle (#446-448)** : nouvelle alerte de désengagement tardif — un désengagement (« Se désinscrire ») survenu à J-2 ou moins de l'échéance d'un besoin est désormais tracé (nouvelle table `task_disengage_alerts`, `lib/taskDisengageAlerts.ts`) et remonté à l'admin comme à l'auteur du besoin dans une nouvelle section « 🔓 Désengagements » de « 🔔 Mes alertes » (#446) ; les boutons « Se désinscrire » ne sont plus proposés une fois l'échéance dépassée (`taskPastDeadline`, y compris pour les courses non achetées), l'aperçu d'une liste de courses (`ShoppingListModal`) passe en lecture seule pour un non-admin après échéance, et le bouton « Se désinscrire » des courses est isolé sous le bloc des contributeurs (#447) ; le grisage des jours passés du calendrier est harmonisé à une opacité de 0.5, désormais identique entre les vues Mensuel et Hebdo (`WeekStrip`) (#448). **Ce cycle (#449-450)** : sur le mur d'Entraide, l'auteur d'un besoin est désormais identifié sur la ligne « 🗓️ Publié le » par sa vraie photo de profil (ou, à défaut, un avatar aux initiales prénom + nom) — voir aussi « Nouvelles/Soutien » ci-dessous pour le même mécanisme appliqué aux publications et messages ; un besoin Repas/Affaires/Administratif/Autre pris en charge via « Je m'en occupe » propose désormais à la personne qui l'a pris en charge les mêmes boutons « ✓ C'est fait » et « Se désinscrire » que Courses/Transport/Relais, au lieu de dépendre exclusivement du marquage « Fait » par l'admin (#450).
- **Ma semaine** *(onglet, cycle #424-427)* : dernière position de la 2ᵉ barre d'onglets (Calendrier / Créneaux / Nuits / Infos / Partager / **Ma semaine**), visible admin + visiteur, masqué intervenant, ouvert par défaut à la connexion — composant partagé `components/MyWeekScreen.tsx`. Tuile « 🟠 Mon agenda » : visites/nuitées de la semaine fusionnées chronologiquement avec les trajets transport sur lesquels la personne est engagée. Tuile « 🟢 Mes engagements » : cartes détaillées, filtre engagé-seulement, avatars des preneurs en charge. Aucun changement dans ce cycle (#440-450).
- **Nouvelles/Soutien** : indicateur « New » (barre bleue = identité connectée, badge/cadre rouge = non-lu — deux mécanismes indépendants depuis #371), fiabilité en arrière-plan (filet `AppState`), permissions admin/visiteur harmonisées. **Ce cycle (#449, corrigé #450)** : sur Nouvelles et sur le Mur de soutien, le pictogramme générique ou l'initiale unique est remplacé par la vraie photo de profil de l'auteur ou, à défaut, un avatar aux initiales prénom + nom (réutilise le roster déjà en place pour les preneurs en charge d'un besoin, `lib/visitorRoster.ts`) ; les cartes/bulles reprennent une disposition façon WhatsApp (les miennes à droite, celles des autres à gauche) avec un retrait **fixe** d'1/5 de la largeur d'écran (`SCREEN_W/5`) depuis le bord opposé, appliqué à toutes les cartes/bulles du côté concerné — corrige un premier essai (#449) qui réduisait chaque carte/bulle à la largeur de son contenu (`maxWidth`) au lieu de l'étirer jusqu'à ce retrait fixe, ce qui donnait un alignement visuellement incohérent d'une carte à l'autre.
- **Calendrier/Visites** : `HomeCalendarScreen` commun admin/visiteur, Mensuel/Hebdo (Hebdo Premium), réservations récurrentes, regroupement par créneau, cascade accompagnant sur modification. **Ce cycle (#448)** : grisage des jours passés uniformisé à opacité 0.5 entre Mensuel et Hebdo (`WeekStrip`).
- **Mon Compte** : Mes souvenirs (page personnelle), Mes documents (courriers pré-remplis), Mes engagements de relais, Mes alertes (avec Archives repliables, **nouvelle section « 🔓 Désengagements » depuis #446**), Mon code PIN.
- **Rôle Intervenant** : masqué derrière `INTERVENANT_ROLE_ENABLED=false` depuis #291 (pas supprimé — code archivé dans `Développement V2/`), Premium.
- **Identité visuelle (logo)** : stable depuis le cycle #420-423, aucun changement dans ce cycle.

**Connu, non bloquant :**
- Branche distante `feat/entraide-creer-checklist` (PR #140, déjà mergée) porte un commit orphelin (`e9472c9`) — nettoyage jamais confirmé, sans conséquence fonctionnelle.
- Trois implémentations distinctes du désengagement/désinscription coexistent volontairement (`performUnclaim` dans `Entraide.tsx`, `lib/taskDisengage.ts` dans les deux `account.tsx`) — convention du repo, à garder en tête si une future demande vise à tout unifier. **Ce cycle (#446-447)** ajoute une contrainte partagée par les trois (`taskPastDeadline`) sans les fusionner.
- **Migrations manuelles au statut d'application non reconfirmé** : `supabase/migrations/20260913_task_disengage_alerts.sql` (#446, **nouveau ce cycle** — crée la table `task_disengage_alerts` ; sans cette exécution manuelle, la nouvelle section « 🔓 Désengagements » de « 🔔 Mes alertes » restera vide côté production, sans erreur visible), `20260909_personal_checklist_context_widen2.sql` (#417), `20260821_visitor_profiles_relation.sql` (#278), `20260827_task_relais_coverage.sql` (#305), `20260830_deleted_content_seen.sql` (#337), `20260901_claimed_bought_at.sql` (#354), `20260901_visitor_claim_reset_pin.sql` (#366), `20260902_pin_reset_requests_resolved_at.sql` (#367). **Confirmées appliquées** : `20260827_reservation_change_history_seen.sql`, `20260902_freemium_7day_window.sql`, `20260904_rgpd_purge_premium_upgrade_extension.sql`, `20260910_patient_spaces_admin_photo.sql`. Le job pg_cron `check-freemium-expiry-daily` n'a toujours pas tourné sur un espace réellement expiré en conditions réelles.
- **Ruleset GitHub `main-protection`** : le check de statut "Vercel" a été retiré manuellement par l'utilisateur — Claude Code ne peut pas modifier les rulesets lui-même (`gh api ... --method PUT` bloqué par le classifieur de sécurité auto-mode).
- **`CHECKLISTS_SUGGEREES.md`** (inventaire complet du contenu des checklists, à la racine du dépôt) toujours non régénéré depuis l'ajout du sous-menu « Soin à domicile » (#416).
- **Documentation à jour** : `Documentation/Documentation Fonctionnalités.docx` synchronisée jusqu'à la PR #450 (**V1.37**, 14/09/2026, livrée par ce cycle documentaire) — cinq cellules existantes complétées par ajout de phrase, sans nouvelle sous-section ni renumérotation : « Fil antichronologique » (5.7 Nouvelles), « Cycle de vie d'un besoin » et « Liste de courses » (5.9 Entraide), « Publier un message » (5.10 Soutien), « 🔔 Mes alertes » (5.11 Compte). `PRD_AvecToi_v1_4.md` **passé de v1.35 à v1.36** : nouveau bloc changelog en tête (6 entrées), §3.8 Entraide (et le point commun §3.8 Soutien) amendés, §3.7 Nouvelles amendé.

**En cours / pas commencé (reporté, à reprendre sur demande) :**
- `fix/souvenirs-rls-and-news-delete` : branche stale, 2 migrations RLS jamais exécutées en prod (`AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Isolation Supabase (séparer l'instance prod du site web historique) : plan dans `ISOLATION_SUPABASE.md`, à réévaluer maintenant que l'ancien site Vercel qui la motivait a été supprimé (#375) — ne pas présumer le plan caduc sans le redemander à l'utilisateur.
- `docs/spec-web-upgrade` : en attente d'une décision utilisateur.
- Points 13-14 (EAS Build APK signé, fiche Play Store) : pas commencés.
- Site marketing `avectoi.care` (dépôt séparé, Next.js, Infomaniak) : toujours pas scaffoldé. DNS/Resend opérationnels pour `support@avectoi.care`.
- Gate Intervenant (§3.9 docx) toujours en `Alert` bloquante, non harmonisé avec le style masquage/`ConfirmModal` introduit par #377-378 — à signaler si une future PR touche ce chantier.
- Nettoyage code mort post-assistant « Publier » (`!editTask &&` dans `taskForm`), à reproposer explicitement sur demande.
- `CHECKLISTS_SUGGEREES.md` à régénérer pour refléter le sous-menu « Soin à domicile » et les 4 nouveaux modèles.
- Unification des trois implémentations de désengagement/désinscription (voir « Connu, non bloquant »), à reproposer explicitement sur demande.
- `AUDIT_RLS_TAILLE_CODE_MORT.md`, `PRD_ClaudeCode_Site_avectoi_care_v3.md`, `eslint.config.js`, `AvecToi-Site/` : fichiers non suivis présents dans l'arbre de travail, non touchés par ce cycle, jamais stagés (exclusion permanente).

## Historique cumulé
- Jusqu'au 17/08/2026 (PR #7-224) : socle V1 complet (auth, calendrier/créneaux/PIN, Souvenirs, Nouvelles, Entraide/Soutien, thèmes, notifications/emails), refonte intervenant Phase A/B, RGPD 90j + purge auto + sauvegarde schéma, checklists perso, EAS Update automatisé, panneau admin Calendrier + permissions Nouvelles, planning intervenants multi-espaces.
- 17/08 → 27/08/2026 (PR #225-301) : "Mes souvenirs", checklists proche-aidant guidées (11 modèles), Mes documents (courriers pré-remplis auto), liste de courses temps réel, besoin de relais ponctuel, réservations récurrentes, lien visiteur-patient, RGPD 60j+30j, retrait du rôle Intervenant en V1 (masqué, archivé V2), calendrier `HomeCalendarScreen` unifié admin/visiteur.
- 27/08 → 30/08/2026 (PR #303-345) : chantier SOS Relais (répartition par sous-périodes, alertes centralisées « Mes alertes »), assistant « Publier » Entraide en popups chaînées, Transport (lien Maps, propositions, validation), badges de non-lu (cadre orange puis indicateur « New »), une dizaine de correctifs UX groupés après tests utilisateur.
- 31/08 → 02/09/2026 (PR #346-371) : indicateur « New » stabilisé (fiabilité `AppState` en arrière-plan), tag Urgent auto à J+3, reconnaissance visiteur cross-device par PIN serveur (`visitor_profiles`), reset PIN admin + self-service visiteur, badge « New »/liseret bleu découplés en deux mécanismes indépendants.
- 02/09 → 05/09/2026 (PR #373-387) : cap Freemium repositionné en essai glissant de 7 jours, nettoyage de l'ancien MVP Vercel mort du dépôt, gating Freemium/Premium aligné sur le site (7,99 €, purge RGPD différenciée par plan), flux « Mot de passe oublié ? » (fix définitif via `flowType: "pkce"`), correctif du bug bloquant `column "timestamp" does not exist` sur toute réservation de Visite.
- 04/09 → 08/09/2026 (PR #390-411, narrative PR-par-PR non détaillée dans ce fichier — voir GitHub) : chantier co-administration temporaire complet — désignation depuis une proposition de relais, acceptation email + code, tableau de bord « Besoins SOS »/« Co-Administrateurs », RPC serveur dédiées avec restrictions RGPD/facturation, badge « New » sur propositions non traitées, barre de progression relais introduite dans Mon Compte.
- 08/09 → 09/09/2026 (PR #412-414) : barre de progression relais réutilisée sur 3 emplacements ; accès « Paramètres Co-Administrateur » stabilisé en onglet caché ; correctif d'affichage de troncature de date.
- 09/09/2026 (PR #416-417) : bibliothèque de checklists passée à 15 modèles — sous-menu « 🏠 Soin à domicile » (5 situations) ; correctifs de navigation et de contrainte CHECK en base.
- 09/09/2026 (PR #418, #420-421) : parité d'affichage des liens officiels multiples ; qualité visuelle du logo (anti-aliasing puis repositionnement de l'anneau décoratif).
- 09/09 → 10/09/2026 (PR #423-427) : correctif v2 du repositionnement du logo ; nouvel onglet « 🗓️ Ma semaine » ; popup d'alerte + badge « New » sur les propositions de transport non vues.
- 12/09/2026 (PR #429-433) : fermeture automatique + badge « Validée » sur un besoin Transport validé, liseré vert « Aujourd'hui », couleurs par contributeur pour les listes de courses, correctifs divers d'avatars/resynchronisation.
- 12/09 → 13/09/2026 (PR #435-438) : popup de rappel d'échéance du jour, « Modifier le besoin » ouvert à tout auteur, cochage automatique au claim courses réintroduit, bouton « ✓ Fait » + badge « Pris en charge / partiellement ».
- 13/09 → 14/09/2026 (PR #440-450) : refonte du modèle Courses — attribution (cocher) et achat réel (« Fait ») décorrélés, avec correctifs itératifs sur la désinscription et le filet de rattrapage historique (#440-444) ; popup « Modifier le besoin » simplifié en édition, « Me désengager » de nouveau restreint au preneur en charge réel (#445) ; alerte de désengagement tardif J-2 (nouvelle table `task_disengage_alerts`, section « 🔓 Désengagements » dans Mes alertes), blocage des désinscriptions après échéance, grisage calendrier uniformisé Mensuel/Hebdo (#446-448) ; avatars photo-ou-initiales et bulles à largeur fixe façon WhatsApp sur Nouvelles/Soutien/auteur de besoin, corrigés en cours de cycle après un premier essai à largeur variable, et boutons « ✓ C'est fait »/« Se désinscrire » étendus aux besoins génériques pris en charge (#449-450).

## 1. Objectif de la session
Onze PR distinctes, toutes déjà mergées par l'utilisateur avant le début de cette session documentaire, livrées sur plusieurs demandes utilisateur successives (correction du modèle Courses, simplification du popup d'édition, alerte de désengagement tardif, puis retour utilisateur sur l'affichage des avatars/bulles) :
- **PR #440** (`fix/courses-claim-vs-purchase`) : décorrélation d'attribution (cocher) et d'achat réel — `bought_by_prenom/nom/at` (qui a coché) et `bought` (achat effectif, posé seulement par « Fait ») deviennent deux notions distinctes ; corrige un badge « Pris en charge » affiché à tort dès qu'un article était simplement coché.
- **PR #441** (`fix/courses-claim-list-still-open`) : correctif d'un cas où une liste réclamée restait signalée « encore ouverte ».
- **PR #442** (`fix/courses-fait-badge-doublon-desinscrire`) : correctif d'un badge « Fait » affiché en doublon avec le bouton « Se désinscrire ».
- **PR #443** (`fix/courses-desinscription-libere-articles`) : « Se désinscrire » libère aussi les articles cochés-mais-non-achetés par la personne qui se désinscrit.
- **PR #444** (`fix/courses-fait-ne-touche-pas-articles-non-coches`) : le filet de rattrapage historique qui laissait un clic « ✓ Fait » absorber des articles jamais cochés par personne est retiré (`courseFaitEligible()` resserré).
- **PR #445** (`fix/besoin-edit-popup-simplifie`) : simplification du popup « Modifier le besoin » en édition (catégorie/Relais masqués, mention « optionnelle » retirée, toggle Urgent simple, « Me désengager » restreint à `isMine`, crayon masqué après échéance).
- **PR #446** (`fix/besoin-desengagement-j2-alerte`) : nouvelle alerte de désengagement tardif — table `task_disengage_alerts`, `lib/taskDisengageAlerts.ts`, section « 🔓 Désengagements » dans « 🔔 Mes alertes » pour l'admin et l'auteur du besoin, sur tout « Se désinscrire » survenu à J-2 ou moins de l'échéance.
- **PR #447** (`fix/courses-bouton-desinscrire-calendrier-opacite`) : bouton « Se désinscrire » des courses isolé sous le bloc des contributeurs (`claimBtnText` centré), `ShoppingListModal` en lecture seule pour un non-admin après échéance, boutons « Se désinscrire » masqués une fois l'échéance dépassée (`taskPastDeadline`).
- **PR #448** (`fix/hebdo-grisage-jours-passes`) : grisage des jours passés harmonisé à opacité 0.5 entre les vues Mensuel et Hebdo (`WeekStrip`).
- **PR #449** (`fix/avatars-nouvelles-soutien-entraide-bulles`) : avatars photo-ou-initiales sur Nouvelles, Soutien et l'auteur d'un besoin sur le mur d'Entraide ; bulles/cartes de publication en disposition façon WhatsApp (miennes à droite, autres à gauche) — premier essai avec `maxWidth` (largeur réduite au contenu).
- **PR #450** (`fix/bulles-largeur-fixe-boutons-besoin`) : suite à un retour utilisateur sur l'incohérence visuelle des bulles à largeur variable de #449, remplacement par un retrait **fixe** `SCREEN_W/5` depuis le bord opposé (`farInset`) sur toutes les bulles du côté concerné ; ajout des boutons « ✓ C'est fait » et « Se désinscrire » pour un besoin Repas/Affaires/Administratif/Autre pris en charge (jusque-là réservés à Courses/Transport/Relais, ou dépendants du marquage « Fait » par l'admin).

## 2. État actuel
- Code : PR #440-450 mergées, une seule migration SQL ce cycle (`20260913_task_disengage_alerts.sql`, #446, statut d'application non reconfirmé — voir « Connu, non bloquant ») — aucune tâche de code restante identifiée.
- `Documentation/Documentation Fonctionnalités.docx` : V1.36 → **V1.37** (14/09/2026). Aucune nouvelle sous-section — cinq cellules existantes complétées par une phrase : « Fil antichronologique » (5.7 Nouvelles), « Cycle de vie d'un besoin » et « Liste de courses » (5.9 Entraide), « Publier un message » (5.10 Soutien), « 🔔 Mes alertes » (5.11 Compte).
- `PRD_AvecToi_v1_4.md` : **v1.35 → v1.36**. Nouveau bloc changelog en tête (6 entrées) ; §3.7 Nouvelles et §3.8 Entraide/Soutien amendés — aucune nouvelle section, sous le seuil « portée produit » du critère 5ter.
- `Handoff/handoff.md` : ce fichier — en-tête, « État global » (bullets Entraide/Nouvelles-Soutien/Calendrier amendés, ligne Documentation à jour, migration ajoutée), « Historique cumulé » complété d'une ligne, section détaillée réécrite.

## 3. Fichiers concernés
- `components/ShoppingListModal.tsx`, `components/Entraide.tsx` (#440-444, #447) — décorrélation `bought`/`bought_by_*`, `courseFaitEligible()` resserré, libération des articles cochés-non-achetés à la désinscription, bouton « Se désinscrire » des courses isolé sous le bloc des contributeurs, lecture seule non-admin après échéance.
- `components/Entraide.tsx` (#445, #450) — popup « Modifier le besoin » simplifié en édition (catégorie/Relais masqués, toggle Urgent simple, `isMine` pour « Me désengager », crayon masqué après échéance) ; boutons « ✓ C'est fait »/« Se désinscrire » étendus aux besoins Repas/Affaires/Administratif/Autre pris en charge.
- `lib/taskDisengageAlerts.ts` (#446, nouveau fichier), `components/MyAlertsModal.tsx` (#446) — nouvelle section « 🔓 Désengagements », table `task_disengage_alerts`.
- `lib/taskDisengage.ts` / logique de désinscription partagée (#447) — `taskPastDeadline`, boutons masqués après échéance.
- Composants calendrier Mensuel + `components/WeekStrip.tsx` (#448) — grisage jours passés uniformisé opacité 0.5.
- `components/NewsFeed.tsx`, `components/Soutien.tsx`, `components/Entraide.tsx`, `lib/visitorRoster.ts` (#449-450) — avatars photo-ou-initiales, bulles/cartes façon WhatsApp avec retrait fixe `SCREEN_W/5` (`farInset`, remplace l'approche `maxWidth` de #449).
- `Documentation/Documentation Fonctionnalités.docx`, `PRD_AvecToi_v1_4.md` — voir §2.

## 4. Ce qui a échoué
Le premier essai de #449 (bulles/cartes façon WhatsApp avec `maxWidth`, la bulle se réduisant à la largeur de son contenu) donnait un alignement visuellement incohérent d'une carte à l'autre sur un même côté — signalé par l'utilisateur après test sur device. Corrigé par #450 : remplacement par un retrait fixe `SCREEN_W/5` (`farInset`) depuis le bord opposé, garantissant que toutes les bulles d'un même côté s'alignent sur la même limite. Rien d'autre côté code dans ce cycle.

## 5. Prochaine étape
Aucune tâche de code en attente identifiée par l'utilisateur — le cycle est clos une fois cette documentation committée/poussée. **Non testé sur un appareil réel pendant cette session** (pas d'accès `adb`/émulateur dans cet environnement) : #440-450 vérifiées par relecture de code uniquement côté Claude ; l'utilisateur a confirmé le fonctionnement de bout en bout après chaque merge, y compris le retour ayant motivé le correctif #450. Committer les 3 documents (docx + PRD + ce handoff) sur la branche `docs/handoff-bulles-boutons-besoin-pr440-450` (déjà créée depuis `origin/main`), pousser, ouvrir la PR.

**⚠️ Rappels à transmettre à l'utilisateur (reportés depuis les cycles précédents, statuts inchangés sauf mention contraire) :**
1. **Nouveau** : `supabase/migrations/20260913_task_disengage_alerts.sql` (#446) — à exécuter manuellement via le Dashboard Supabase (SQL Editor) ; sans ça, la nouvelle section « 🔓 Désengagements » de « 🔔 Mes alertes » restera vide côté production, sans erreur visible.
2. **Nouveau** : depuis #450, chercher `farInset` (et non plus `maxWidth`/`alignItems`) pour retrouver la logique de largeur des bulles/cartes façon WhatsApp dans `NewsFeed.tsx`, `Soutien.tsx` et `Entraide.tsx` — l'ancienne approche à largeur variable de #449 a été remplacée, pas complétée.
3. `supabase/migrations/20260909_personal_checklist_context_widen2.sql` (#417) — à exécuter manuellement via le Dashboard Supabase (SQL Editor) ; sans ça, publier en privé une checklist « ménage », « portage de repas », « télésurveillance » ou « matériel médical » échoue encore avec une erreur de contrainte CHECK. Inchangé, toujours non confirmé.
4. `supabase/migrations/20260821_visitor_profiles_relation.sql` (#278) — statut d'application non confirmé, inchangé.
5. `supabase/migrations/20260827_task_relais_coverage.sql` (#305) — statut d'application non reconfirmé explicitement, inchangé.
6. Symptôme (b) du cycle #307-308 — alertes SOS relais qui semblaient disparaître — reste seulement instrumenté (logs d'erreur), jamais conclusivement corrigé, inchangé.
7. `supabase/migrations/20260830_deleted_content_seen.sql` (#337) — statut d'application non confirmé, inchangé.
8. `supabase/migrations/20260901_claimed_bought_at.sql` (#354) — statut d'application non confirmé, inchangé.
9. Nettoyage du code mort post-assistant « Publier » (`!editTask &&` dans `taskForm`) — à reproposer explicitement sur demande, inchangé.
10. Le « Modifier » visible par PIN partagé sur une réservation d'autrui reste un comportement explicitement voulu (cycle #350), pas un bug — inchangé.
11. Le badge « New » ne se réinitialise qu'à un vrai redémarrage complet de l'app — inchangé.
12. L'Urgent automatique à J+3 persiste après échéance mais reste borné à ~60s par `closeOverdue` — inchangé.
13. `supabase/migrations/20260901_visitor_claim_reset_pin.sql` (#366) et `20260902_pin_reset_requests_resolved_at.sql` (#367) — statut d'application non confirmé, inchangé.
14. Badge « New » et liseret bleu sont deux mécanismes indépendants (`NewIndicator` `{isNew, mine}`) depuis #371 — inchangé.
15. `visitorSession.ts` (AsyncStorage) n'est qu'un cache de confort depuis #358, pas la source de vérité de l'identité visiteur — inchangé.
16. `supabase/migrations/20260902_freemium_7day_window.sql` (#374, corrigée #386) confirmée appliquée, mais le job `check-freemium-expiry-daily` n'a toujours pas tourné sur un espace réellement expiré en conditions réelles — comportement de bout en bout (email + `cap_email_sent_at`) à surveiller en priorité au premier espace qui atteint sa 7ᵉ journée — inchangé.
17. Le plan `ISOLATION_SUPABASE.md` mériterait d'être réévalué maintenant que l'ancien site Vercel qui partageait l'instance a été supprimé (#375) — ne pas présumer qu'il est caduc sans le redemander à l'utilisateur — inchangé.
18. Le site marketing `avectoi.care` n'est toujours pas scaffoldé — inchangé.
19. Le gate Intervenant (§3.9 docx), toujours Premium, garde l'ancien comportement d'`Alert` bloquante — non harmonisé avec les deux styles de gate (masquage / `ConfirmModal`) introduits par #377-378 — inchangé.
20. `avectoi://auth/reset-password` dépend du flux PKCE (`flowType: "pkce"`, #383) — toute autre feature future utilisant un lien email Supabase Auth en bénéficie implicitement — inchangé.
21. Toute nouvelle colonne temporelle sur `reservations` doit utiliser `created_at` (le nom réel en base) — `timestamp` n'a jamais existé et a causé le bug corrigé par #386 — vérifier `supabase/schema_backups/schema_snapshot.sql` en cas de doute plutôt que de supposer — inchangé.
22. `CHECKLISTS_SUGGEREES.md` (inventaire du contenu des checklists, à la racine du dépôt) n'a pas été régénéré depuis l'ajout du sous-menu « Soin à domicile » (#416) — inchangé.
23. `assets/adaptive-icon.png` n'a reçu aucun des correctifs de logo (#420 anti-aliasing, #421/#423 repositionnement) — non vérifiable sans device. Inchangé.
24. `logo.png` à la racine du dépôt est une copie statique de `assets/icon-sans-512.png` au moment de chaque commit — ne se met pas à jour automatiquement si l'asset source est retouché. Inchangé.
25. `TransportProposalAlertModal.tsx` (#427) ne marque une proposition « vue » que sur une action explicite, jamais à la simple fermeture du popup — même convention que `RelaisAlertModal.tsx` et désormais que `task_disengage_alerts` (#446, marquage explicite requis). Inchangé.
26. Depuis #429, `validateTransportLeg` pose `status="ferme"` dès qu'un horaire est validé — un besoin Transport validé ne reste plus jamais « ouvert ». Inchangé.
27. `lib/visitorRoster.ts` porte deux façons de résoudre une identité en photo — `loadPhotoRoster(spaceId)` (#431, utilisé par `Entraide.tsx`, `NewsFeed.tsx`, `Soutien.tsx` depuis #449) et l'équivalent inline pré-existant dans `MyWeekScreen.tsx` (jamais refactoré vers le helper partagé). À unifier si une future demande touche encore à cette zone.
28. **Nouveau** : trois implémentations distinctes du désengagement/désinscription (`performUnclaim` dans `Entraide.tsx`, `lib/taskDisengage.ts` dans les deux `account.tsx`) ont chacune reçu leur propre ajout de `taskPastDeadline` ce cycle (#446-447) sans être fusionnées — à unifier si une future demande vise à tout centraliser.

Points en suspens à reproposer sur demande : nettoyage du code mort post-assistant « Publier » (rappel 9), nettoyage de la branche distante `feat/entraide-creer-checklist` (commit orphelin `e9472c9`), harmonisation du gate Intervenant (rappel 19), réévaluation d'`ISOLATION_SUPABASE.md` (rappel 17), scaffold du site marketing `avectoi.care`, déploiement `avectoi-site` sur Infomaniak (bloqué sans les accès de l'utilisateur), rebase de `fix/souvenirs-rls-and-news-delete`, décision `docs/spec-web-upgrade`, isolation Supabase, taille de l'app/audit code mort (`AUDIT_RLS_TAILLE_CODE_MORT.md`), régénération de `CHECKLISTS_SUGGEREES.md` (rappel 22), unification des trois implémentations de désengagement (rappel 28).
