# Handoff — AvecToi
_Généré le : 2026-08-18_

## État global du projet

**Stack :** React Native + Expo SDK 51+, Expo Router, Supabase (BDD/Auth/Storage/Realtime/Edge Functions), EAS Build + EAS Update (channels development/preview/production, `expo-updates`, publication OTA automatique sur push `main` via `.github/workflows/eas-update-preview.yml`). expo-notifications, expo-calendar, expo-image-picker, expo-sharing (partage simple) + react-native-share (partage groupé multi-fichiers, nouvelle dépendance native #245). Resend (transactionnel) branché sur les Edge Functions. Stripe côté web uniquement. Supabase CLI bloqué sur cette machine (App Control Policy Windows) : déploiement des migrations/Edge Functions exclusivement via le Dashboard Supabase.

**⚠️ Règle Git STRICTE (CLAUDE.md) :** chaque modification, même un correctif ponctuel, part sur une branche neuve (depuis `origin/main` à jour) et se termine par sa propre PR poussée sur GitHub — jamais de nouveau commit ajouté à une branche dont la PR est déjà mergée. **Autorisation permanente : commit + push + PR dès qu'une tâche de code est terminée et vérifiée, sans attendre que l'utilisateur le redemande** — ne jamais s'arrêter en proposant simplement le diff ou en demandant confirmation avant de committer. **L'utilisateur merge lui-même, puis teste** (le merge déclenche la publication OTA qui charge le nouveau bundle sur son development build — aucun autre moyen de tester une modif avant de merger, **sauf si la modif ajoute une nouvelle dépendance native** : dans ce cas l'OTA seule ne suffit pas, un `eas build` + réinstallation complète de l'APK est nécessaire, voir "Testé et confirmé" ci-dessous). Le merge et le push sur `main` restent toujours à l'utilisateur.

**⚠️ Règle Git STRICTE (CLAUDE.md) :** chaque modification, même un correctif ponctuel, part sur une branche neuve (depuis `origin/main` à jour) et se termine par sa propre PR poussée sur GitHub — jamais de nouveau commit ajouté à une branche dont la PR est déjà mergée. **L'utilisateur merge d'abord, puis teste** (le merge déclenche la publication OTA qui charge le nouveau bundle sur son téléphone — il n'a aucun autre moyen de tester une modif avant de merger). Toujours vérifier l'état réel d'une PR via `gh pr view <n> --json state,mergedAt` plutôt que de supposer.

**Repo GitHub :** `https://github.com/EI-HCS-Consulting/AvecToi`, branche `main` protégée par ruleset (PR obligatoire).

**PR ouverte, en attente de test/merge :** aucune.

**Livré (V1 + V2 partiel) :** tout le socle (points 1-10 CLAUDE.md), refonte intervenant complète (métiers, accès, priorité créneaux configurable, canal Nouvelles séparé, verrou Premium sur l'activation du rôle), RGPD **60j par défaut + prolongation gratuite 30j, gate J-7, popup in-app systématique en plus de l'email** (évolution du 90j historique, voir #232-234 ci-dessous) + purge auto + sauvegarde schéma hebdo, checklists perso réutilisables, rattachement multi-espaces intervenant, onboarding séquencé, cap freemium (8 visites), Paramètres 4 sections + historique, emails Resend (annulation, cap atteint, confirmation invité, demande de changement de nom, confirmation de créneau intervenant), Chronologie, mode "1 visite/jour" unifié, icône adaptive Android, EAS Update automatisé, liens d'invitation vers `app.avectoi.care`. Flux "Demande de changement de nom" fonctionnel. Texte de partage personnalisé au prénom du patient. "Mes Checklists" restructuré avec flux d'ajout d'item intégré et répétable. Convention "popup centré + couleurs du thème" appliquée à 23+ popups. Entraide/Publier/Administratif dispose de son propre flux de création de checklist. **Calendrier principal (visiteur/admin/intervenant) refondu** : bascule unifiée Mensuel/Hebdo + Visites/Soins, vue Hebdo réservable pour tous les rôles, grisage pré-admission, marqueurs d'admission/sortie, planning intervenant scindé "Soins planifiés"/"Historique". **Panneau admin Calendrier + permissions Nouvelles** (#192-200), **Planning intervenants multi-espaces "Mes espaces patients"** avec édition croisée des créneaux/soins (#201-207), **stabilisation planning cross-space** : couleurs, popups soin, longpress carte, créneaux déjà pris, sécurité nom patient, navigation jour (#208-223). **"Mes Modèles" intervenant** (checklists privées réutilisables entre espaces patients) : import piloté par un popup de sélection d'items, anti-doublons global, complétion incrémentale, suppression par appui long (#225-227). **Fiche intervenant express** côté admin (création sans compte, `pin` nullable, email partout) + confirmation de créneau réservé envoyée par SMS/email (#235). **Calendrier des visites (visiteur)** refondu façon Planning intervenant : liseré de couleur par visiteur, légende filtrable, fond pastel, popup "Modifier ce créneau", affichage des accompagnants sur la ligne principale, blocage du tap sur créneau déjà passé, correctif de sécurité (vérification systématique de propriété avant Modifier/Y Aller) (#236-238). **Nouvelle organisation des onglets** : "Accueil" ajouté en tête de barre (visiteur/admin), "Souvenirs" retiré de la barre mais toujours accessible (Compte → Mes souvenirs, fiche patient), vue "Médias" en grille dans Nouvelles (#239-240). **"Mes souvenirs"** : nouvelle page personnelle (Compte → bouton dédié) séparant "Photos publiées" et "Photos téléchargées", sélection multiple avec partage groupé en un seul envoi natif via `react-native-share` (#241-246, détaillé ci-dessous).

**Nouveau ce cycle (#241 → #246) — "Mes souvenirs" : page personnelle, sélection multiple, partage groupé :**
- PR #241 : "Mes souvenirs" quitte l'accordéon de Mon Compte pour devenir un bouton plein-largeur dédié (entre la grille de contributions et "Mes Checklists"), ouvrant `components/MesSouvenirs.tsx` — deux sections dépliables : "Photos publiées" (dérivées à la volée de `news_entries`/`support_messages`, filtrées par nom pour un visiteur, `author_pin === "ADMIN"` pour l'admin — aucune table dédiée), "Photos téléchargées" (nouvelle table `saved_media`, alimentée à chaque téléchargement/partage d'une photo d'autrui). Nouveau helper partagé `lib/mediaShare.ts` (`downloadAndShare`/`isShareAvailable`/`logSavedMedia`) qui factorise un pattern jusque-là dupliqué dans `NewsFeed.tsx`/`Soutien.tsx`/`SouvenirsGallery.tsx`.
- PR #242 : sélection multiple (appui long, barre de sélection, "Tout sélect.", suppression du choix pièce par pièce) portée dans les grilles Médias de Nouvelles et Soutien, pour télécharger/ajouter plusieurs photos à "Mes souvenirs" en une seule action.
- PR #243 : après premier test réel par l'utilisateur — (a) les téléchargements ne remontaient pas dans "Mes souvenirs" : `logSavedMedia` était gardé par le PIN, vide pour tout visiteur n'ayant encore rien publié ; corrigé en ajoutant `saved_by_prenom`/`saved_by_nom` et en faisant correspondre par nom, comme "Photos publiées" ; (b) **faille de sécurité corrigée** : `sessionPinMatches` ne vérifiait que le PIN à 4 chiffres (10 000 combinaisons, collisions possibles) sans jamais croiser le nom — corrigé au niveau du helper partagé (exige désormais PIN + prénom/nom), répercuté sur ses 5 points d'appel dans 3 fichiers (`NewsFeed.tsx`, `Entraide.tsx`, `BookingFlow.tsx`), pas seulement Nouvelles où le bug avait été signalé ; (c) logo AvecToi dans la feuille de partage native : confirmé impossible (feuille 100 % contrôlée par l'OS, aucune API de branding côté `expo-sharing`).
- PR #244 : la migration de #243 avait été mergée avant qu'un commit correctif ne soit poussé (orphelin sur la branche fermée) — l'utilisateur a obtenu `ERROR: relation "saved_media" does not exist` car la toute première migration créant la table (#241) n'avait en réalité jamais été appliquée en prod. Corrigé en rendant la migration idempotente (`CREATE TABLE IF NOT EXISTS` + `DROP POLICY IF EXISTS`/`CREATE POLICY`), fonctionne qu'elle parte de zéro ou par-dessus une table déjà partiellement créée. Migration confirmée exécutée avec succès par l'utilisateur.
- PR #245 : deux demandes — (a) sélectionner plusieurs médias déclenchait N partages natifs successifs (un fichier à la fois) au lieu d'un seul groupé : limitation dure d'`expo-sharing` (une seule URL par appel, confirmé via son `.d.ts`) ; corrigé en ajoutant `react-native-share` (**nouvelle dépendance native**, `Share.open({ urls })`), nouveau `downloadAndShareMultiple` dans `lib/mediaShare.ts`, branché sur les 3 flux de sélection multiple (le partage d'un seul média garde `downloadAndShare`) ; (b) `Mon Compte → Mes Nouvelles` n'affichait aucune vignette photo contrairement à `Mon Compte → Soutien` : corrigé en ajoutant le même helper `newsPhotoUrl` + rendu de vignette (`app/(visitor)/account.tsx`).
- PR #246 : même capacité de sélection multiple + partage groupé portée directement dans `MesSouvenirs.tsx` (jusque-là seulement présente dans Nouvelles/Soutien/Souvenirs) — sélection *scopée à une section à la fois* (`selectSection: "published" | "downloaded"`) puisque les deux listes ont des espaces d'identifiants indépendants. Changement JS pur, aucune nouvelle dépendance native, livré par OTA sans rebuild.

**Testé et confirmé par l'utilisateur ce cycle :** les six PR mergées et confirmées explicitement. La PR #245 (ajout de `react-native-share`, dépendance **native**) a nécessité un vrai rebuild EAS — accompagnement complet de l'utilisateur en direct : `eas build --platform android --profile preview` (premier essai échoué car lancé depuis `C:\Windows\System32` au lieu du répertoire du projet), build confirmé réussi via `eas build:list`, puis deux crashes au lancement (`TurboModuleRegistry... 'RNShare' could not be found`, puis écran blanc) — cause racine : **l'OTA automatique publie le JS sur le channel `preview` à chaque merge, y compris quand une PR ajoute un module natif qui, lui, n'est pas dans le binaire déjà installé sur le téléphone** ; l'utilisateur voit "j'ai reçu la mise à jour" et croit tester le nouveau code alors que seule la moitié JS est arrivée. Résolu par désinstallation complète de l'app puis réinstallation propre de l'APK (un téléchargement de ~180 Mo en 4G avait probablement aussi été corrompu en cours de route). **Leçon retenue pour toute future dépendance native** : signaler explicitement que l'OTA seule ne suffit pas, qu'un rebuild + réinstallation complète (désinstaller avant de réinstaller, de préférence en Wi-Fi) est nécessaire.

**Connu, non bloquant :**
- Branche distante `feat/entraide-creer-checklist` (PR #140, déjà mergée) porte toujours un commit orphelin (`e9472c9`) poussé par erreur après le merge — nettoyage (reset + force-push) proposé à l'utilisateur, jamais confirmé, sans conséquence fonctionnelle.
- **Gap de documentation historique** : ce handoff n'avait pas été régénéré entre le 2026-08-13 (PR #152) et le 2026-08-17 (PR #225) — les PR #153 à #224 ne sont pas détaillées individuellement ici, seulement résumées au paragraphe "Livré" et dans "Historique cumulé" ci-dessous ; voir `git log --merges` pour le détail précis. Ce gap est refermé pour tout ce qui suit : `Documentation/Documentation Fonctionnalités.docx` est désormais tenue à jour jusqu'à la PR #246 (V1.5, 18/08/2026), comme ce handoff.
- Migration `20260728_intervenant_checklist_templates.sql` : confirmée exécutée en prod de fait — la fonctionnalité "Mes Modèles" (PR #225-227) repose dessus et est active.

**En cours / pas commencé (reporté, à reprendre sur demande) :**
- `fix/souvenirs-rls-and-news-delete` : branche stale, 2 migrations RLS jamais exécutées en prod (`AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Isolation Supabase (séparer l'instance prod partagée avec le site web historique) : plan complet dans `ISOLATION_SUPABASE.md`.
- `docs/spec-web-upgrade` : en attente d'une décision utilisateur.
- Taille de l'app (~180 Mo, désormais alourdie par `react-native-share`) et audit code mort : volontairement reporté (voir `AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Points 13-14 (EAS Build APK signé, fiche Play Store) : pas commencés.
- Site marketing `avectoi-site` (dépôt séparé, poussé sur `main`) : toujours pas déployé (Infomaniak, bloqué sans les accès de l'utilisateur). Décommissionnement du site Vercel historique dépend de ce déploiement.
- Branche distante `feat/entraide-creer-checklist` : commit orphelin à nettoyer, voir "Connu, non bloquant" ci-dessus.
- `PRD_ClaudeCode_Site_avectoi_care_v3.md` et `AUDIT_RLS_TAILLE_CODE_MORT.md` : fichiers non suivis présents dans l'arbre de travail (à committer ou nettoyer selon leur statut, non touchés par ce cycle).

## Historique cumulé
- Jusqu'au 2026-07-22 : lots 1-10 (fonctionnalités de base) + refonte intervenant Phase A/B, RGPD 90j + purge auto + sauvegarde schéma auto, checklists perso réutilisables, rattachement multi-espaces intervenant, EAS Update automatisé — PR #7 à #102.
- 2026-07-22 → 08-01 : resynchronisation PRD (#103), snapshot schéma (#104), liens d'invitation → `app.avectoi.care` (#105), verrou Premium "Planning des intervenants" (#106), fix datepicker onboarding + navigation Réglages (#107), fix décalage de date/toggle intervenants/lien email confirmation (#108, remergé sous #111), site marketing `avectoi-site` construit et poussé (pas déployé), règle Git "1 branche = 1 PR" documentée (#109).
- 2026-08-01 → 08-02 : diagnostic + fix définitif du lien Google Maps (#110), Site URL Supabase corrigé, PRD v1.6→v1.7 (#112), rafraîchissement adresse + popup centré + première version du flux in-app changement de nom (#113).
- 2026-08-09 → 08-10 (matin) : Edge Function `notify-name-change` recréée sous le bon slug (#115-117), flux changement de nom confirmé fonctionnel.
- 2026-08-10 (journée) : bug tenace du bouton "Fermer" invisible résolu après 6 tentatives infructueuses — cause : styles partagés `btnPrimary`/`btnPrimaryText` réutilisés hors d'une rangée `sheetBtns` bornée ; correctif = taille fixe + styles 100% inline (PR #118-128).
- 2026-08-10 (soir) → 08-11 (matin) : texte "Partager" personnalisé, refonte UX "Mes Checklists", popups centrés étendus à toute l'app (23 popups), 8 boutons "Fermer"/"Annuler" invisibles corrigés au global — PR #130, #131.
- 2026-08-11 : nouveau flux "Créer une checklist" dans Entraide/Publier/Administratif + renommage "Checklists suggérées" dans Mon Compte (PR #140) ; correctif texte invisible sur 2 boutons du même flux + renommage du bandeau admin Entraide (PR #141).
- 2026-08-11 → 08-12 : cycle non détaillé dans ce handoff (voir "Connu, non bloquant" ci-dessus) — PR #133 à #149 : cadres de réservation et légende du calendrier, refonte du planning des intervenants en vue hebdo, historique des soins planifiés, modération soins/visites, réservation de soins par l'intervenant depuis le calendrier, largeur du popup d'intervention, panneau et bascule de vue du planning intervenant.
- 2026-08-12 → 08-13 : vue Hebdo réservable pour tous les rôles avec marqueurs hospitalisation/sortie, parité complète visiteur/admin (#150) ; alignement visuel des deux switches du calendrier + élargissement de la bande verte "mes visites" (#151) ; panneau "Historique des soins" du rôle intervenant scindé en "Soins planifiés" (toujours visible) / "Historique des soins" (menu dépliable), bascule précise à la minute (#152).
- 2026-08-13 → 08-17 : cycle non détaillé PR-par-PR dans ce handoff (voir "Connu, non bloquant" ci-dessus) — PR #153 à #224 : panneau admin Calendrier + permissions Nouvelles (#192-200), planning intervenants multi-espaces "Mes espaces patients" avec édition croisée (#201-207), stabilisation planning cross-space — couleurs, popups soin, longpress carte, créneaux déjà pris, sécurité nom patient, navigation jour (#208-223), plus plusieurs cycles calendrier/checklist/nuitées intermédiaires (#153-191). `Documentation Fonctionnalités.docx` tenue à jour jusqu'à la PR #223.
- 2026-08-17 (matin) : "Mes Modèles" (checklists cross-space intervenant) passe d'un import direct à un popup de sélection d'items pré-cochés (#225), ajout de la suppression d'une checklist perso entière par appui long (#226), anti-doublons global + complétion incrémentale + grisage des modèles entièrement importés (#227).
- 2026-08-17 (matin) : date du jour figée sur l'onglet Accueil (le curseur revient sur aujourd'hui à chaque retour), panneau "Visites/Soins planifiés" filtré sur la période affichée avec sous-rubrique "Autres..." pour le reste (#230) ; page d'accueil qui s'ouvre par défaut en mode Visite avec "Afficher mes créneaux" désactivé, y compris pour l'intervenant (#231).
- 2026-08-17 : RGPD passe de 90j à 60j de rétention par défaut, prolongation gratuite ramenée de 90j à 30j, nouveau popup in-app systématique en fenêtre J-7 (en plus de l'email, suite à un incident de purge silencieuse sans alerte) (#232) ; gate du bouton "Prolonger" à J-7 exactement, calendrier admin filtrable par intervenant, autofocus connexion (#233) ; popup RGPD harmonisé (`ConfirmModal`), photos/pictogrammes ajoutés aux fiches et soins intervenants, droits d'édition admin restreints aux soins qu'il a lui-même créés (`pin === "ADMIN"`) (#234).
- 2026-08-17 : Fiche intervenant express côté admin — création à la volée d'un intervenant sans compte (prénom/nom/métier/soins/email, `pin` désormais nullable) depuis "Ajouter une intervention", puis envoi d'une confirmation de créneau (SMS/email, nouvelle Edge Function `notify-intervention-confirmation`) (#235).
- 2026-08-17 : Calendrier des visites (visiteur) refondu façon Planning intervenant — liseré de couleur par visiteur, légende filtrable, fond Orange/Rouge, popup "Modifier ce créneau" (#236) ; 5 correctifs post-test : faille de sécurité (Modifier/Y Aller accessible sans vérification de propriété, y compris sur les réservations de l'admin), créneau passé réservable via une valeur pré-sélectionnée non revalidée, fond pastel + texte contrasté, point vert "Dispo" réinstallé, bascule Visites/Soins retirée du propre profil du visiteur (#237).
- 2026-08-17 : blocage du tap sur un créneau déjà passé + affichage des accompagnants sur la ligne de réservation principale (#238) ; onglet "Accueil" ajouté en tête de barre, onglet "Souvenirs" retiré (page conservée via Compte et fiche patient), vue "Médias" en grille dans Nouvelles avec lightbox dédiée, bouton "Ajouter au mur de souvenirs" supprimé (#239) ; correctifs post-test : crash de la vue Médias, liseré de couleur invisible sur fond identique, sous-headers allégés (#240).
- 2026-08-17 → 08-18 : "Mes souvenirs" — page personnelle publiées/téléchargées (#241), sélection multiple dans Nouvelles/Soutien (#242), correctif de fond (téléchargements non tracés) + faille de sécurité PIN (#243), migration idempotente (#244), partage groupé multi-médias via `react-native-share` + vignette Mes Nouvelles (#245), sélection multiple dans Mes souvenirs (#246) — détaillé ci-dessus.

## 1. Objectif de la session
Suite directe du dernier volet de la demande initiale à 5 points (post-PR #240) : permettre aux médias qu'on publie ET qu'on télécharge d'alimenter une page personnelle "Mes souvenirs" dans Mon Compte. Six itérations, chacune déclenchée par un retour de test réel de l'utilisateur sur son téléphone : page personnelle (#241) → sélection multiple dans Nouvelles/Soutien (#242) → bug de tracking + faille de sécurité PIN (#243) → migration cassée (#244) → partage groupé multi-fichiers + vignette Mes Nouvelles, avec le premier vrai rebuild EAS natif du cycle (#245) → sélection multiple dans Mes souvenirs elle-même (#246).

État "done" : atteint — les 6 PR sont mergées, chacune confirmée explicitement par l'utilisateur avant la suivante. Dernière confirmation : "app désinstallé puis réinstallé... et l'app s'ouvre" (rebuild EAS de la PR #245), puis "mergé / ok" pour la PR #246.

## 2. État actuel
Le travail de ce cycle touche principalement `components/MesSouvenirs.tsx`, `lib/mediaShare.ts` (nouveau helper partagé) et les 3 grilles Médias existantes (`NewsFeed.tsx`, `Soutien.tsx`, `SouvenirsGallery.tsx`) :
- `lib/mediaShare.ts` centralise `downloadAndShare` (un seul fichier, `expo-sharing`), `downloadAndShareMultiple` (plusieurs fichiers en un seul envoi, `react-native-share`), `isShareAvailable`, `logSavedMedia`.
- `components/MesSouvenirs.tsx` : deux accordéons "Photos publiées"/"Photos téléchargées", chacun avec sa propre sélection multiple scopée (`selectSection`), barre de sélection (compteur, "Tout sélect.", "↗️ Partager", "Annuler"), badge de coche sur les vignettes sélectionnées.
- Table `saved_media` (migration idempotente `20260818_saved_media_identity_fix.sql`) : trace qui a téléchargé/partagé la photo de qui, matché par prénom/nom (pas seulement PIN, pour couvrir les visiteurs sans PIN encore choisi).
- `lib/visitorSession.ts` : `sessionPinMatches` exige désormais PIN + prénom/nom (faille de collision PIN corrigée, répercutée dans `NewsFeed.tsx`, `Entraide.tsx`, `BookingFlow.tsx`).
- `app/(visitor)/account.tsx` : vignette photo ajoutée à la section "Mes Nouvelles", même pattern que "Soutien".

**Dernière action avant ce handoff :** régénération de ce handoff + mise à jour de `Documentation/Documentation Fonctionnalités.docx` (V1.4 → V1.5), demandées par l'utilisateur juste après confirmation du merge de la PR #246. Le docx couvrait #228-246 (jamais synchronisé depuis #227) : sections Navigation (barre d'onglets Accueil/Souvenirs), Accueil→Calendrier (date du jour, panneau filtré, mode par défaut), Nouvelles (vue Médias), Souvenirs (accès depuis Compte), Compte admin/visiteur (bouton Mes souvenirs), Règles/Histo (RGPD 60j/30j/J-7), Planning des intervenants (fiche express), Calendrier visiteur (rendu façon Planning, sécurité), Gestion admin (calendrier filtrable par intervenant) ont toutes été mises à jour, avec relecture de vérification après sauvegarde.

## 3. Fichiers concernés
- `lib/mediaShare.ts` → nouveau helper partagé, étendu en #245 (`downloadAndShareMultiple`).
- `components/MesSouvenirs.tsx` → page personnelle (#241), sélection multiple (#246).
- `components/NewsFeed.tsx`, `components/Soutien.tsx`, `components/SouvenirsGallery.tsx` → sélection multiple (#242), branchement sur le partage groupé (#245).
- `lib/visitorSession.ts` → faille PIN corrigée (#243), répercutée dans `Entraide.tsx`, `BookingFlow.tsx`.
- `supabase/migrations/20260817_saved_media_personnel.sql`, `20260818_saved_media_identity_fix.sql` → table `saved_media` + correctif identité + idempotence (#241, #243, #244).
- `app/(visitor)/account.tsx` → vignette "Mes Nouvelles" (#245).
- `package.json`/`package-lock.json` → ajout `react-native-share` (#245, dépendance native).
- `Handoff/handoff.md` → ce fichier.
- `Documentation/Documentation Fonctionnalités.docx` → mis à jour dans la foulée de ce handoff (V1.4 → V1.5), couvre désormais #228-246.

## 4. Ce qui a échoué
Rien côté code applicatif au sens strict, mais deux détours opérationnels notables : (1) la PR #243 a été mergée avant qu'un commit correctif ne soit poussé, laissant ce commit orphelin sur la branche fermée — l'utilisateur a rencontré une erreur SQL en exécutant la migration, nécessitant une PR #244 dédiée pour re-livrer le correctif proprement ; (2) le premier rebuild EAS natif du projet (PR #245) a nécessité 3 allers-retours avec l'utilisateur (mauvais répertoire de lancement, confusion OTA vs Build, app corrompue après un téléchargement lent) avant d'aboutir — aucun bug de code, mais un vrai coût de temps/support qui a débouché sur la leçon "OTA vs Build" désormais documentée en mémoire pour toute future dépendance native.

## 5. Prochaine étape
Aucune action immédiate requise côté code — les 6 PR de ce cycle sont mergées et confirmées par l'utilisateur, et ce handoff + le docx sont à jour jusqu'à la PR #246. Reste à committer les deux documents sur une branche `docs/` dédiée et ouvrir la PR correspondante.

Points en suspens à reproposer sur demande : nettoyage de la branche distante `feat/entraide-creer-checklist` (commit orphelin `e9472c9`), reconstitution PR-par-PR du cycle #153-#224 si ce niveau de détail devient utile, `PRD_ClaudeCode_Site_avectoi_care_v3.md`/`AUDIT_RLS_TAILLE_CODE_MORT.md`/`eslint.config.js` non suivis à committer ou nettoyer.

Items reportés (inchangés depuis les cycles précédents, ordre suggéré, sur demande uniquement) : rebase de `fix/souvenirs-rls-and-news-delete`, décision `docs/spec-web-upgrade`, déploiement `avectoi-site` sur Infomaniak (bloqué sans les accès de l'utilisateur), isolation Supabase, taille de l'app.
