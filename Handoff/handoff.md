# Handoff — AvecToi
_Généré le : 2026-08-27 (cycle #303-305)_

## État global du projet

**Stack :** React Native + Expo SDK 51+, Expo Router, Supabase (BDD/Auth/Storage/Realtime/Edge Functions), EAS Build + EAS Update (channels development/preview/production, `expo-updates`, publication OTA automatique sur push `main` via `.github/workflows/eas-update-preview.yml`). expo-notifications, expo-calendar, expo-image-picker, expo-sharing (partage simple) + react-native-share (partage groupé multi-fichiers, dépendance native ajoutée #245). Resend (transactionnel) branché sur les Edge Functions. Stripe côté web uniquement. Supabase CLI bloqué sur cette machine (App Control Policy Windows) : déploiement des migrations/Edge Functions exclusivement via le Dashboard Supabase, jamais auto-appliqué par Claude.

**⚠️ Règle Git STRICTE (CLAUDE.md) :** chaque modification, même un correctif ponctuel, part sur une branche neuve (depuis `origin/main` à jour) et se termine par sa propre PR poussée sur GitHub — jamais de nouveau commit ajouté à une branche dont la PR est déjà mergée. **Autorisation permanente : commit + push + PR dès qu'une tâche de code est terminée et vérifiée, sans attendre que l'utilisateur le redemande.** **L'utilisateur merge lui-même, puis teste** (le merge déclenche la publication OTA — sauf si la modif ajoute une nouvelle dépendance native, auquel cas un `eas build` + réinstallation complète de l'APK est nécessaire). Toujours vérifier l'état réel d'une PR via `gh pr view <n> --json state,url` plutôt que de supposer — ce repo merge vite, souvent en un seul message de l'utilisateur.

**⚠️ Convention UI (CLAUDE.md, section "Conventions UI", ajoutée #268) :** toute nouvelle confirmation doit utiliser le composant thémé `ConfirmModal`, jamais `Alert.alert` (natif, hors charte) ; tout nouveau bouton doit reprendre les couleurs/emoji/style déjà en usage à l'écran ; toute notification éphémère doit réutiliser le pattern `showToast` existant. Vérifier ce point avant toute PR ajoutant un popup ou un bouton, pour éviter une PR d'harmonisation de rattrapage.

**Repo GitHub :** `https://github.com/EI-HCS-Consulting/AvecToi`, branche `main` protégée par ruleset (PR obligatoire).

**PR ouverte, en attente de test/merge :** aucune. PR #305 est la dernière mergée en date.

**Livré (V1 + V2 archivé) :** tout le socle (points 1-10 CLAUDE.md), RGPD 60j par défaut + prolongation gratuite 30j + gate J-7, purge auto + sauvegarde schéma hebdo, onboarding séquencé, cap freemium (8 visites), Paramètres 4 sections + historique, emails Resend, Chronologie, EAS Update automatisé. **Répartition d'un besoin de relais entre plusieurs preneurs** *(nouveau ce cycle, #305)* : sous-périodes distinctes réclamables par personne (table `task_relais_coverage`), désinscription individuelle par sous-période, choix de la période en deux popups centrés « Du »/« Au » restreints à la fenêtre demandée par l'admin, bloc « 🤝 Mes engagements de relais » dans Mon Compte. **Popup relais enrichi + « 🔔 Mes alertes »** *(nouveau ce cycle, #303, #305)* : bouton « 🗓️ Je regarde mon planning » (ferme sans décider, l'alerte revient à la prochaine connexion), liste des personnes sollicitées visible dans le popup, historique de « 🔔 Mes alertes » filtré aux seules entrées jamais vues (`reservation_change_history.seen`). **Publication d'une checklist suggérée avec choix de destination** *(#297)* : Mur d'Entraide seul, « Mes Checklists » seul, ou les deux à la fois avec synchronisation automatique (statut « Fait », identité de qui s'en occupe) ; l'auteur d'un besoin (visiteur inclus) peut désormais le supprimer lui-même, avec suppression en cascade proposée du reste de la checklist suggérée si d'autres items restent publiés et ouverts *(#296, #297)*. **Import privé (Mon Compte) indépendant du Mur** *(#295)* : l'anti-doublon public ne s'applique plus qu'en activant « Publier aussi sur le Mur d'Entraide ». **Traçabilité "Pas cette fois"** sur un besoin de relais ponctuel, visible admin uniquement *(#295)*. **Calendrier principal (Accueil)** : composant unique `HomeCalendarScreen` partagé admin/visiteur *(#292)* — Mensuel/Hebdo, "⚡ Prochaine disponibilité", grisage pré-admission, marqueurs admission/sortie, bouton « 📅 Créneaux » dans Planning du jour, email de confirmation par accompagnant, synchronisation Mensuel→Hebdo sur le jour sélectionné *(#295)*. **Sélection du jour inversée** *(#293)* : l'onglet bas « Accueil » conserve désormais la sélection entre deux visites, seul le bandeau « 📅 Calendrier » réinitialise sur aujourd'hui. **Rôle Intervenant retiré de V1** *(#291-292)* : masqué derrière `INTERVENANT_ROLE_ENABLED=false` (pas supprimé — code archivé dans `Développement V2/` pour réintégration future), plus aucune trace admin (Paramètres → Règles/Histo, Chronologie) ni visiteur. **Panneau admin Calendrier + permissions Nouvelles**, **Fiche intervenant express**, **Calendrier des visites (visiteur)**, onglets réorganisés (Accueil en tête, Médias en grille), **"Mes souvenirs"** personnelles, **Besoin de relais ponctuel** (réservé admin), **Liste de courses** affinée (cochage attribué/verrouillé), **Réservations récurrentes**, **Lien visiteur-patient**, **Popup jour d'hospitalisation dédié**. Détail complet des cycles antérieurs : voir Historique cumulé ci-dessous.

**Connu, non bloquant :**
- Branche distante `feat/entraide-creer-checklist` (PR #140, déjà mergée) porte toujours un commit orphelin (`e9472c9`) — nettoyage jamais confirmé par l'utilisateur, sans conséquence fonctionnelle.
- **Trois implémentations distinctes du "désengagement/désinscription"** coexistent volontairement (convention du repo : `account.tsx`/`Entraide.tsx` dupliquent leurs flux plutôt que de les partager) : `performUnclaim` (Entraide.tsx, avec nettoyage de la photo de claim, utilisé par Transport + le bouton du formulaire d'édition), et `lib/taskDisengage.ts` (les deux `account.tsx`, sans nettoyage de photo). À garder en tête si une future demande vise à tout unifier.
- Migrations manuelles en attente de confirmation d'exécution : `20260819_shopping_list_items.sql`, `20260820_tasks_relais_ponctuel.sql`, `20260821_shopping_list_items_bought_by.sql` (confirmées appliquées) — voir Historique cumulé. **⚠️ `supabase/migrations/20260821_visitor_profiles_relation.sql` (introduite par #278) : statut d'application non confirmé par l'utilisateur.** #279 ajoute un repli client qui masque le symptôme (select `visitor_profiles` incluant `relation` échouant entièrement si la colonne n'existe pas encore côté base — comportement PostgREST), mais la migration doit être rejouée manuellement dans le dashboard Supabase pour que le lien visiteur-patient soit réellement stocké/lu — le repli n'est qu'un filet de sécurité, pas un correctif. `20260827_reservation_change_history_seen.sql` (#305) : **confirmée appliquée par l'utilisateur** le 27/08/2026. `20260827_task_relais_coverage.sql` (#305, introduite plus tôt dans le même cycle pour la table de couverture des sous-périodes) : statut d'application non reconfirmé explicitement dans ce message-ci — à revérifier si un test de la répartition multi-preneurs échoue en base.
- **Documentation à jour** : `Documentation/Documentation Fonctionnalités.docx` synchronisée jusqu'à la PR #305 (V1.14, 27/08/2026) — §5.9 Entraide (admin), ligne "Besoin de relais ponctuel" complétée (bouton "Je regarde mon planning", liste des sollicités, répartition multi-preneurs, popups "Du"/"Au" bornés, message de remerciement dédié) ; §5.11 Compte admin et §6.4 Compte visiteur : deux nouvelles lignes "🔔 Mes alertes" et "🤝 Mes engagements de relais" ; en-tête métadonnées mis à jour ; `PRD_AvecToi_v1_4.md` passé en v1.13 avec un changelog dédié, sections §2, §3.8 mises à jour, et le schéma DB complété (`task_relais_coverage`, colonne `reservation_change_history.seen`).

**En cours / pas commencé (reporté, à reprendre sur demande) :**
- `fix/souvenirs-rls-and-news-delete` : branche stale, 2 migrations RLS jamais exécutées en prod (`AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Isolation Supabase (séparer l'instance prod partagée avec le site web historique) : plan complet dans `ISOLATION_SUPABASE.md`.
- `docs/spec-web-upgrade` : en attente d'une décision utilisateur.
- Taille de l'app (~180 Mo) et audit code mort : volontairement reporté (voir `AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Points 13-14 (EAS Build APK signé, fiche Play Store) : pas commencés.
- Site marketing `avectoi-site` (dépôt séparé) : toujours pas déployé (Infomaniak, bloqué sans les accès de l'utilisateur).
- Branche distante `feat/entraide-creer-checklist` : commit orphelin à nettoyer, voir "Connu, non bloquant" ci-dessus.
- `PRD_ClaudeCode_Site_avectoi_care_v3.md` et `AUDIT_RLS_TAILLE_CODE_MORT.md` : fichiers non suivis présents dans l'arbre de travail, non touchés par ce cycle.

## Historique cumulé
- Jusqu'au 2026-07-22 : lots 1-10 (fonctionnalités de base) + refonte intervenant Phase A/B, RGPD 90j + purge auto + sauvegarde schéma auto, checklists perso réutilisables, rattachement multi-espaces intervenant, EAS Update automatisé — PR #7 à #102.
- 2026-07-22 → 08-01 : resynchronisation PRD (#103), snapshot schéma (#104), liens d'invitation → `app.avectoi.care` (#105), verrou Premium "Planning des intervenants" (#106), fix datepicker onboarding + navigation Réglages (#107), fix décalage de date/toggle intervenants/lien email confirmation (#108, remergé sous #111), site marketing `avectoi-site` construit et poussé (pas déployé), règle Git "1 branche = 1 PR" documentée (#109).
- 2026-08-01 → 08-02 : diagnostic + fix définitif du lien Google Maps (#110), Site URL Supabase corrigé, PRD v1.6→v1.7 (#112), rafraîchissement adresse + popup centré + première version du flux in-app changement de nom (#113).
- 2026-08-09 → 08-10 (matin) : Edge Function `notify-name-change` recréée sous le bon slug (#115-117), flux changement de nom confirmé fonctionnel.
- 2026-08-10 (journée) : bug tenace du bouton "Fermer" invisible résolu après 6 tentatives infructueuses — cause : styles partagés `btnPrimary`/`btnPrimaryText` réutilisés hors d'une rangée bornée ; correctif = taille fixe + styles 100% inline (PR #118-128).
- 2026-08-10 (soir) → 08-11 (matin) : texte "Partager" personnalisé, refonte UX "Mes Checklists", popups centrés étendus à toute l'app (23 popups), 8 boutons "Fermer"/"Annuler" invisibles corrigés au global — PR #130, #131.
- 2026-08-11 : nouveau flux "Créer une checklist" dans Entraide/Publier/Administratif + renommage "Checklists suggérées" dans Mon Compte (PR #140) ; correctif texte invisible sur 2 boutons du même flux + renommage du bandeau admin Entraide (PR #141).
- 2026-08-11 → 08-12 : cycle non détaillé PR-par-PR — PR #133 à #149 : cadres de réservation et légende du calendrier, refonte du planning des intervenants en vue hebdo, historique des soins planifiés, modération soins/visites, réservation de soins par l'intervenant depuis le calendrier, largeur du popup d'intervention, panneau et bascule de vue du planning intervenant.
- 2026-08-12 → 08-13 : vue Hebdo réservable pour tous les rôles avec marqueurs hospitalisation/sortie, parité complète visiteur/admin (#150) ; alignement visuel des switches du calendrier + bande verte "mes visites" (#151) ; panneau "Historique des soins" intervenant scindé en "Soins planifiés"/"Historique des soins" (#152).
- 2026-08-13 → 08-17 : cycle non détaillé PR-par-PR — PR #153 à #224 : panneau admin Calendrier + permissions Nouvelles (#192-200), planning intervenants multi-espaces "Mes espaces patients" avec édition croisée (#201-207), stabilisation planning cross-space — couleurs, popups soin, longpress carte, créneaux déjà pris, sécurité nom patient, navigation jour (#208-223), plus plusieurs cycles calendrier/checklist/nuitées intermédiaires (#153-191).
- 2026-08-17 (matin) : "Mes Modèles" intervenant passe d'un import direct à un popup de sélection (#225), suppression d'une checklist perso entière par appui long (#226), anti-doublons global + complétion incrémentale + grisage des modèles entièrement importés (#227).
- 2026-08-17 : date du jour figée sur l'onglet Accueil, panneau "Visites/Soins planifiés" filtré sur la période affichée (#230) ; page d'accueil par défaut en mode Visite (#231).
- 2026-08-17 : RGPD 90j → 60j de rétention par défaut, prolongation ramenée à 30j, popup in-app systématique J-7 (#232) ; gate "Prolonger" à J-7 exactement, calendrier admin filtrable par intervenant (#233) ; popup RGPD harmonisé (`ConfirmModal`), photos/pictogrammes intervenants, droits d'édition admin restreints à ses propres soins (#234).
- 2026-08-17 : Fiche intervenant express côté admin — création sans compte (`pin` nullable), confirmation de créneau par SMS/email (#235).
- 2026-08-17 : Calendrier des visites (visiteur) refondu façon Planning intervenant (#236) ; 5 correctifs post-test dont une faille de sécurité (Modifier/Y Aller sans vérification de propriété) (#237).
- 2026-08-17 : blocage du tap sur créneau passé + accompagnants sur la ligne principale (#238) ; onglet "Accueil" en tête de barre, "Souvenirs" retiré de la barre, vue "Médias" en grille (#239) ; correctifs post-test Médias (#240).
- 2026-08-17 → 08-18 : "Mes souvenirs" — page personnelle publiées/téléchargées (#241), sélection multiple Nouvelles/Soutien (#242), correctif tracking + faille PIN (#243), migration idempotente (#244), partage groupé multi-médias `react-native-share` + vignette Mes Nouvelles — premier vrai rebuild EAS natif du projet (#245), sélection multiple dans Mes souvenirs (#246). Doc PR #247 : handoff + docx (V1.4→V1.5) synchronisés jusqu'à #246, refermant un gap de 18 PR (#228-246).
- 2026-08-18 : bibliothèque de checklists passée de 3 à 11 modèles, orientés proche aidant, données fournies par l'utilisateur (#248) ; échéance de lot + urgence/précision par item, toggle privé/public par défaut privé, pièces à réunir cochables, correctif de type `checklist_context` (#249).
- 2026-08-18 : refonte en assistant séquentiel per-item (précision → échéance → urgence → photo), `repit_aidant`/`conge_proche_aidant` passés `personalOnly`, correctif de doublons sur besoins supprimés en douceur, migration de la contrainte CHECK jamais élargie depuis #248 (#250).
- 2026-08-18 : pièces à réunir nichées sous leur item parent, liens officiels persistants après import + 12/15 liens corrigés, anti-doublon croisant aussi le privé, premiers modèles de courriers administratifs (congé proche aidant → employeur) (#251). Doc PR #252 : handoff partiel (#248-251).
- 2026-08-18 → 08-19 : cycle #253-259 non détaillé PR-par-PR — correctif RLS, décalage d'onglet, alignement droit des courriers, édition/suppression dans "Mes documents".
- 2026-08-19 : "Mes documents" — "Retélécharger" devient "👁️ Aperçu" avec rendu WYSIWYG, boutons Modifier/Télécharger (#261) ; suppression du bouton "Envoyer" (doublon fonctionnel de "Télécharger"), "Modifier" harmonisé en bleu plein, 7 nouveaux modèles de courriers (autorisation de soins enfant, attestation d'autorité parentale — cadrée comme déclaration sur l'honneur —, courrier école/crèche, déclaration mutuelle/CPAM, procuration bancaire, absence hospitalisation d'un proche, déclaration de sinistre) (#262).
- 2026-08-19 : pré-remplissage automatique des courriers à partir des données déjà connues de l'app (nom, adresse, hôpital, date de naissance patient), correctif d'un titre de checklist à 2 lignes qui poussait le chevron hors écran (#263).
- 2026-08-19 : nouvelle fonctionnalité "liste de courses" — catégorie Courses devient une liste d'articles cochables en temps réel (nouvelle table `shopping_list_items`, popup `ShoppingListModal`), surfacée aussi dans "Mes documents" (préfixe 🛒) (#264) ; bloc de saisie encadré comme le formulaire Transport, auto-complétion du besoin quand le dernier article est coché (#265).
- 2026-08-19 : désengagement généralisé à toutes les catégories via `performUnclaim` réutilisé (bouton dans "Modifier le besoin" + appui long sur carte "Fait"), auto-réouverture symétrique d'une liste de courses en cas de décochage (#266) ; harmonisation du wording ("Me désengager"), nouveau helper partagé `lib/taskDisengage.ts`, section "besoins pris en charge" ajoutée à Mon Compte admin (#267).
- 2026-08-19 : les trois confirmations de désengagement passent d'`Alert.alert` à `ConfirmModal`, nouvelle section "Conventions UI" dans `CLAUDE.md` (#268).
- 2026-08-19 → 08-20 : nouvelle fonctionnalité "besoin de relais ponctuel" — catégorie technique dédiée publiée uniquement depuis Mon Compte, ciblage d'audience, alerte à la connexion via `RelaisAlertModal` (#270) ; correctifs post-test (boutons centrés, onglet SOS Relais masqué si aucun besoin visible, miroir `personal_checklist_items` désynchronisé) (#271) ; liste de courses affinée en 4 étapes — cochage attribué + verrouillé au preneur en charge (#272), affichage multi-contributeurs cumulé (#273), suffixe "… partiellement" si liste incomplète (#274), verrouillage par article + complétion automatique des articles restants à la prise en charge tardive (#275). Doc PR #276 : handoff + docx (V1.9→V1.10) + PRD (v1.8→v1.9) synchronisés jusqu'à #275.
- 2026-08-20 : retrait du cochage auto des courses au claim introduit par erreur en #275, ajout de la liste des visiteurs (`VisitorsList`/`VisitorsListModal`), bouton "Intervenants"→"Visiteurs" côté visiteur, bloc "Besoin de relais" retiré du compte visiteur (réservé admin), boutons "Fiche patient"/"Visiteurs" ajoutés au compte admin (#277) ; lien visiteur-patient — colonne `visitor_profiles.relation`, catalogue `lib/relations.ts`, champ dans Mes informations, affiché sur la liste des visiteurs et leur fiche avec la phrase totem (#278) ; correctif régression PostgREST (repli sans la colonne `relation` tant que la migration n'est pas rejouée), ajout Belle-fille/Beau-fils, remplacement des chips par un `RelationPickerModal` (#279).
- 2026-08-20 : liens Beau-père/Belle-mère + réordonnancement du catalogue, Planning du jour cliquable si vide, bouton "Ajouter une Visite" depuis le popup de visite, réservation directe par tap sur un créneau libre d'un autre visiteur (#280) ; places restantes affichées, ajout Grand-père/Grand-mère, correctif traits de bord visiteur en vue Hebdo (#281).
- 2026-08-20 : réservations récurrentes — récupération d'une fonctionnalité déjà validée (#283) mais jamais réellement mergée sur `main` (branche stackée sur la mauvaise base) (#285) ; extension à l'admin, popup jour d'hospitalisation dédié (picto 🏥, titre "Hospitalisation de [prénom]"), picto anniversaire 🎂→🎉, autofocus champ Prénom à la connexion (#287) ; correctif appui long cassé sur le jour d'hospitalisation, mention "Jour d'hospitalisation" dans Planning du jour, centrage bouton récurrentes, agrandissement calendrier de plage, mémoïsation `SpaceContext`/`VisitorContext` (perf) (#288).
- 2026-08-20 → 08-21 : correctif racine du popup jour d'hospitalisation (déclenchement systématique, plus par coïncidence), correctif centrage bouton "Créer mes réservations" (`includeFontPadding`), index mémoïsé par date pour les grilles Mensuelles, synchronisation Mensuel→Hebdo sur le jour sélectionné, conservation de la sélection au retour via le bandeau "Calendrier" (#289). Doc PR #290 : handoff + docx (V1.10→V1.11) + PRD (v1.9→v1.10) synchronisés jusqu'à #289.
- 2026-08-21 : rôle Intervenant masqué en V1 derrière `INTERVENANT_ROLE_ENABLED=false` (pas supprimé, code archivé dans `Développement V2/` avec instructions de réintégration) (#291) ; masquage des dernières traces admin (bloc "Intervenants"/"Soins planifiés" en Paramètres → Règles/Histo, soins retirés de la Chronologie) et calendrier Accueil unifié admin/visiteur (`HomeCalendarScreen` commun, bouton "📅 Créneaux" dans Planning du jour, email de confirmation par accompagnant) (#292) ; sémantique de sélection du calendrier inversée — l'onglet "Accueil" conserve désormais la sélection entre deux visites, seul le bandeau "Calendrier" réinitialise sur aujourd'hui — et masquage des réservations "soins" résiduelles dans l'onglet Créneaux (#293). Doc PR #294 : handoff + docx (V1.11→V1.12) + PRD (v1.10→v1.11) synchronisés jusqu'à #293.
- 2026-08-21 : synchronisation Mensuel→Hebdo sur le jour sélectionné après correctif d'un bug de closure, checklists privées (Mon Compte) rendues indépendantes de l'anti-doublon public du Mur d'Entraide (l'anti-doublon ne s'applique plus qu'en activant "Publier aussi sur le Mur d'Entraide"), traçabilité admin des réponses "Pas cette fois" sur un besoin de relais ponctuel (#295) ; rétablissement de la suppression en cascade du reste d'une checklist suggérée publiée quand d'autres items restent publiés et ouverts, en suppression individuelle et en masse, Mon Compte et Entraide (#296) ; choix de la destination de publication d'une checklist suggérée (Mur seul / Mes Checklists seul / les deux, copies liées et synchronisées), suppression d'un besoin par son propre auteur (pas seulement l'admin), première tentative de correctif du bouton "Retour" du wizard d'import (#297) ; correctifs post-test de #297 — synchronisation du statut "Fait"/prise en charge entre les deux copies liées Mur/Mes Checklists, popup de suppression liée enchaîné à celui de la checklist, icône de suppression épurée, "Retour" toujours cassé dans certains cas (#298) ; cause racine définitive du bug "Retour" — deux `<Modal>` React Native restaient simultanément `visible=true` (incompatible Android), fusion des 3 Modals du flux Entraide en un seul avec bascule de contenu interne, popups de suppression enchaînés plutôt que superposés, 4 irritants mineurs corrigés (#299) ; même bug "Retour" corrigé dans le flux d'import indépendant de Mon Compte (`MyChecklist.tsx`, qui a son propre picker/wizard distinct d'`Entraide.tsx`) (#300). Doc PR #301 : handoff + docx (V1.12→V1.13) + PRD (v1.11→v1.12) synchronisés jusqu'à #300.
- 2026-08-27 : chantier SOS relais en 3 volets planifié (popup planning + répartition de période + co-administration, cette dernière reportée) — Feature 1 : bouton "🗓️ Je regarde mon planning" dans `RelaisAlertModal` (ferme sans décider, l'alerte revient à la prochaine connexion), nouveau `lib/relaisAlerts.ts` centralisant la logique déjà dupliquée, nouveau bouton "🔔 Mes alertes" (Mon Compte admin et visiteur) regroupant RGPD + besoins de relais ouverts + historique de recasage, liste des personnes sollicitées visible dans le détail du popup (#303). Feature 2 : répartition d'un besoin de relais entre plusieurs preneurs sur des sous-périodes distinctes (nouvelle table `task_relais_coverage`, fusion d'intervalles réelle dans `lib/relaisCoverage.ts`), désinscription individuelle par sous-période, choix "🙋 Je m'en charge (ce qu'il reste)" ou "📅 Choisir une période" (#304, mergée dans une branche intermédiaire puis reproposée vers `main` avec le reste du cycle sous #305). Retours utilisateur après premier test device : texte du popup de remerciement spécifique à la catégorie relais (au lieu du texte générique invitant à cliquer sur "Fait"), nouveau bloc "🤝 Mes engagements de relais" dans Mon Compte, "🔔 Mes alertes" filtré aux entrées d'historique jamais vues (nouvelle colonne `reservation_change_history.seen`), choix de la période scindé en deux popups centrés successifs "Du"/"Au" où seuls les jours de la période demandée par l'admin sont sélectionnables et surlignés en orange, avec rappel persistant de cette période à chaque étape (#305). Doc PR à venir : handoff + docx (V1.13→V1.14) + PRD (v1.12→v1.13) synchronisés jusqu'à #305.

## 1. Objectif de la session
Suite directe du dernier handoff (#301, qui s'arrêtait à #300) : chantier SOS relais en 3 volets, planifié en Plan Mode avec l'utilisateur (`C:\Users\ReMarkt\.claude\plans\inherited-cuddling-yao.md`) — popup de planning et de visibilité (Feature 1), répartition d'une période de relais entre plusieurs preneurs (Feature 2), co-administration partagée (Feature 3, plus grosse, reportée jusqu'à validation des deux premières). **#303** (Feature 1, branche `feat/relais-planning-alerte-partage`) : nouveau bouton "🗓️ Je regarde mon planning" dans `RelaisAlertModal` qui ferme le popup sans rien décider (le besoin reste ouvert, l'alerte revient à la prochaine connexion) ; nouveau `lib/relaisAlerts.ts` centralisant la logique d'identité/requête déjà dupliquée entre `RelaisAlertModal.tsx` et `Entraide.tsx` ; nouveau bouton "🔔 Mes alertes" (Mon Compte, admin et visiteur) regroupant alerte RGPD + besoins de relais ouverts + historique de recasage ; liste des personnes sollicitées visible dans le détail du popup dès que le ciblage n'est pas "tous les proches". **#304** (Feature 2, branche `feat/relais-periode-partagee` chaînée sur la précédente) : nouvelle table `task_relais_coverage` (une ligne par sous-période réclamée), `lib/relaisCoverage.ts` avec une vraie fusion d'intervalles (`isRelaisFullyCovered`/`computeRelaisGaps`, pas une simple somme de jours), flux de claim en deux choix ("🙋 Je m'en charge (ce qu'il reste)" vs "📅 Choisir une période"), désinscription individuelle par sous-période qui rouvre le trou correspondant si besoin. Cette PR a été mergée dans la branche intermédiaire `feat/relais-planning-alerte-partage` et non directement dans `main` — piège de navigation de PR chain repéré en préparant #305, `main` ne contenait donc que la Feature 1 jusque-là. Après un premier test complet sur device (tunnel Expo), l'utilisateur a renvoyé un retour détaillé en 4 points, tous implémentés dans **#305** (poussée sur la même branche `feat/relais-periode-partagee`, PR ouverte directement vers `main` pour ramener Feature 1 + Feature 2 + ce round en une fois, sans conflit) : (1) le popup de remerciement après claim affiche désormais un message spécifique à la catégorie relais (les autres personnes sollicitées seront informées) au lieu du texte générique invitant à revenir cliquer sur "Fait" ; (2) nouveau bloc "🤝 Mes engagements de relais" dans Mon Compte (admin et visiteur, nouveau composant `MyRelaisCommitments.tsx`), sur le modèle de "Mes Checklists" ; (3) "🔔 Mes alertes" ne montre plus tout l'historique permanent de `reservation_change_history` mais seulement les entrées jamais vues (nouvelle colonne `seen`, marquée à la fermeture du popup) ; (4) le choix de période se scinde en deux popups centrés successifs "Du"/"Au" (au lieu d'un seul step combiné), avec une nouvelle prop `allowedRange` sur `MiniCalendar.tsx` qui grise/bloque les jours hors de la période demandée par l'admin et surligne en orange ceux qui sont dedans, plus un rappel persistant de cette période affiché à chaque étape du flux. L'utilisateur a mergé #305, confirmé l'application manuelle de la migration `reservation_change_history_seen.sql`, puis demandé de générer ce handoff avant de tester sur device.

État "done" : atteint pour le code — PR #303, #304 (contenu repris dans #305) et #305 sont mergées. **Aucun test on-device n'a encore eu lieu sur le contenu de #305** (le round de retours lui-même) — contrairement au cycle précédent, ce handoff est généré *avant* validation utilisateur du round de retours, à la demande explicite de l'utilisateur ("Je ferais les test après que tu ais généré le handoff"). La partie handoff (ce fichier + le docx + le PRD) est finalisée dans cette session, reste à committer/pousser/ouvrir la PR docs.

## 2. État actuel
- `Documentation/Documentation Fonctionnalités.docx` : passée de V1.13 à V1.14 dans cette session — §5.9 Entraide (admin), ligne "Besoin de relais ponctuel" complétée d'un long ajout couvrant #303/#305 (bouton "Je regarde mon planning", liste des sollicités, répartition multi-preneurs par sous-périodes, popups "Du"/"Au" bornés à la période demandée, message de remerciement dédié) ; §5.11 Compte admin (table 17) et §6.4 Compte visiteur (table 26) : deux nouvelles lignes de tableau insérées dans chacune (pas de simple ajout de texte cette fois) — "🔔 Mes alertes" (juste après le profil/les informations) et "🤝 Mes engagements de relais" (juste après le bouton "🆘 Besoin de relais"/"👥 Visiteurs", avant "Checklist personnelle") ; métadonnées (§0) mises à jour.
- `PRD_AvecToi_v1_4.md` : passé de v1.12 à v1.13 dans cette session — nouveau bloc changelog v1.12→v1.13 (3 entrées datées avec n° de PR), §2 complété côté admin et côté visiteur des deux nouveaux boutons "🔔 Mes alertes"/"🤝 Mes engagements de relais", §3.8 (paragraphe "Besoin de relais ponctuel") réécrit pour intégrer les 4 nouveautés, §5 (schéma DB) : nouvelle table `task_relais_coverage` documentée, colonne `seen` ajoutée au schéma de `reservation_change_history`.
- Relecture obligatoire effectuée avant de considérer l'édition du docx terminée : re-dump complet des tables 15/17/26 + la table de métadonnées après le script d'édition — les 2 nouvelles lignes par table, l'ajout textuel de la table 15 et le bump de version sont passés sans troncature ni corruption, aucune erreur de première passe.

**Dernière action avant ce handoff :** rédaction de ce fichier lui-même (`Handoff/handoff.md`) — Historique cumulé et sections 1-5 mis à jour après le docx et le PRD.

## 3. Fichiers concernés
- `components/RelaisAlertModal.tsx` → bouton "Je regarde mon planning", liste des sollicités, refactor vers `lib/relaisAlerts.ts` (#303).
- `components/MyAlertsModal.tsx` → nouvelle section besoins de relais (#303) ; `history` reçu déjà filtré aux entrées non vues côté appelant (#305, pas de changement interne au composant).
- `lib/relaisAlerts.ts` (nouveau, #303) → `relaisIdentityKey`/`resolveRelaisIdentity`/`fetchOpenRelaisAlerts`, extrait de code dupliqué.
- `lib/relaisCoverage.ts` (nouveau, #304) → `isRelaisFullyCovered`/`computeRelaisGaps`, fusion d'intervalles.
- `components/Entraide.tsx` → flux de claim relais en 2 étapes + affichage des contributeurs/désinscription par ligne (#304) ; texte de remerciement spécifique relais + split du choix de période en deux popups centrés "Du"/"Au" + rappel persistant de la période demandée (#305, fichier le plus gros de l'app, ~4700+ lignes).
- `components/MiniCalendar.tsx` → nouvelle prop optionnelle `allowedRange` (grise/bloque hors période, orange dedans), sans effet sur les 9+ autres appelants qui ne la passent pas (#305).
- `components/MyRelaisCommitments.tsx` (nouveau, #305) → bloc "🤝 Mes engagements de relais", requête `task_relais_coverage` filtrée par identité.
- `app/(admin)/account.tsx`, `app/(visitor)/account.tsx` → chargement des alertes relais + badge + câblage `MyAlertsModal` (#303) ; intégration `MyRelaisCommitments` + filtrage/marquage "vu" de l'historique (#305).
- `lib/types.ts` → nouveau champ `seen` sur `ReservationChangeHistoryEntry` (#305).
- `supabase/migrations/20260827_task_relais_coverage.sql` (#304) et `20260827_reservation_change_history_seen.sql` (#305) → migrations manuelles, voir "Connu, non bloquant" pour le statut de confirmation.
- `Handoff/handoff.md` → ce fichier.
- `Documentation/Documentation Fonctionnalités.docx` → V1.13 → V1.14, mis à jour dans cette session.
- `PRD_AvecToi_v1_4.md` → v1.12 → v1.13, mis à jour dans cette session.

## 4. Ce qui a échoué
Rien de bloquant ce cycle. Le seul accroc a été de navigation de PR chain, pas de code : #304 a été mergée dans la branche intermédiaire `feat/relais-planning-alerte-partage` plutôt que directement dans `main`, laissant `main` en retard d'une Feature complète sans qu'aucune erreur ne le signale — repéré seulement en vérifiant `gh pr view 304` avant d'ouvrir #305, via `git log --oneline origin/main` qui ne montrait que la Feature 1. Corrigé en ouvrant #305 directement `feat/relais-periode-partagee → main`, qui ramène tout le contenu sans conflit (le merge-base des deux branches ne divergeait que par des commits de merge, pas de contenu réel).

Aucune erreur de compilation introduite sur ce cycle (`npx tsc --noEmit` vérifié avant chaque commit, seules les erreurs préexistantes non liées — Edge Functions Deno, `lib/notifications.ts` — subsistent).

## 5. Prochaine étape
Committer `Handoff/handoff.md`, `Documentation/Documentation Fonctionnalités.docx` et `PRD_AvecToi_v1_4.md` sur une branche `docs/` neuve (depuis `origin/main`, à jour avec #303-305 mergées), pousser, ouvrir la PR correspondante.

**⚠️ Priorité immédiate après ce handoff :** l'utilisateur va tester sur device le round de retours (#305) — thanks-modal, bloc Mon Compte, filtrage "Mes alertes", popups "Du"/"Au". Aucun de ces 4 points n'a encore été validé en conditions réelles.

**⚠️ Rappels à transmettre à l'utilisateur :** (1) le rappel de #290 sur `supabase/migrations/20260821_visitor_profiles_relation.sql` reste valable, statut toujours non confirmé — inchangé par ce cycle ; (2) reconfirmer que `20260827_task_relais_coverage.sql` a bien été appliquée (seule `reservation_change_history_seen.sql` a été explicitement confirmée dans ce message-ci) ; (3) Feature 3 (co-administration) du plan reste explicitement en attente de la validation de ce round sur device avant de démarrer.

Points en suspens à reproposer sur demande : nettoyage de la branche distante `feat/entraide-creer-checklist` (commit orphelin `e9472c9`), reconstitution PR-par-PR du cycle #153-224 et #253-259 si ce niveau de détail devient utile, `PRD_ClaudeCode_Site_avectoi_care_v3.md`/`AUDIT_RLS_TAILLE_CODE_MORT.md`/`eslint.config.js` non suivis à committer ou nettoyer, unification éventuelle des trois implémentations de désengagement/désinscription (voir "Connu, non bloquant").

Items reportés (inchangés depuis les cycles précédents, ordre suggéré, sur demande uniquement) : rebase de `fix/souvenirs-rls-and-news-delete`, décision `docs/spec-web-upgrade`, déploiement `avectoi-site` sur Infomaniak (bloqué sans les accès de l'utilisateur), isolation Supabase, taille de l'app.
