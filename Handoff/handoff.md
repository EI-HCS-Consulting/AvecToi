# Handoff — AvecToi
_Généré le : 2026-09-01 (cycle #352-356)_

## État global du projet

**Stack :** React Native + Expo SDK 51+, Expo Router, Supabase (BDD/Auth/Storage/Realtime/Edge Functions), EAS Build + EAS Update (channels development/preview/production, `expo-updates`, publication OTA automatique sur push `main` via `.github/workflows/eas-update-preview.yml`). expo-notifications, expo-calendar, expo-image-picker, expo-sharing (partage simple) + react-native-share (partage groupé multi-fichiers, dépendance native ajoutée #245). Resend (transactionnel) branché sur les Edge Functions. Stripe côté web uniquement. Supabase CLI bloqué sur cette machine (App Control Policy Windows) : déploiement des migrations/Edge Functions exclusivement via le Dashboard Supabase, jamais auto-appliqué par Claude.

**⚠️ Règle Git STRICTE (CLAUDE.md) :** chaque modification, même un correctif ponctuel, part sur une branche neuve (depuis `origin/main` à jour) et se termine par sa propre PR poussée sur GitHub — jamais de nouveau commit ajouté à une branche dont la PR est déjà mergée. **Autorisation permanente : commit + push + PR dès qu'une tâche de code est terminée et vérifiée, sans attendre que l'utilisateur le redemande.** **L'utilisateur merge lui-même, puis teste** (le merge déclenche la publication OTA — sauf si la modif ajoute une nouvelle dépendance native, auquel cas un `eas build` + réinstallation complète de l'APK est nécessaire). Toujours vérifier l'état réel d'une PR via `gh pr view <n> --json state,url` plutôt que de supposer — ce repo merge vite, souvent en un seul message de l'utilisateur.

**⚠️ Convention UI (CLAUDE.md, section "Conventions UI", ajoutée #268) :** toute nouvelle confirmation doit utiliser le composant thémé `ConfirmModal`, jamais `Alert.alert` (natif, hors charte) ; tout nouveau bouton doit reprendre les couleurs/emoji/style déjà en usage à l'écran ; toute notification éphémère doit réutiliser le pattern `showToast` existant. Vérifier ce point avant toute PR ajoutant un popup ou un bouton, pour éviter une PR d'harmonisation de rattrapage.

**Repo GitHub :** `https://github.com/EI-HCS-Consulting/AvecToi`, branche `main` protégée par ruleset (PR obligatoire).

**PR ouverte, en attente de test/merge :** aucune. PR #356 est la dernière mergée en date (documentation de ce cycle non encore ouverte — voir §5. Prochaine étape).

**Livré (V1 + V2 archivé) :** tout le socle (points 1-10 CLAUDE.md), RGPD 60j par défaut + prolongation gratuite 30j + gate J-7, purge auto + sauvegarde schéma hebdo, onboarding séquencé, cap freemium (8 visites), Paramètres 4 sections + historique, emails Resend, Chronologie, EAS Update automatisé. **Entraide : Urgent automatique à J+3 persistant après échéance, regroupement par créneau, actions visiteur, scroll ciblé** *(nouveau ce cycle, #352, #355-356)* : le tag Urgent (cadre rouge) se déclenche désormais automatiquement dès qu'un besoin ouvert entre dans sa fenêtre d'échéance à J+3 ou moins, quelle que soit son ancienneté de publication, et reste actif même une fois l'échéance dépassée tant qu'il n'est pas pris en charge (borné en pratique à une fenêtre courte par la fermeture automatique existante) ; le bouton « Fermer » de la popup de réservation (qui ne revenait toujours pas à Mon Compte malgré la tentative `router.dismissTo` du cycle précédent — cause racine : incompatible avec la frontière Tabs/Stack) utilise désormais `router.navigate` ; les réservations d'un même jour et créneau se regroupent en un seul bloc visuel dans le Planning mensuel/hebdo ; le popup de sa propre réservation (visiteur) gagne « 📅 Ajouter à mon calendrier » et « 🗑️ Annuler cette visite » ; taper une réservation depuis la fiche d'un visiteur scrolle et surligne directement le créneau horaire concerné (pas seulement le bon jour) ; le bouton « J'ai compris » ramène désormais vers le besoin lui-même sauf pour un relais ponctuel (accueil, inchangé) ; taper le créneau d'un autre visiteur ouvre un popup de confirmation au lieu de réserver/naviguer silencieusement ; le badge « … partiellement » d'un besoin courses disparaît dès qu'une prise en charge formelle existe. **Nouvelles admin visibles des visiteurs, badges « New » unifiés, date de prise en charge** *(nouveau ce cycle, #353-354)* : les nouvelles publiées par l'admin sont désormais toujours visibles des visiteurs (l'ancien gate hérité du rôle Intervenant masqué en V1 les rendait invisibles par défaut sans bascule fonctionnelle) ; `lib/wallUnread.ts` réécrit en un seul mécanisme persistant (`useWallReadTracking`) alimentant à la fois le point rouge des onglets et les badges « New », corrigeant trois incohérences (point rouge sans badge visible, badge manquant sur besoin fermé/pris en charge jamais vu, badge absent pour le publieur lui-même) ; nouvelle ligne « 📌 Pris en charge le » sous « 🗓️ Publié le » dans Mon Compte (claim classique ou simple contribution courses sans claim explicite, nouvelles colonnes `tasks.claimed_at`/`shopping_list_items.bought_at`) ; les réponses postées sur les nouvelles d'autrui remontent désormais aussi dans Mon Compte ; une réservation du jour même dont l'horaire est déjà passé bascule immédiatement en Historique sans attendre minuit (`isReservationFullyPast`). **Indicateur « New » remplace le cadre orange + fiabilité en arrière-plan + 3 correctifs Entraide/Nouvelles/réservation** *(#346, #348-350)* : le cadre orange non lu (#344) ne plaisait pas visuellement — remplacé par une barre latérale bleue + badge « New » sur toute publication d'un autre auteur postée depuis la connexion en cours (marquage 100% en mémoire, `sessionLoginTimestamp` fixé une fois au chargement du bundle JS, sans persistance ni table "dernière connexion" ; sur un besoin Urgent, seul le badge reste affiché sans barre bleue) ; les murs Entraide/Soutien/Nouvelles ne se rechargeaient jusque-là que via un canal Supabase Realtime souscrit une seule fois au montage, susceptible de se couper silencieusement en arrière-plan — filet `AppState` ajouté pour recharger systématiquement au retour au premier plan ; dans Entraide, les besoins encore "New" remontent groupés en tête de la section Ouverts (chaque groupe gardant son tri Urgent/chronologique habituel) ; appui long sur une photo du fil Nouvelles pour la télécharger, comme sur le Mur de soutien ; popup PIN (Modifier/Annuler une réservation) passé à la largeur pleine des autres popups de l'app. **Murs Entraide/Soutien/Nouvelles : cadre orange non-lu + délai de perception** *(#344)* : corrige l'absence de point rouge (onglets Nouvelles/Soutien) et de mise en avant des publications non lues à la connexion/navigation — un délai d'environ 1,2s est désormais laissé avant tout marquage automatique "vu" déclenché par la seule présence à l'écran (un vrai scroll utilisateur reste marqué immédiatement, les murs étant triés du plus récent au plus ancien l'élément non lu le plus probable était sinon marqué "vu" dans le même cycle de rendu que son premier affichage) ; le fond pastel orange est remplacé par un cadre orange sur chaque publication non lue ; sur un besoin Urgent (déjà cerné de rouge), le cadre orange se superpose à l'intérieur du cadre rouge plutôt que de le remplacer. **Alerte accompagnant sur cascade + fond non-lu Entraide/Soutien/Nouvelles** *(nouveau ce cycle, #341-342)* : l'accompagnant dont le créneau est déplacé en cascade (édition visiteur avec "Modifier aussi le créneau de X" coché) reçoit une alerte de connexion + "Mes alertes" précisant qui a changé quoi et le nouveau jour/horaire ; nouveau mécanisme partagé `lib/wallUnread.ts` affichant un fond pastel orange sur toute publication non lue des murs Entraide/Soutien/Nouvelles (effacé seulement au défilement dans la zone visible, pas à l'ouverture de l'onglet, avec bootstrap anti-historique à la 1ère connexion) et un point rouge sur les pictogrammes Soutien/Nouvelles de la barre d'onglets (même convention qu'Entraide). **Neuf correctifs Entraide/Mon Compte groupés après test de l'assistant "Publier"** *(nouveau ce cycle, #334-340)* : droits d'édition admin restreints à ses propres publications (Entraide + Nouvelles), lien Google Maps non prématuré, dédoublonnage Mon Compte/Entraide, liens profonds précis vers réservation/besoin/nouvelle/message avec cadre rouge d'atterrissage, date de publication + horaire Transport 3 lignes, popup réservation admin ouvrant directement Modifier/Annuler sans PIN superflu côté visiteur, alerte + bandeau de suppression admin en douceur, filtre "Mes besoins", vues "Planifié/À venir" + "Historique" repliable sur réservations et besoins, réservations passées verrouillées, "Publié par XXX", tri affiné, compteurs Ouvert/Fermé scopés au filtre, fermeture automatique étendue aux besoins pris en charge à échéance dépassée, "↩ Réouvrir" bloqué après échéance définitive, correctif racine "Prochaine disponibilité" (flag intervenant ignoré). **Entraide : assistant "Publier" en popups chaînées** *(nouveau ce cycle, #320-328)* : la création d'un besoin (hors relais ponctuel) remplace l'ancien formulaire unique à défilement par une séquence de popups centrées catégorie par catégorie (Repas/Administratif/Autre, Courses avec accès direct aux Produits récurrents, Transport en 4 écrans Départ/Arrivée → type de trajet → calendrier → horaires) puis un écran commun "Autres options" avant Publier — l'édition d'un besoin déjà publié garde l'ancien formulaire ; au passage, le bouton "Je m'en occupe" d'un besoin Courses disparaît dès qu'un article est coché, et le popup Produits récurrents surligne les articles déjà ajoutés (#320). **Entraide/Transport : lien Google Maps, propositions étendues et droits de validation** *(nouveau ce cycle, #329-332)* : champ "🗺️ Lien Google Maps" bidirectionnel sur l'adresse de départ, recalculé automatiquement à l'enregistrement pour toujours inclure le code postal et la ville ; la personne pour qui le besoin a été publié (pas seulement l'auteur) peut consulter et valider les propositions reçues, avec un menu Accepter/Répondre par appui long et une alerte au proposant ; la liste des propositions est désormais visible par tout visiteur, mais seuls l'auteur, le bénéficiaire ou l'admin-auteur peuvent valider ou décliner ("Aucune ne convient", protégé par une confirmation) — une proposition déclinée reste visible avec un tag "❌ Déclinée" au lieu d'être supprimée ; le tri chronologique départage une égalité de date par l'horaire du premier trajet ; un besoin Transport ne peut plus être créé avec une date/horaire d'aller déjà passés le jour même. **Entraide : tri des besoins + regroupement fermés, tags contextuels, produits récurrents, corrections UX** *(nouveau ce cycle, #310, #315-#318)* : les besoins ouverts sont triés Urgent en tête puis par proximité croissante ; un besoin pris en charge rejoint les besoins fermés, répartis en « À venir »/« Historique » ; le tag Urgent (cadre rouge) se coche automatiquement si l'échéance tombe à J+2 ou moins et disparaît dès que le besoin n'est plus ouvert ; un besoin fermé et passé ne peut plus être supprimé ni désinscrit par son auteur ; icône de suppression déplacée en position fixe face au titre ; ligne « 🗓️ Publié le … » ajoutée ; catégorie Courses dotée d'un catalogue « 🔁 Produits récurrents » par espace (auto-alimenté, admin peut le nettoyer) et d'une popup dédiée de création ; croix de suppression rapide d'un article restreinte à l'auteur du besoin ou à l'admin. **Accueil : tri chronologique des visites planifiées** *(nouveau ce cycle, #316, corrigé #318)* : « Autres visites planifiées » (à venir) trié du plus proche au plus éloigné, « Autres visites réalisées » (passées) inchangé (plus récent en premier). **Sélection multiple visiteur + badges du pictogramme Entraide** *(nouveau ce cycle, #311, corrigé #312, affiné #313)* : un visiteur peut désormais, par appui long, sélectionner plusieurs besoins qu'il a lui-même publiés pour les supprimer en masse (même mécanique que l'admin, strictement limitée à ses propres besoins) ; le pictogramme Entraide de la barre d'onglets (admin et visiteur) garde la couleur normale du thème et affiche jusqu'à 2 points rouges empilés façon « : » — le point du haut signale un besoin Urgent non pris en charge, celui du bas un nouveau besoin publié par quelqu'un d'autre depuis la dernière visite de l'écran par ce viewer. **Répartition d'un besoin de relais entre plusieurs preneurs** *(nouveau ce cycle, #305)* : sous-périodes distinctes réclamables par personne (table `task_relais_coverage`), désinscription individuelle par sous-période, choix de la période en deux popups centrés « Du »/« Au » restreints à la fenêtre demandée par l'admin, bloc « 🤝 Mes engagements de relais » dans Mon Compte. **Popup relais enrichi + « 🔔 Mes alertes »** *(#303, #305, affiné #307-#308)* : bouton « 🗓️ Je regarde mon planning » (ferme sans décider, l'alerte revient à la prochaine connexion), liste des personnes sollicitées visible dans le popup, historique de « 🔔 Mes alertes » filtré aux seules entrées jamais vues (`reservation_change_history.seen`), marquées vues uniquement via un bouton « Marquer comme lu » explicite par entrée *(corrigé #308, plus à la fermeture du popup)*. Un besoin déjà couvert par l'identité connectée sort des alertes actives et bascule en Historique, et le bouton « J'ai compris » du popup de remerciement ramène vers l'accueil *(#307)*. **Photo de profil (Mon Compte visiteur)** *(nouveau ce cycle, #308)* : repli automatique sur la copie serveur si la photo locale (`localPhotoUri`) ne charge plus après un rebuild natif. **Publication d'une checklist suggérée avec choix de destination** *(#297)* : Mur d'Entraide seul, « Mes Checklists » seul, ou les deux à la fois avec synchronisation automatique (statut « Fait », identité de qui s'en occupe) ; l'auteur d'un besoin (visiteur inclus) peut désormais le supprimer lui-même, avec suppression en cascade proposée du reste de la checklist suggérée si d'autres items restent publiés et ouverts *(#296, #297)*. **Import privé (Mon Compte) indépendant du Mur** *(#295)* : l'anti-doublon public ne s'applique plus qu'en activant « Publier aussi sur le Mur d'Entraide ». **Traçabilité "Pas cette fois"** sur un besoin de relais ponctuel, visible admin uniquement *(#295)*. **Calendrier principal (Accueil)** : composant unique `HomeCalendarScreen` partagé admin/visiteur *(#292)* — Mensuel/Hebdo, "⚡ Prochaine disponibilité", grisage pré-admission, marqueurs admission/sortie, bouton « 📅 Créneaux » dans Planning du jour, email de confirmation par accompagnant, synchronisation Mensuel→Hebdo sur le jour sélectionné *(#295)*. **Sélection du jour inversée** *(#293)* : l'onglet bas « Accueil » conserve désormais la sélection entre deux visites, seul le bandeau « 📅 Calendrier » réinitialise sur aujourd'hui. **Rôle Intervenant retiré de V1** *(#291-292)* : masqué derrière `INTERVENANT_ROLE_ENABLED=false` (pas supprimé — code archivé dans `Développement V2/` pour réintégration future), plus aucune trace admin (Paramètres → Règles/Histo, Chronologie) ni visiteur. **Panneau admin Calendrier + permissions Nouvelles**, **Fiche intervenant express**, **Calendrier des visites (visiteur)**, onglets réorganisés (Accueil en tête, Médias en grille), **"Mes souvenirs"** personnelles, **Besoin de relais ponctuel** (réservé admin), **Liste de courses** affinée (cochage attribué/verrouillé), **Réservations récurrentes**, **Lien visiteur-patient**, **Popup jour d'hospitalisation dédié**. Détail complet des cycles antérieurs : voir Historique cumulé ci-dessous.

**Connu, non bloquant :**
- Branche distante `feat/entraide-creer-checklist` (PR #140, déjà mergée) porte toujours un commit orphelin (`e9472c9`) — nettoyage jamais confirmé par l'utilisateur, sans conséquence fonctionnelle.
- **Trois implémentations distinctes du "désengagement/désinscription"** coexistent volontairement (convention du repo : `account.tsx`/`Entraide.tsx` dupliquent leurs flux plutôt que de les partager) : `performUnclaim` (Entraide.tsx, avec nettoyage de la photo de claim, utilisé par Transport + le bouton du formulaire d'édition), et `lib/taskDisengage.ts` (les deux `account.tsx`, sans nettoyage de photo). À garder en tête si une future demande vise à tout unifier.
- Migrations manuelles en attente de confirmation d'exécution : `20260819_shopping_list_items.sql`, `20260820_tasks_relais_ponctuel.sql`, `20260821_shopping_list_items_bought_by.sql` (confirmées appliquées) — voir Historique cumulé. **⚠️ `supabase/migrations/20260821_visitor_profiles_relation.sql` (introduite par #278) : statut d'application non confirmé par l'utilisateur.** #279 ajoute un repli client qui masque le symptôme (select `visitor_profiles` incluant `relation` échouant entièrement si la colonne n'existe pas encore côté base — comportement PostgREST), mais la migration doit être rejouée manuellement dans le dashboard Supabase pour que le lien visiteur-patient soit réellement stocké/lu — le repli n'est qu'un filet de sécurité, pas un correctif. `20260827_reservation_change_history_seen.sql` (#305, rendue idempotente en #307 — `drop policy if exists` avant `create policy`) : **confirmée appliquée par l'utilisateur** le 27/08/2026. `20260827_task_relais_coverage.sql` (#305, introduite plus tôt dans le même cycle pour la table de couverture des sous-périodes) : statut d'application non reconfirmé explicitement — à revérifier si un test de la répartition multi-preneurs échoue en base. **⚠️ `supabase/migrations/20260830_deleted_content_seen.sql` (introduite par #337, colonne `deleted_seen` sur `tasks`/`news_entries`/`support_messages`) : statut d'application non confirmé par l'utilisateur.**
- **Documentation à jour** : `Documentation/Documentation Fonctionnalités.docx` synchronisée jusqu'à la PR #356 (V1.22, 01/09/2026) — §4 (transverse) complétée du regroupement par créneau (Planning mensuel/hebdo, #355) et de la réécriture unifiée `wallUnread`/badges « New » (#353-354) ; §5.7 (Nouvelles) complétée de la levée du gate Intervenant sur les nouvelles admin (#353) ; §5.9 (Entraide) complétée de l'Urgent auto J+3 persistant (#355-356), du "J'ai compris" qui ramène au besoin (#352) et du badge courses partiel qui disparaît après claim (#352) ; §5.11/§6.4 (Compte) complétées du correctif `router.navigate`, de `isReservationFullyPast`, de la date "Pris en charge le" et des réponses/contributions courses remontées (#353-354) ; §6.2 complétée du popup de confirmation sur créneau d'autrui, des boutons calendrier/annulation et du scroll ciblé sur créneau (#352, #355) ; en-tête métadonnées mis à jour ; `PRD_AvecToi_v1_4.md` passé en v1.21 avec un changelog dédié (§4/§5.7/§5.9/§5.11/§6.2/§6.4).

**En cours / pas commencé (reporté, à reprendre sur demande) :**
- `fix/souvenirs-rls-and-news-delete` : branche stale, 2 migrations RLS jamais exécutées en prod (`AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Isolation Supabase (séparer l'instance prod partagée avec le site web historique) : plan complet dans `ISOLATION_SUPABASE.md`.
- `docs/spec-web-upgrade` : en attente d'une décision utilisateur.
- Taille de l'app (~180 Mo) et audit code mort : volontairement reporté (voir `AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Points 13-14 (EAS Build APK signé, fiche Play Store) : pas commencés.
- Site marketing `avectoi-site` (dépôt séparé) : toujours pas déployé (Infomaniak, bloqué sans les accès de l'utilisateur).
- Branche distante `feat/entraide-creer-checklist` : commit orphelin à nettoyer, voir "Connu, non bloquant" ci-dessus.
- `PRD_ClaudeCode_Site_avectoi_care_v3.md` et `AUDIT_RLS_TAILLE_CODE_MORT.md` : fichiers non suivis présents dans l'arbre de travail, non touchés par ce cycle.

## Historique cumulé
- Jusqu'au 2026-07-22 : lots 1-10 (fonctionnalités de base) + refonte intervenant Phase A/B, RGPD 90j + purge auto + sauvegarde schéma auto, checklists perso réutilisables, rattachement multi-espaces intervenant, EAS Update automatisé — PR #7 à #102.
- 2026-07-22 → 08-01 : resynchronisation PRD (#103), snapshot schéma (#104), liens d'invitation → `app.avectoi.care` (#105), verrou Premium "Planning des intervenants" (#106), fix datepicker onboarding + navigation Réglages (#107), fix décalage de date/toggle intervenants/lien email confirmation (#108, remergé sous #111), site marketing `avectoi-site` construit et poussé (pas déployé), règle Git "1 branche = 1 PR" documentée (#109).
- 2026-08-01 → 08-02 : diagnostic + fix définitif du lien Google Maps (#110), Site URL Supabase corrigé, PRD v1.6→v1.7 (#112), rafraîchissement adresse + popup centré + première version du flux in-app changement de nom (#113).
- 2026-08-09 → 08-10 (matin) : Edge Function `notify-name-change` recréée sous le bon slug (#115-117), flux changement de nom confirmé fonctionnel.
- 2026-08-10 (journée) : bug tenace du bouton "Fermer" invisible résolu après 6 tentatives infructueuses — cause : styles partagés `btnPrimary`/`btnPrimaryText` réutilisés hors d'une rangée bornée ; correctif = taille fixe + styles 100% inline (PR #118-128).
- 2026-08-10 (soir) → 08-11 (matin) : texte "Partager" personnalisé, refonte UX "Mes Checklists", popups centrés étendus à toute l'app (23 popups), 8 boutons "Fermer"/"Annuler" invisibles corrigés au global — PR #130, #131.
- 2026-08-11 : nouveau flux "Créer une checklist" dans Entraide/Publier/Administratif + renommage "Checklists suggérées" dans Mon Compte (PR #140) ; correctif texte invisible sur 2 boutons du même flux + renommage du bandeau admin Entraide (PR #141).
- 2026-08-11 → 08-12 : cycle non détaillé PR-par-PR — PR #133 à #149 : cadres de réservation et légende du calendrier, refonte du planning des intervenants en vue hebdo, historique des soins planifiés, modération soins/visites, réservation de soins par l'intervenant depuis le calendrier, largeur du popup d'intervention, panneau et bascule de vue du planning intervenant.
- 2026-08-12 → 08-13 : vue Hebdo réservable pour tous les rôles avec marqueurs hospitalisation/sortie, parité complète visiteur/admin (#150) ; alignement visuel des switches du calendrier + bande verte "mes visites" (#151) ; panneau "Historique des soins" intervenant scindé en "Soins planifiés"/"Historique des soins" (#152).
- 2026-08-13 → 08-17 : cycle non détaillé PR-par-PR — PR #153 à #224 : panneau admin Calendrier + permissions Nouvelles (#192-200), planning intervenants multi-espaces "Mes espaces patients" avec édition croisée (#201-207), stabilisation planning cross-space — couleurs, popups soin, longpress carte, créneaux déjà pris, sécurité nom patient, navigation jour (#208-223), plus plusieurs cycles calendrier/checklist/nuitées intermédiaires (#153-191).
- 2026-08-17 (matin) : "Mes Modèles" intervenant passe d'un import direct à un popup de sélection (#225), suppression d'une checklist perso entière par appui long (#226), anti-doublons global + complétion incrémentale + grisage des modèles entièrement importés (#227).
- 2026-08-17 : date du jour figée sur l'onglet Accueil, panneau "Visites/Soins planifiés" filtré sur la période affichée (#230) ; page d'accueil par défaut en mode Visite (#231).
- 2026-08-17 : RGPD 90j → 60j de rétention par défaut, prolongation ramenée à 30j, popup in-app systématique J-7 (#232) ; gate "Prolonger" à J-7 exactement, calendrier admin filtrable par intervenant (#233) ; popup RGPD harmonisé (`ConfirmModal`), photos/pictogrammes intervenants, droits d'édition admin restreints à ses propres soins (#234).
- 2026-08-17 : Fiche intervenant express côté admin — création sans compte (`pin` nullable), confirmation de créneau par SMS/email (#235).
- 2026-08-17 : Calendrier des visites (visiteur) refondu façon Planning intervenant (#236) ; 5 correctifs post-test dont une faille de sécurité (Modifier/Y Aller sans vérification de propriété) (#237).
- 2026-08-17 : blocage du tap sur créneau passé + accompagnants sur la ligne principale (#238) ; onglet "Accueil" en tête de barre, "Souvenirs" retiré de la barre, vue "Médias" en grille (#239) ; correctifs post-test Médias (#240).
- 2026-08-17 → 08-18 : "Mes souvenirs" — page personnelle publiées/téléchargées (#241), sélection multiple Nouvelles/Soutien (#242), correctif tracking + faille PIN (#243), migration idempotente (#244), partage groupé multi-médias `react-native-share` + vignette Mes Nouvelles — premier vrai rebuild EAS natif du projet (#245), sélection multiple dans Mes souvenirs (#246). Doc PR #247 : handoff + docx (V1.4→V1.5) synchronisés jusqu'à #246, refermant un gap de 18 PR (#228-246).
- 2026-08-18 : bibliothèque de checklists passée de 3 à 11 modèles, orientés proche aidant, données fournies par l'utilisateur (#248) ; échéance de lot + urgence/précision par item, toggle privé/public par défaut privé, pièces à réunir cochables, correctif de type `checklist_context` (#249).
- 2026-08-18 : refonte en assistant séquentiel per-item (précision → échéance → urgence → photo), `repit_aidant`/`conge_proche_aidant` passés `personalOnly`, correctif de doublons sur besoins supprimés en douceur, migration de la contrainte CHECK jamais élargie depuis #248 (#250).
- 2026-08-18 : pièces à réunir nichées sous leur item parent, liens officiels persistants après import + 12/15 liens corrigés, anti-doublon croisant aussi le privé, premiers modèles de courriers administratifs (congé proche aidant → employeur) (#251). Doc PR #252 : handoff partiel (#248-251).
- 2026-08-18 → 08-19 : cycle #253-259 non détaillé PR-par-PR — correctif RLS, décalage d'onglet, alignement droit des courriers, édition/suppression dans "Mes documents".
- 2026-08-19 : "Mes documents" — "Retélécharger" devient "👁️ Aperçu" avec rendu WYSIWYG, boutons Modifier/Télécharger (#261) ; suppression du bouton "Envoyer" (doublon fonctionnel de "Télécharger"), "Modifier" harmonisé en bleu plein, 7 nouveaux modèles de courriers (autorisation de soins enfant, attestation d'autorité parentale — cadrée comme déclaration sur l'honneur —, courrier école/crèche, déclaration mutuelle/CPAM, procuration bancaire, absence hospitalisation d'un proche, déclaration de sinistre) (#262).
- 2026-08-19 : pré-remplissage automatique des courriers à partir des données déjà connues de l'app (nom, adresse, hôpital, date de naissance patient), correctif d'un titre de checklist à 2 lignes qui poussait le chevron hors écran (#263).
- 2026-08-19 : nouvelle fonctionnalité "liste de courses" — catégorie Courses devient une liste d'articles cochables en temps réel (nouvelle table `shopping_list_items`, popup `ShoppingListModal`), surfacée aussi dans "Mes documents" (préfixe 🛒) (#264) ; bloc de saisie encadré comme le formulaire Transport, auto-complétion du besoin quand le dernier article est coché (#265).
- 2026-08-19 : désengagement généralisé à toutes les catégories via `performUnclaim` réutilisé (bouton dans "Modifier le besoin" + appui long sur carte "Fait"), auto-réouverture symétrique d'une liste de courses en cas de décochage (#266) ; harmonisation du wording ("Me désengager"), nouveau helper partagé `lib/taskDisengage.ts`, section "besoins pris en charge" ajoutée à Mon Compte admin (#267).
- 2026-08-19 : les trois confirmations de désengagement passent d'`Alert.alert` à `ConfirmModal`, nouvelle section "Conventions UI" dans `CLAUDE.md` (#268).
- 2026-08-19 → 08-20 : nouvelle fonctionnalité "besoin de relais ponctuel" — catégorie technique dédiée publiée uniquement depuis Mon Compte, ciblage d'audience, alerte à la connexion via `RelaisAlertModal` (#270) ; correctifs post-test (boutons centrés, onglet SOS Relais masqué si aucun besoin visible, miroir `personal_checklist_items` désynchronisé) (#271) ; liste de courses affinée en 4 étapes — cochage attribué + verrouillé au preneur en charge (#272), affichage multi-contributeurs cumulé (#273), suffixe "… partiellement" si liste incomplète (#274), verrouillage par article + complétion automatique des articles restants à la prise en charge tardive (#275). Doc PR #276 : handoff + docx (V1.9→V1.10) + PRD (v1.8→v1.9) synchronisés jusqu'à #275.
- 2026-08-20 : retrait du cochage auto des courses au claim introduit par erreur en #275, ajout de la liste des visiteurs (`VisitorsList`/`VisitorsListModal`), bouton "Intervenants"→"Visiteurs" côté visiteur, bloc "Besoin de relais" retiré du compte visiteur (réservé admin), boutons "Fiche patient"/"Visiteurs" ajoutés au compte admin (#277) ; lien visiteur-patient — colonne `visitor_profiles.relation`, catalogue `lib/relations.ts`, champ dans Mes informations, affiché sur la liste des visiteurs et leur fiche avec la phrase totem (#278) ; correctif régression PostgREST (repli sans la colonne `relation` tant que la migration n'est pas rejouée), ajout Belle-fille/Beau-fils, remplacement des chips par un `RelationPickerModal` (#279).
- 2026-08-20 : liens Beau-père/Belle-mère + réordonnancement du catalogue, Planning du jour cliquable si vide, bouton "Ajouter une Visite" depuis le popup de visite, réservation directe par tap sur un créneau libre d'un autre visiteur (#280) ; places restantes affichées, ajout Grand-père/Grand-mère, correctif traits de bord visiteur en vue Hebdo (#281).
- 2026-08-20 : réservations récurrentes — récupération d'une fonctionnalité déjà validée (#283) mais jamais réellement mergée sur `main` (branche stackée sur la mauvaise base) (#285) ; extension à l'admin, popup jour d'hospitalisation dédié (picto 🏥, titre "Hospitalisation de [prénom]"), picto anniversaire 🎂→🎉, autofocus champ Prénom à la connexion (#287) ; correctif appui long cassé sur le jour d'hospitalisation, mention "Jour d'hospitalisation" dans Planning du jour, centrage bouton récurrentes, agrandissement calendrier de plage, mémoïsation `SpaceContext`/`VisitorContext` (perf) (#288).
- 2026-08-20 → 08-21 : correctif racine du popup jour d'hospitalisation (déclenchement systématique, plus par coïncidence), correctif centrage bouton "Créer mes réservations" (`includeFontPadding`), index mémoïsé par date pour les grilles Mensuelles, synchronisation Mensuel→Hebdo sur le jour sélectionné, conservation de la sélection au retour via le bandeau "Calendrier" (#289). Doc PR #290 : handoff + docx (V1.10→V1.11) + PRD (v1.9→v1.10) synchronisés jusqu'à #289.
- 2026-08-21 : rôle Intervenant masqué en V1 derrière `INTERVENANT_ROLE_ENABLED=false` (pas supprimé, code archivé dans `Développement V2/` avec instructions de réintégration) (#291) ; masquage des dernières traces admin (bloc "Intervenants"/"Soins planifiés" en Paramètres → Règles/Histo, soins retirés de la Chronologie) et calendrier Accueil unifié admin/visiteur (`HomeCalendarScreen` commun, bouton "📅 Créneaux" dans Planning du jour, email de confirmation par accompagnant) (#292) ; sémantique de sélection du calendrier inversée — l'onglet "Accueil" conserve désormais la sélection entre deux visites, seul le bandeau "Calendrier" réinitialise sur aujourd'hui — et masquage des réservations "soins" résiduelles dans l'onglet Créneaux (#293). Doc PR #294 : handoff + docx (V1.11→V1.12) + PRD (v1.10→v1.11) synchronisés jusqu'à #293.
- 2026-08-21 : synchronisation Mensuel→Hebdo sur le jour sélectionné après correctif d'un bug de closure, checklists privées (Mon Compte) rendues indépendantes de l'anti-doublon public du Mur d'Entraide (l'anti-doublon ne s'applique plus qu'en activant "Publier aussi sur le Mur d'Entraide"), traçabilité admin des réponses "Pas cette fois" sur un besoin de relais ponctuel (#295) ; rétablissement de la suppression en cascade du reste d'une checklist suggérée publiée quand d'autres items restent publiés et ouverts, en suppression individuelle et en masse, Mon Compte et Entraide (#296) ; choix de la destination de publication d'une checklist suggérée (Mur seul / Mes Checklists seul / les deux, copies liées et synchronisées), suppression d'un besoin par son propre auteur (pas seulement l'admin), première tentative de correctif du bouton "Retour" du wizard d'import (#297) ; correctifs post-test de #297 — synchronisation du statut "Fait"/prise en charge entre les deux copies liées Mur/Mes Checklists, popup de suppression liée enchaîné à celui de la checklist, icône de suppression épurée, "Retour" toujours cassé dans certains cas (#298) ; cause racine définitive du bug "Retour" — deux `<Modal>` React Native restaient simultanément `visible=true` (incompatible Android), fusion des 3 Modals du flux Entraide en un seul avec bascule de contenu interne, popups de suppression enchaînés plutôt que superposés, 4 irritants mineurs corrigés (#299) ; même bug "Retour" corrigé dans le flux d'import indépendant de Mon Compte (`MyChecklist.tsx`, qui a son propre picker/wizard distinct d'`Entraide.tsx`) (#300). Doc PR #301 : handoff + docx (V1.12→V1.13) + PRD (v1.11→v1.12) synchronisés jusqu'à #300.
- 2026-08-27 : chantier SOS relais en 3 volets planifié (popup planning + répartition de période + co-administration, cette dernière reportée) — Feature 1 : bouton "🗓️ Je regarde mon planning" dans `RelaisAlertModal` (ferme sans décider, l'alerte revient à la prochaine connexion), nouveau `lib/relaisAlerts.ts` centralisant la logique déjà dupliquée, nouveau bouton "🔔 Mes alertes" (Mon Compte admin et visiteur) regroupant RGPD + besoins de relais ouverts + historique de recasage, liste des personnes sollicitées visible dans le détail du popup (#303). Feature 2 : répartition d'un besoin de relais entre plusieurs preneurs sur des sous-périodes distinctes (nouvelle table `task_relais_coverage`, fusion d'intervalles réelle dans `lib/relaisCoverage.ts`), désinscription individuelle par sous-période, choix "🙋 Je m'en charge (ce qu'il reste)" ou "📅 Choisir une période" (#304, mergée dans une branche intermédiaire puis reproposée vers `main` avec le reste du cycle sous #305). Retours utilisateur après premier test device : texte du popup de remerciement spécifique à la catégorie relais (au lieu du texte générique invitant à cliquer sur "Fait"), nouveau bloc "🤝 Mes engagements de relais" dans Mon Compte, "🔔 Mes alertes" filtré aux entrées d'historique jamais vues (nouvelle colonne `reservation_change_history.seen`), choix de la période scindé en deux popups centrés successifs "Du"/"Au" où seuls les jours de la période demandée par l'admin sont sélectionnables et surlignés en orange, avec rappel persistant de cette période à chaque étape (#305). Doc PR #306 : handoff + docx (V1.13→V1.14) + PRD (v1.12→v1.13) synchronisés jusqu'à #305.
- 2026-08-27 : après un test sur le Dev Build du contenu de #305, trois retours utilisateur — photo de profil "Denis Baupin" manquante en Mon Compte (root cause : `localPhotoUri` pointe vers un fichier local perdu après un rebuild natif Dev Build, alors que la copie Supabase reste intacte), alertes SOS relais qui semblaient disparaître de "Mes alertes" (investigué, non conclusivement reproduit en lecture de code — logs d'erreur défensifs ajoutés comme filet de diagnostic plutôt qu'un correctif confirmé), et surtout les alertes de changement de créneau ("Mes alertes") qui disparaissaient silencieusement même jamais lues. Root cause du 3ᵉ point : `handleAlertsModalClose` marquait `seen=true` en masse sur tout `reservation_change_history` non vu à la simple fermeture du popup "Mes alertes" — régression volontaire introduite par le commit `5cc42f6` du cycle précédent, mais non désirée à l'usage. Deux PR : **#307** (branche `fix/relais-post-claim-nav-alertes-historique`) — le bouton "J'ai compris" du popup de remerciement relais ramène vers l'accueil au lieu de simplement fermer le popup, un besoin de relais déjà couvert (même partiellement) par l'identité connectée sort des alertes actives et bascule en Historique au lieu de re-solliciter une aide déjà donnée, migration `reservation_change_history_seen.sql` rendue idempotente, correctif CI (`eas-cli` 22.6.0 nécessite Node ≥22, workflow bump 20→22). **#308** (branche `fix/relais-alerts-mark-seen-photo-fallback`) — photo de profil Mon Compte (visiteur) : repli `onError` sur la copie Supabase si la photo locale ne charge plus, avec réparation de la session locale ; "Mes alertes" ne marque plus rien comme vu à la fermeture du popup, nouveau bouton "Marquer comme lu" explicite par entrée d'historique (`app/(admin)/account.tsx`, `app/(visitor)/account.tsx`, `components/MyAlertsModal.tsx`) ; logs d'erreur ajoutés aux requêtes `task_relais_coverage`/`tasks` de `lib/relaisAlerts.ts` pour diagnostiquer plus vite un futur échec silencieux similaire. Les deux PR mergées et confirmées fonctionnelles par l'utilisateur. Doc PR #309 : handoff + docx (V1.14→V1.15) + PRD (v1.13→v1.14) synchronisés jusqu'à #308.
- 2026-08-27 → 08-28 : sélection multiple étendue au visiteur (appui long sur un besoin qu'il a lui-même publié, même mécanique de suppression en masse que l'admin mais strictement limitée à ses propres besoins — `deleteOrSoftDeleteTasks` déjà correctement scindé "mine"/"others" par auteur, aucun changement backend requis) ; pictogramme Entraide de la barre d'onglets passé en rouge (intérieur) quand un besoin Urgent n'est pas pris en charge, plus une petite cloche rouge si quelqu'un d'autre a publié un besoin non encore vu — nouveaux `lib/entraideBadges.ts` (hook `useEntraideBadges` + `markEntraideSeen`, suivi "vu" par AsyncStorage keyé espace+identité, réutilise `relaisIdentityKey`/`resolveRelaisIdentity` de `lib/relaisAlerts.ts`) et `components/EntraideTabIcon.tsx`, restructuration de `app/(admin)/_layout.tsx` pour que `AdminGate` rende directement le `<Tabs>` (accès à `space.id`) (#311). Crash remonté juste après merge par l'utilisateur (capture d'écran Metro) : `Error: cannot add 'postgres_changes' callbacks ... after subscribe()` — root cause, react-navigation/bottom-tabs invoque `tabBarIcon` plusieurs fois en parallèle (passes de mesure/animation), deux montages concurrents de `EntraideTabIcon` réutilisaient le même nom de canal Supabase Realtime fixe ; correctif générique = suffixe aléatoire par montage dans le nom du canal (#312). Ajustement demandé après retour utilisateur : le pictogramme repasse à la couleur normale du thème (plus de remplissage rouge conditionnel), les deux signaux (urgent non pris en charge / nouveau besoin d'un autre auteur) deviennent deux points rouges indépendants empilés façon « : » au lieu d'une couleur + une cloche — `EntraideTabIcon.tsx` réécrit en conséquence (#313). Les trois PR mergées et confirmées par l'utilisateur. Doc PR #314 : handoff + docx (V1.15→V1.16) + PRD (v1.14→v1.15) synchronisés jusqu'à #313.
- 2026-08-27 : besoins Entraide ouverts triés Urgent en tête puis par proximité croissante (échéance/date transport/début de relais selon catégorie) ; un besoin "pris en charge" rejoint désormais les besoins fermés au lieu d'être compté ouvert, réparti en "À venir" (proximité croissante) et "Historique" (plus récent d'abord) ; cadre rouge autour de la carte dès que le tag Urgent est actif, quel que soit le statut ; tag Urgent coché automatiquement à la création si l'échéance tombe à J+2 ou moins ; correctif connexe de l'effet deep-link (`?focusTaskId=`) qui nettoyait les filtres ouvert/fermé sur l'ancienne sémantique de statut (#310, mergée mais jamais reprise dans un handoff avant ce cycle).
- 2026-08-28 : retours utilisateur sur les besoins Entraide après test des cycles #310-#313 — tag Urgent/cadre rouge masqués dès qu'un besoin n'est plus "ouvert" (au lieu de rester affichés indéfiniment) ; bouton "✓ C'est fait" (transport) masqué une fois la carte déjà affichée "Fait" ; nouveau helper `isTaskClosedPast(t)` (statut fermé + date passée) masquant pour l'auteur la corbeille, l'éligibilité à la sélection multiple, et "Se désinscrire" (relais/transport) sur un besoin fermé et passé — droits admin inchangés ; icône de suppression sortie de la ligne de badges (largeur variable) vers une ligne dédiée face au titre (position fixe) ; ligne "🗓️ Publié le …" ajoutée après la description ; nouveau helper `coursePartial(t)` ajoutant une 2ᵉ ligne "partiellement" sous le tag "Pris en charge" d'un besoin courses incomplet (#315).
- 2026-08-28 : deux fonctionnalités demandées dans le même message, livrées en deux PR indépendantes (une branche = une PR). **#316** (`fix/visites-tri-antechronologique`) : correctif du tri anté-chronologique de "Autres soins/visites réalisés" (passés) dans `components/SoinsPlanifiesBlock.tsx` — le tri ne dépendait que d'un `.order()` Supabase silencieusement court-circuité quand le prop `reservations` est fourni directement (cas de `HomeCalendarScreen.tsx`) ; ajout d'un tri défensif interne (`sortedSoins`) indépendant de la source. **#317** (`feat/courses-produits-recurrents`) : nouvelle table `recurring_shopping_items` (migration `20260828_recurring_shopping_items.sql`, dédoublonnée par index unique `(space_id, lower(label))`), catalogue "🔁 Produits récurrents" par espace alimenté automatiquement à chaque article saisi (nouvelle liste ou liste déjà publiée), sélection "Courses" ouvrant désormais une popup dédiée "🛒 Créer une liste de courses" (remplace le formulaire intégré), suppression multiple du catalogue réservée à l'admin (sans effet sur les listes déjà publiées) ; suppression multiple par appui long ajoutée aux listes déjà publiées, réservée à l'auteur du besoin ou à l'admin (`ShoppingListModal.tsx`, nouveaux props `spaceId`/`isAuthor`).
- 2026-08-28 : premier test sur device de #316/#317 → 4 irritants remontés, corrigés dans **#318** (`fix/visites-tri-ux-papercuts`) : (1) le prop `chronological` retiré à tort par #316 sur l'appel "Autres visites planifiées" de `HomeCalendarScreen.tsx` — ce bloc liste des visites *à venir*, qui doivent être triées du plus proche au plus éloigné (exactement ce que produit `chronological`), pas anté-chronologiquement comme les visites passées ; prop réintégré. (2) bouton "Valider" de la popup "Créer une liste de courses" illisible — `styles.btnPrimary` (`flex: 1.3`) est conçu pour un contexte `flexDirection: row` (pair avec `btnSecondary` dans `sheetBtns`), utilisé seul dans un conteneur en colonne il s'écrase à hauteur quasi nulle (`flexBasis: 0%` sans espace à occuper) ; remplacé par une taille explicite (`width: "100%", height: 48`). (3) les 2 pastilles rouges du pictogramme Entraide (`EntraideTabIcon.tsx`), plaquées en haut à droite de l'icône, recentrées verticalement à côté de l'icône (`top:0, bottom:0, justifyContent:"center"` au lieu de `top:-3`). (4) la croix de suppression rapide d'un article (`ShoppingListModal.tsx`) était rendue pour tout le monde dès que le mode sélection n'était pas actif, contournant le gate `canManageList` déjà correctement appliqué à la sélection multiple par appui long — gate étendu à la croix. Les cinq PR (#310, #315, #316, #317, #318) mergées et confirmées par l'utilisateur. Doc PR #319 : handoff + docx (V1.16→V1.17) + PRD (v1.15→v1.16) synchronisés jusqu'à #318.
- 2026-08-28 : correctif rapide sur le besoin Courses — le bouton "Je m'en occupe" disparaît désormais dès qu'une personne a coché au moins un article (même liste incomplète), pour éviter un double claim en pleine répartition collaborative ; le popup "🔁 Produits récurrents" surligne un article déjà présent dans la liste en cours au lieu d'un simple toast "Déjà dans la liste" (#320).
- 2026-08-28 → 08-29 : refonte complète de la création d'un besoin Entraide en assistant "Publier" — remplacement de l'ancien formulaire unique à défilement par une séquence de popups centrées chaînées, catégorie par catégorie (l'édition d'un besoin déjà publié garde l'ancien formulaire, inchangé), livrée en 4 PR indépendantes testables une à une : **#321** scaffolding (état machine `publishStep`, popup Catégorie, écran générique Repas/Affaires/Autre, écran commun "Autres options") ; **#322** correctif de dimensionnement des popups (`maxHeight` en pourcentage sur fond de `ScrollView` non bornée, ambigu sous Android surtout clavier ouvert — remplacé par un dimensionnement en pixels réels via `useWindowDimensions()`, pattern devenu obligatoire pour tous les écrans suivants du même assistant) ; **#323-326** Repas/Administratif, bouton "✓ Valider" (ex-"‹ Retour"), auteur affiché sur chaque besoin, accès à "Créer une checklist" depuis Administratif, correctif du double-mirroring d'un besoin réclamé vers "Mes Checklists" (root cause isolée par question à l'utilisateur : c'était `performClaim()`, pas le switch de publication, qui mirrorait à tort) puis retour partiel sur le passage forcé par "Besoin Administratif" pour la checklist suggérée (inutile, elle a déjà son propre assistant séquentiel échéance/urgence/précision) ; **#327** Courses passe dans l'assistant, sélection multiple des Produits récurrents avec confirmation explicite ("Ajouter à la liste") au lieu du tap-to-toggle immédiat ; **#328** Transport en 4 écrans (Départ/Arrivée → type de trajet → calendrier → horaires), "Autres options" réduites pour cette catégorie (pas d'échéance ni de prise en charge immédiate, déjà gérées ailleurs dans le formulaire Transport). Toutes les 8 PR (#320-328) mergées et confirmées par l'utilisateur.
- 2026-08-29 → 08-30 : trois PR de suivi sur le besoin Transport après test de l'assistant "Publier" et du système de propositions existant. **#329** (`feat/transport-maps-link`) : champ "🗺️ Lien Google Maps" bidirectionnel sur l'étape Départ/Arrivée (lien collé → adresse/CP/ville/pays via `resolvePlaceFromMapsUrl`, adresse tapée → lien généré via `googleMapsSearchUrl`/`joinAddress`), nouvelle colonne `tasks.transport_home_maps_url`. **#330** (`feat/transport-propositions-beneficiaire`) : la personne pour qui le besoin a été publié (`isForPerson(t)`, identité par prénom/nom de session) obtient les mêmes droits que l'auteur sur les propositions ; en cours de route, correctif d'un bug préexistant dans `lib/address.ts` (`parseAddressFromSegments` traitait à tort le premier segment d'un lien Maps comme un nom d'établissement même quand aucun nom ne précède le code postal/ville, coupant la rue) ; long-press sur une proposition ouvre un menu inline Accepter/Répondre (`TransportProposal` gagne `response`/`response_seen`), réponse visible sur la proposition et alerte au proposant au chargement de l'onglet Entraide. **#331** (`feat/transport-propose-layout-visibility`) : correctif de chevauchement visuel du bloc aller-retour avec le calendrier au-dessus (restructuré en deux colonnes) ; les propositions reçues deviennent visibles par tout visiteur consultant le besoin, les boutons de validation/réponse/"Aucune ne convient" restant réservés aux personnes habilitées (nouveau bouton "🕐 Proposer un horaire" pour les autres, directement dans la même liste). Les trois PR mergées et confirmées par l'utilisateur.
- 2026-08-30 : cinq retours utilisateur groupés sur les droits de validation Transport après test de #329-331, livrés dans une seule PR (**#332**) : droits de validation restreints à l'auteur, à la personne pour qui le besoin a été publié, ou à l'admin lorsqu'il en est lui-même l'auteur (`canManageTransport` renommé `canValidateTransport`, même contournement `author_pin === "ADMIN"` que `isMine` puisque `mySession` est toujours nul pour l'admin) ; l'auteur ne peut plus proposer un horaire sur son propre besoin ; "Aucune ne convient" protégé par une confirmation inline (pas de `<Modal>` imbriqué, contrainte Android déjà documentée) ; `TransportProposal` gagne `declined: boolean` — décliner ne vide plus le tableau mais marque chaque entrée, qui reste visible avec un tag "❌ Déclinée" (validation/réponse/long-press masqués sur les entrées déclinées) ; correctif de fond du lien Google Maps du domicile — `saveTask()` recalcule désormais `transport_home_maps_url` à l'enregistrement dès que le champ est vide ou correspond encore à un lien généré par recherche (root cause : `regenerateTransportMapsUrl()` ne se déclenchait qu'au `onBlur`, laissant un lien généré incomplet si l'utilisateur tapait "Suivant" sans quitter le champ) ; nouveau helper `taskEffectiveTime(t)` départageant à horaire égal les besoins Transport d'une même date dans les deux tris chronologiques existants ; nouveau `transportTimeAlreadyPast` (comparaison en heure locale via `toISO(new Date())`, pas `todayIso()` qui est en UTC) bloquant la création d'un besoin Transport sur un horaire d'aller déjà passé le jour même ; largeur des boutons "Annuler"/"Suivant" harmonisée dans les popups Transport (`flex: 1` scoping local, styles globaux `btnPrimary`/`btnSecondary` non touchés). PR mergée et confirmée par l'utilisateur, aucune migration Supabase nécessaire (le nouveau champ `declined` vit dans la colonne `jsonb` déjà existante `transport_proposals`). Doc PR #333 : rattrapage du gap documentaire #320-331 (jamais repris depuis le handoff #319) + #332 — handoff + docx (V1.17→V1.18) + PRD (v1.16→v1.17) synchronisés jusqu'à #332.
- 2026-08-30 : neuf retours utilisateur groupés après test de l'assistant "Publier" et du système de propositions Transport, livrés en 7 PR indépendantes. **#334** (`fix/entraide-transport-maps-moderation-entraide-ux`) : lien Google Maps du besoin Transport plus généré tant que rue/CP/ville ne sont pas tous saisis ; créer/modifier un besoin scrolle et surligne désormais ce besoin précis ; l'admin ne peut plus éditer un besoin/une nouvelle qu'il n'a pas publié lui-même (suppression toujours possible via modération douce, Soutien.tsx déjà correct) ; Mon Compte/Entraide dédoublonné (besoins publiés + auto-pris-en-charge fusionnés) ; boutons Du/Au/Créer mes réservations recentrés ; Mon Compte/Mes réservations (visiteur) rouvre précisément la réservation cliquée, deep-link `focusTaskId` ajouté côté admin. **#335** (`fix/entraide-deeplink-highlight-reservation-close`) : correctif racine du bug empêchant un 2ᵉ clic (ou clic sur un autre élément) depuis Mon Compte de rescroller/resurligner (écrans montés en permanence dans les Tabs) — sélection automatique de la catégorie ciblée + cadre rouge d'atterrissage qui s'estompe en 1s (reste fin si Urgent), même correctif sur Nouvelles/Soutien ; date de publication + échéance affichées sous chaque besoin Mon Compte ; "Fermer" de la popup réservation ramène vers Mon Compte/Mes réservations à la position précédente si ouverte depuis là. **#336** (`fix/entraide-lag-transport-horaire-popup-pin`) : correctif d'un décalage d'une position sur les liens profonds Entraide lors de clics successifs (deux effets React se déclenchant dans le même commit) ; horaire Transport demandé (aller-retour) affiché dans Mon Compte ; popup réservation (Mon Compte, visiteur déjà connecté) saute directement à modifier/annuler/ajouter au calendrier sans redemander le PIN. **#337** (`fix/prochaine-dispo-alerte-suppression-filtre-transport`) : correctif racine de "Prochaine disponibilité" (ignorait le flag `INTERVENANT_ROLE_ENABLED`, d'anciennes réservations "Intervention" bloquant à tort des créneaux libres) ; alerte connexion + bandeau rouge Mon Compte/Entraide/Nouvelles/Soutien quand l'admin supprime en douceur une publication visiteur ; filtre "Mes besoins" sur le mur Entraide ; horaire Transport séparé en 3 lignes Demandé/heure/Retour ; migration manuelle `20260830_deleted_content_seen.sql` (colonne `deleted_seen`, statut non confirmé). **#338** (`fix/entraide-filtre-admin-popup-resa-planifie-historique`) : filtre "Mes besoins" ne matchait jamais côté admin (bascule sur la convention `"ADMIN"` déjà utilisée par `canValidateTransport`) ; popup réservation Mon Compte (admin) ouvre directement `AdminEditReservation` via nouveau `pendingEditReservationId` sur `SpaceContext` ; vues "À venir/Planifié" (toujours visible, tri chronologique) + "Historique" (repliée par défaut, dépliable) pour Mes réservations et Mon Compte/Entraide, admin et visiteur. **#339** (`fix/mon-compte-popups-tri-liens-profonds`) : "Publié par XXX" sur chaque besoin ; "À venir" renommé "Planifié" ; modale admin Mon Compte alignée sur le flux visiteur (détail → Modifier/Annuler), "Fermer" revient à Mon Compte ; réservations passées verrouillées (non modifiables/annulables/supprimables même par l'admin, tap renvoie au jour) ; correctif deep-link Mon Compte → Mes nouvelles (admin) ; tri Entraide par date de transport/échéance selon catégorie, Historique antichronologique avec échéance en critère secondaire ; correctif deep-link Soutien/Mes nouvelles (atterrissait toujours en tête de liste). **#340** (`fix/entraide-compteurs-cloture-auto-fermer-popup`) : badges Ouvert/Fermé scopés au filtre "Mes besoins"/catégorie actif (au lieu du total global) ; un besoin pris en charge à échéance dépassée se ferme automatiquement même sans "Fait" ; "↩ Réouvrir" masqué une fois l'échéance définitivement dépassée ; bouton "Fermer" du popup réservation (Mon Compte admin) navigue explicitement vers l'onglet Compte. Les sept PR mergées et confirmées par l'utilisateur.
- 2026-08-30 : deux PR sur la cascade accompagnant et le non-lu des murs, faisant suite à un test des réservations groupées. **#341** (`fix/reservation-accompagnant-cascade-avec-x`) : le popup de modification visiteur (BookingFlow) ne mettait à jour que sa propre réservation, jamais celle de l'accompagnant lié par `group_id` (divergence silencieuse Mon Compte/Calendrier) ; ajout d'une case à cocher par accompagnant ("Modifier aussi le créneau de X", même principe qu'`AdminEditReservation.tsx`) ; partout où "Avec X"/un regroupement `group_id` est affiché (HomeCalendarScreen, Mes réservations, Mes contributions admin), vérification que les deux réservations partagent toujours la même date/créneau — un accompagnant non coché redevient une entrée indépendante. **#342** (`feat/alerte-accompagnant-et-non-lus-murs`) : l'accompagnant dont le créneau est déplacé en cascade reçoit une alerte (connexion + Mes alertes) précisant qui a fait le changement et le nouveau jour/horaire, avec confirmation qu'il reste avec cette personne ; nouveau mécanisme partagé `lib/wallUnread.ts` — fond pastel orange sur les publications non lues des murs Entraide/Soutien/Nouvelles, effacé uniquement au défilement dans la zone visible (pas à l'ouverture de l'onglet), bootstrap sur la 1ère connexion pour ne pas faire ressurgir l'historique existant ; point rouge ajouté sur les pictogrammes Soutien/Nouvelles (même convention qu'Entraide), disparaissant en direct à la lecture ; `lib/entraideBadges.ts` refactorisé pour réutiliser ce mécanisme partagé (suppression de l'ancien `markEntraideSeen` à horodatage unique). Les deux PR mergées et confirmées par l'utilisateur. Doc PR #343 : rattrapage du gap documentaire #334-340 (jamais repris dans un handoff) + #341-342 — handoff + docx (V1.18→V1.19) + PRD (v1.17→v1.18) synchronisés jusqu'à #342.
- 2026-08-30 : correctif remonté après test — les points rouges des onglets Nouvelles/Soutien n'apparaissaient pas à la connexion et la navigation dans ces onglets ne montrait aucun fond non-lu, malgré le mécanisme partagé livré en #342. Root cause : les murs étant triés du plus récent au plus ancien, l'élément non lu le plus probable atterrit tout en haut — pile la zone déjà visible sans le moindre scroll à l'ouverture de l'écran — et `useWallVisibility`/le `onViewableItemsChanged` de `NewsFeed` le marquaient "vu" dans le même cycle de rendu que son premier affichage, avant toute perception possible par l'utilisateur (**#344**, branche `fix/murs-non-lu-cadre-orange`) : un délai d'environ 1,2s (`setTimeout` débounced, `scheduleCheck`) est désormais laissé avant tout marquage automatique déclenché par la seule présence à l'écran (`onScrollViewLayout`/`registerItemLayout`/effet sur `unreadIds` dans `lib/wallUnread.ts`, `minimumViewTime: 1200` sur le `viewabilityConfig` de `NewsFeed.tsx`) — un vrai scroll utilisateur (`onScroll`) reste marqué immédiatement, preuve d'une attention réelle. Au passage, changement de design demandé par l'utilisateur : le fond pastel orange (`UNREAD_WALL_FILL`, supprimé de `lib/themes.ts`) est remplacé par un cadre orange (`borderColor: C.orange, borderWidth: 2`) sur chaque publication non lue dans `Entraide.tsx`/`Soutien.tsx`/`NewsFeed.tsx` ; sur un besoin Urgent déjà cerné de rouge, un second cadre orange (`unreadInnerRing`, `View` absolue à l'intérieur du cadre existant) se superpose à l'intérieur plutôt que de remplacer le rouge. PR mergée et confirmée par l'utilisateur, aucune migration Supabase nécessaire (mécanisme 100% client). Doc PR #345 : handoff + docx (V1.19→V1.20) + PRD (v1.18→v1.19) synchronisés jusqu'à #344.
- 2026-08-31 : retour utilisateur sur le design du cadre orange ("ne me plaît pas") → remplacé par un indicateur "New" : barre latérale bleue (`NEW_ACCENT = "#2E75B6"`, `lib/themes.ts`) + badge "New" (`components/NewIndicator.tsx`) sur Entraide/Soutien/Nouvelles, basé sur `sessionLoginTimestamp` (constante de module fixée une seule fois au chargement du bundle JS, aucune persistance) ; sur un besoin Urgent, seul le badge reste affiché sans barre, pour ne pas se superposer au cadre rouge existant ; `lib/entraideBadges.ts` corrigé d'un import cassé par le renommage (**#346** cadre orange fixé session, **#348** remplacement par "New"). Retours post-merge : badge "New" signalé invisible et une publication Nouvelles d'un visiteur invisible pour un autre visiteur (visible admin only) — root cause commune, les trois murs ne se rechargeaient que via un canal Supabase Realtime souscrit une seule fois au montage, susceptible de se couper silencieusement en arrière-plan (comportement connu React Native/mobile) ; filet `AppState.addEventListener("change", ...)` ajouté dans les 3 fichiers pour recharger systématiquement au retour au premier plan, indépendamment de l'état du canal temps réel — pour le badge "New" spécifiquement, aucun bug trouvé, rappelé qu'il faut un vrai redémarrage complet de l'app (réinitialise `sessionLoginTimestamp`) et un test avec une publication faite par un autre compte (exclusion auteur soi-même) (**#349**). Quatre demandes groupées ensuite, deux résolues sans code après clarification utilisateur (le "Modifier" par PIN partagé sur une réservation d'autrui et le tap sur le créneau d'un autre visiteur au planning mensuel sont des comportements déjà voulus/existants, pas des bugs — confirmé explicitement par l'utilisateur pour le premier point) : dans Entraide, les besoins encore "New" remontent groupés en tête de la section Ouverts, chaque groupe gardant son tri Urgent/chronologique habituel (`compareOpenSectionWithNew`) ; appui long sur une photo du fil Nouvelles pour la télécharger (signature de `logDownloadIfNotMine` généralisée comme sur Soutien.tsx) ; popup PIN (Modifier/Annuler une réservation, `BookingFlow.tsx`) passé de 88% à 100% de largeur pour matcher la convention `ConfirmModal` (**#350**). Toutes les PR (#346, #348, #349, #350) mergées et confirmées par l'utilisateur.
- 2026-08-31 → 09-01 : cinq PR de suivi (#352-356), sans nouveau retour utilisateur groupé — corrections ponctuelles enchaînées au fil des demandes. **#352** (`fix/nav-fiche-visiteur-popup-visite-entraide`) : atterrissage précis (`focusDate`) sur le bon jour depuis la fiche visiteur étendu à Nuits (déjà correct sur Créneaux) ; taper le créneau d'un autre visiteur en planning mensuel/hebdo ouvre désormais un popup de confirmation dédié (`VisiteAutreVisiteurPopup.tsx`, « Réserver cette place »/« Voir la journée ») au lieu de réserver ou de naviguer silencieusement, visiteur uniquement ; le bouton « Je m'en occupe » d'un besoin courses avec au moins un article coché a été confirmé déjà correct (pas de second claim possible) ; le badge « … partiellement » d'un besoin courses ne s'affiche plus qu'avant toute prise en charge formelle, pour ne plus contredire le badge « Pris en charge ». **#353** (`fix/liseret-new-nouvelles-admin-courses-partielles`) : liseret latéral bleu de l'indicateur « New » recadré dans un conteneur au même rayon que la carte (`overflow:hidden`), pour épouser le coin au lieu de produire une pointe visible ; nouvelles publiées par l'admin rendues toujours visibles des visiteurs — l'ancien gate `news_intervenant_mode` (hérité du rôle Intervenant, masqué en V1 depuis #291) les laissait invisibles par défaut sans bascule fonctionnelle pour corriger cela, bandeau associé supprimé ; réponses postées par l'identité connectée sur les nouvelles d'autrui (pas seulement ses propres publications) désormais remontées dans Mon Compte/Mes nouvelles ; contribution « courses » sans claim explicite (article coché sans cliquer « Je m'en occupe ») désormais visible dans Mon Compte/Entraide, jusque-là invisible partout. **#354** (`fix/dismissto-creneau-passe-wallunread-pris-en-charge`) : première tentative de correctif du bouton « Fermer » de la popup de détail réservation admin, qui ne revenait pas à Mon Compte — passage de `router.push` à `router.dismissTo` (s'avérera encore insuffisant, voir #355) ; nouveau helper `isReservationFullyPast` (heure-aware pour Visite/Intervention, date-only pour Nuit) basculant immédiatement une réservation du jour même déjà passée de « Planifié » vers « Historique » sans attendre minuit ; `lib/wallUnread.ts` réécrit autour d'un seul mécanisme persistant (`useWallReadTracking`) alimentant à la fois le point rouge des onglets et les badges « New », corrigeant trois incohérences (point rouge sans badge visible, badge manquant sur besoin fermé/pris en charge jamais vu, badge absent à la création pour le publieur lui-même) ; nouvelle ligne « 📌 Pris en charge le XX/XX/XXXX » sous « 🗓️ Publié le » dans Mon Compte, pour un claim classique ou une contribution courses sans claim explicite, nouvelles colonnes `tasks.claimed_at`/`shopping_list_items.bought_at` (migration manuelle `20260901_claimed_bought_at.sql`). **#355** (`fix/urgent-j3-groupement-creneaux-focus-slot`) : correctif définitif du bouton « Fermer » — `router.dismissTo` (#354) ne traversait toujours pas la frontière Tabs/Stack, remplacé par `router.navigate` ; nouveau `taskDueSoon(t)` déclenchant automatiquement le tag Urgent (cadre rouge) pour tout besoin ouvert dont l'échéance tombe à J+3 ou moins ; réservations d'un même jour et créneau regroupées en un seul bloc visuel dans le Planning mensuel/hebdo (`SoinsPeriodBlock.tsx`, mêmes colonnes que `PlanningDuJourBlock.tsx`) ; popup de sa propre réservation (visiteur) enrichi de « 📅 Ajouter à mon calendrier » et « 🗑️ Annuler cette visite », déjà présents côté admin ; taper une réservation depuis la fiche d'un visiteur scrolle et surligne désormais directement le créneau horaire concerné (`focusCreneau`, 2,5s), pas seulement le bon jour. Confirmé déjà correct sans changement de code : le bouton « J'ai compris » d'Entraide re-scrollait déjà vers le besoin réclamé via le mécanisme `focusTarget`/`thanksModalTaskId` existant. **#356** (`fix/urgent-j3-persiste-apres-echeance`) : retour utilisateur — l'Urgent automatique à J+3 doit rester actif même après l'échéance elle-même dépassée, tant que le besoin n'est pas pris en charge (pas seulement dans la fenêtre `[maintenant, J+3]`) ; borne basse retirée de `taskDueSoon`, en pratique limité à une fenêtre courte par la fermeture automatique déjà en place (`closeOverdue`, ~60s après l'échéance). Les cinq PR mergées et confirmées par l'utilisateur. Doc PR #357 : handoff + docx (V1.21→V1.22) + PRD (v1.20→v1.21) synchronisés jusqu'à #356.

## 1. Objectif de la session
Rattraper la documentation (handoff + docx + PRD) sur le cycle #352-356, resté en gap depuis le handoff #351 (qui couvrait jusqu'à #350) : (1) correctif définitif du bouton "Fermer" de la popup de réservation admin, deux tentatives successives (`router.dismissTo` en #354, insuffisant, puis `router.navigate` en #355) ; (2) Urgent automatique à J+3 sur un besoin ouvert, rendu persistant même après échéance dépassée suite à une clarification utilisateur (#355, #356) ; (3) regroupement visuel des réservations par créneau, actions calendrier/annulation sur sa propre réservation visiteur, et scroll ciblé sur le créneau exact depuis la fiche d'un visiteur (#355) ; (4) popup de confirmation sur le créneau d'un autre visiteur, badge courses partiel, "J'ai compris" qui ramène au besoin (#352) ; (5) nouvelles admin enfin visibles des visiteurs, réécriture unifiée des badges "New"/point rouge, date "Pris en charge le", réponses et contributions courses remontées dans Mon Compte, réservation du jour même déjà passée basculée en Historique sans attendre minuit (#353, #354). Aucun développement de code dans cette session : uniquement la mise à jour des 3 fichiers de documentation.

État "done" : atteint pour le code — #352 à #356 mergées et confirmées par l'utilisateur (voir Historique cumulé). La partie handoff (ce fichier + le docx + le PRD) est finalisée dans cette session, reste à committer/pousser/ouvrir la PR docs.

## 2. État actuel
- `Documentation/Documentation Fonctionnalités.docx` : passée de V1.21 à V1.22 dans cette session — §4 transverse (table 6, lignes "Calendrier Accueil unifié" et "Indicateur « New »") complétée du regroupement par créneau (#355) et de la réécriture `wallUnread`/badges "New" + liseret clip (#353-354) ; §5.7 Nouvelles (table 13, ligne "Visibilité intervenants") complétée de la levée du gate hérité de l'ex-rôle Intervenant (#353) ; §5.9 Entraide (table 15, lignes "Cycle de vie d'un besoin" et "Liste de courses") complétée de l'Urgent auto J+3 persistant (#355-356), du "J'ai compris" qui ramène au besoin (#352) et du badge "… partiellement" qui disparaît après claim (#352) ; §5.11 Compte admin (table 17, ligne "Mes contributions") et §6.4 Compte visiteur (table 26, ligne "Mes réservations…") complétées du correctif `router.navigate`, de `isReservationFullyPast`, de la date "Pris en charge le" et des réponses/contributions courses remontées (#353-354) ; §6.2 (table 24, lignes "Modifier ce créneau" et "Réservation rapide par tap") complétée du popup de confirmation sur créneau d'autrui (#352), des boutons calendrier/annulation et du scroll ciblé (#355) ; en-tête métadonnées (table 0) mis à jour.
- `PRD_AvecToi_v1_4.md` : passé de v1.20 à v1.21 dans cette session — nouveau bloc changelog v1.20→v1.21 (6 entrées : Urgent auto J+3 persistant, correctif définitif "Fermer", regroupement par créneau, actions/scroll réservation visiteur, popup confirmation/badge courses/"J'ai compris" #352, nouvelles admin/badges New unifiés/date prise en charge #353-354).
- Relecture obligatoire effectuée avant de considérer chaque édition du docx terminée : re-dump ciblé des 10 cellules modifiées (table 0 métadonnées ; table 6 lignes 13/14 ; table 13 ligne 5 ; table 15 lignes 4/6 ; table 17 ligne 4 ; table 24 lignes 9/11 ; table 26 ligne 3) après le script d'édition, via fichier intermédiaire UTF-8 (jamais imprimé directement en console — cp1252 plante sur les accents) — toutes les insertions (appends sur le dernier run de chaque cellule, cellules à paragraphe unique) et le bump de version sont passés sans troncature ni corruption, aucune erreur de première passe.

**Dernière action avant ce handoff :** rédaction de ce fichier lui-même (`Handoff/handoff.md`) — Historique cumulé et sections 1-5 mis à jour après le docx et le PRD.

## 3. Fichiers concernés
Code déjà mergé avant cette session (documentaire) — rappel des fichiers touchés par #352-356 :
- `app/(visitor)/home/nights.tsx`, `app/(visitor)/home/slots.tsx`, `app/(admin)/home/slots.tsx` → `focusDate` étendu à Nuits côté visiteur (#352) ; `focusCreneau` end-to-end (param, `scrollRef`, `highlightCreneau`, `handleFocusSlotLayout`) (#355).
- `components/VisitorProfileModal.tsx` → `goToReservation` passe `focusCreneau` (Visite uniquement) en plus de `focusDate` (#355).
- `components/VisitorSlotsList.tsx`, `components/AdminSlotsList.tsx` → props `focusCreneau`/`onFocusSlotLayout`, styling `isFocused` (#355).
- `components/VisiteAutreVisiteurPopup.tsx` → nouveau composant, popup de confirmation sur le créneau d'un autre visiteur (#352).
- `components/Entraide.tsx` → `taskDueSoon(t)` (Urgent auto J+3, borne basse retirée en #356) ; badge courses "… partiellement" limité à avant claim ; "J'ai compris" (déjà correct, confirmé sans changement) (#352, #355, #356).
- `components/SoinsPeriodBlock.tsx` → `groupByCreneau(rows)`, regroupement visuel par jour+créneau (#355).
- `components/SoinActionModal.tsx`, `components/HomeCalendarScreen.tsx`, `components/AdminReservationDetail.tsx` → boutons "Ajouter à mon calendrier"/"Annuler cette visite" sur le popup de sa propre réservation (visiteur) ; `router.navigate` remplace `router.dismissTo` pour "Fermer" (#354→#355).
- `components/NewIndicator.tsx` → liseret dans un conteneur `overflow:hidden` au rayon de la carte (#353).
- `components/NewsFeed.tsx` → gate `news_intervenant_mode` retiré (admin toujours visible visiteurs), bandeau supprimé (#353).
- `app/(admin)/account.tsx`, `app/(visitor)/account.tsx` → réponses aux nouvelles d'autrui + contributions courses sans claim remontées (#353) ; date "Pris en charge le" (#354).
- `lib/wallUnread.ts` → réécrit autour de `useWallReadTracking`, mécanisme unique point rouge + badges "New" (#354).
- `lib/slotUtils.ts`, `lib/types.ts` → `isReservationFullyPast` (#354).
- `supabase/migrations/20260901_claimed_bought_at.sql` → `tasks.claimed_at`, `shopping_list_items.bought_at` (#354, statut d'application non confirmé — voir rappels ci-dessous).
- `Handoff/handoff.md` → ce fichier.
- `Documentation/Documentation Fonctionnalités.docx` → V1.21 → V1.22, mis à jour dans cette session.
- `PRD_AvecToi_v1_4.md` → v1.20 → v1.21, mis à jour dans cette session.

## 4. Ce qui a échoué
Rien de bloquant côté documentaire (édition du docx vérifiée sans corruption au premier passage). Côté code, une itération a échoué avant d'aboutir : #354 (`router.dismissTo`) n'a pas résolu le bug "Fermer" malgré une correction apparemment ciblée — cause racine identifiée seulement en #355 (`dismissTo` ne traverse pas la frontière Tabs/Stack, contrairement à `router.navigate`) ; gardé tel quel dans l'Historique cumulé plutôt que réécrit comme si #355 avait été la première tentative, pour que ce genre d'écueil expo-router reste visible pour la suite.

## 5. Prochaine étape
Committer `Handoff/handoff.md`, `Documentation/Documentation Fonctionnalités.docx` et `PRD_AvecToi_v1_4.md` sur une branche `docs/` neuve (depuis `origin/main`, à jour avec #356 mergée), pousser, ouvrir la PR correspondante.

**⚠️ Rappels à transmettre à l'utilisateur :** (1) le rappel de #290 sur `supabase/migrations/20260821_visitor_profiles_relation.sql` reste valable, statut toujours non confirmé — inchangé par ce cycle ; (2) reconfirmer que `20260827_task_relais_coverage.sql` a bien été appliquée (rappel #309 toujours valable) ; (3) le symptôme (b) du cycle #307-308 — alertes SOS relais qui semblaient disparaître — reste seulement instrumenté (logs d'erreur), pas conclusivement corrigé ; (4) Feature 3 (co-administration) du plan SOS relais reste en attente, non redémarrée ce cycle ; (5) `supabase/migrations/20260830_deleted_content_seen.sql` (#337) — statut d'application toujours non confirmé, inchangé par ce cycle ; (6) **nouvelle migration à appliquer : `supabase/migrations/20260901_claimed_bought_at.sql`** (#354, 2 `ADD COLUMN IF NOT EXISTS` non destructifs) — statut d'application non confirmé ; (7) une fois le cleanup post-#328 souhaité (code mort `!editTask &&` dans `taskForm`), il devra être explicitement redemandé (voir mémoire du projet) — inchangé par ce cycle ; (8) le comportement "Modifier" visible par PIN partagé sur une réservation faite pour quelqu'un d'autre a été explicitement confirmé voulu par l'utilisateur (cycle #350) — ne pas le retraiter comme un bug sans nouvelle demande explicite ; (9) le badge "New" ne se réinitialise qu'à un vrai redémarrage complet de l'app (pas seulement l'update OTA) — inchangé par ce cycle ; (10) **nouveau** : l'Urgent automatique à J+3 persiste après échéance dépassée tant qu'un besoin n'est pas pris en charge, mais est en pratique borné à une fenêtre d'environ 60 secondes par la fermeture automatique déjà en place (`closeOverdue`) — si l'utilisateur signale un jour que "ça ne reste pas Urgent assez longtemps", c'est `closeOverdue` (pas `taskDueSoon`) qu'il faudra revoir, sur demande explicite seulement.

Points en suspens à reproposer sur demande : nettoyage du code mort post-assistant "Publier" dans `taskForm` (voir rappel 7 ci-dessus), nettoyage de la branche distante `feat/entraide-creer-checklist` (commit orphelin `e9472c9`), reconstitution PR-par-PR des cycles #153-224 et #253-259 si ce niveau de détail devient utile, `PRD_ClaudeCode_Site_avectoi_care_v3.md`/`AUDIT_RLS_TAILLE_CODE_MORT.md`/`eslint.config.js` non suivis à committer ou nettoyer, unification éventuelle des trois implémentations de désengagement/désinscription (voir "Connu, non bloquant").

Items reportés (inchangés depuis les cycles précédents, ordre suggéré, sur demande uniquement) : rebase de `fix/souvenirs-rls-and-news-delete`, décision `docs/spec-web-upgrade`, déploiement `avectoi-site` sur Infomaniak (bloqué sans les accès de l'utilisateur), isolation Supabase, taille de l'app.
