# Handoff — AvecToi
_Généré le : 2026-09-13 (cycle #435-438, clos, rattrapage documentaire)_

## État global du projet

**Stack :** React Native + Expo SDK 51+, Expo Router, Supabase (BDD/Auth/Storage/Realtime/Edge Functions), EAS Build + EAS Update (channels development/preview/production, `expo-updates`, publication OTA automatique sur push `main` via `.github/workflows/eas-update-preview.yml`). expo-notifications, expo-calendar, expo-image-picker, expo-sharing + react-native-share (partage groupé). Resend (transactionnel + SMTP Supabase Auth, sous-domaine `notifications.avectoi.care`) branché sur les Edge Functions. Stripe côté web uniquement. Supabase CLI bloqué sur cette machine (App Control Policy Windows) : déploiement des migrations/Edge Functions exclusivement via le Dashboard Supabase, jamais auto-appliqué par Claude.

**⚠️ Règle Git STRICTE (CLAUDE.md) :** chaque modification, même un correctif ponctuel, part sur une branche neuve (depuis `origin/main` à jour) et se termine par sa propre PR poussée sur GitHub — jamais de nouveau commit ajouté à une branche dont la PR est déjà mergée. **Autorisation permanente : commit + push + PR dès qu'une tâche de code est terminée et vérifiée, sans attendre que l'utilisateur le redemande.** **L'utilisateur merge lui-même, puis teste** (le merge déclenche la publication OTA — sauf nouvelle dépendance native, qui nécessite un `eas build` + réinstallation complète). Toujours vérifier l'état réel d'une PR via `gh pr view <n> --json state,url` plutôt que de supposer.

**⚠️ Convention UI (CLAUDE.md, "Conventions UI") :** toute nouvelle confirmation utilise `ConfirmModal` (jamais `Alert.alert` natif) ; tout nouveau bouton reprend les couleurs/emoji/style déjà en usage à l'écran ; toute notification éphémère réutilise le pattern `showToast` existant.

**Repo GitHub :** `https://github.com/EI-HCS-Consulting/AvecToi`, branche `main` protégée par ruleset (PR obligatoire).

**PR ouverte, en attente de test/merge :** aucune. #435, #436, #437, #438 toutes mergées, confirmé par l'utilisateur (« mergé »/« mergé. Génère un handoff »). Aucune migration SQL dans ce cycle. Ce cycle-ci (documentaire) livre uniquement docx + PRD + ce fichier.

**Livré — snapshot par domaine (V1 + V2 archivé) :**
- **Socle V1** (points 1-10 CLAUDE.md) : auth admin, accès visiteur par lien/QR/code dossier, calendrier + créneaux + réservation + PIN, Souvenirs, Nouvelles, Entraide + Soutien, 6 thèmes, "Prochaine disponibilité", ajout calendrier natif Android, notifications + emails Resend.
- **RGPD & Freemium/Premium** : purge différenciée par plan (Freemium 30j non prolongeable / Premium 60j + 30j renouvelable) + gate J-7 ; cap Freemium en essai de 7 jours glissants depuis la 1ère Visite (`lib/freemiumCap.ts`, scan quotidien pg_cron `check-freemium-expiry-daily`) ; prix Premium 7,99 € (paiement unique, Stripe côté web uniquement) ; 5 fonctionnalités Premium (rôle Intervenant, vue Hebdo, besoin 🆘 SOS Relais, Documents types, Chronologie/livret + prolongation RGPD) réparties entre masquage complet et popup `ConfirmModal` selon l'écran (le gate Intervenant reste sur l'ancien style `Alert` bloquante, non harmonisé — voir rappels).
- **Reconnaissance visiteur cross-device** : identité vérifiée côté serveur par prénom + nom + PIN (table `visitor_profiles`, 4 RPC `SECURITY DEFINER`), reset PIN par l'admin (fiche visiteur) ou en self-service visiteur (table `pin_reset_requests`), historique symétrique dans « 🔔 Mes alertes ».
- **Co-administration temporaire** : un admin désigne un visiteur ayant proposé du relais comme co-admin (accès Admin complet hors facturation/RGPD/gestion des autres co-admins), acceptation par email + code à 6 chiffres, tableau de bord `app/(admin)/coadmins.tsx` en 2 sections « 🆘 Besoins SOS » / « 🛡️ Co-Administrateurs », barre de progression relais partagée (`components/RelaisDayProgress.tsx`) réutilisée à 3 emplacements, accès Paramètres via onglet caché monté dans les `Tabs` du visiteur.
- **Entraide** : assistant « Publier » en popups chaînées par catégorie, besoin de relais ponctuel avec répartition par sous-périodes, Transport (lien Maps, propositions, droits de validation), Courses (produits récurrents, cochage attribué/verrouillé, multi-contributeurs), tri Urgent/proximité + regroupement fermés, badges « New », sélection multiple, checklists suggérées avec choix de destination de publication. **Cycle #416-417** : bibliothèque passée à **15 modèles** — la carte « 🏠 Soin à domicile » ouvre désormais un sous-menu de 5 situations (🧹 ménage, 🍽️ portage de repas, 📞 télésurveillance, 🛏️ matériel médical, 📋 mise en place générale) au lieu de publier directement son contenu d'origine, avec la **même présentation côté Mon Compte → Mes Checklists** (`components/MyChecklist.tsx` réplique désormais le pattern sous-menu de `components/Entraide.tsx`) ; certains items (ex. formulaire national d'aides à l'autonomie) proposent plusieurs liens officiels au lieu d'un seul (`lienExterne` accepte objet unique ou tableau, normalisé par `checklistItemLinks()`, introduit par **PR #418**, pas #417 malgré l'attribution du cycle précédent — voir §1). **Cycle #418** : le mur d'Entraide affichait encore un seul lien par item (boucle manquante côté `Entraide.tsx`) alors que Mon Compte → Mes Checklists en affichait déjà plusieurs — corrigé, les deux écrans sont maintenant alignés y compris pour un item déjà publié. **Cycle #427** : un popup d'alerte à la connexion (même architecture que celui du relais ponctuel — requête indépendante, aucun marquage « vu » implicite à la fermeture) prévient l'auteur d'un besoin Transport ou la personne à transporter dès qu'une proposition d'horaire n'a pas encore été vue (`lib/transportAlerts.ts`, `components/TransportProposalAlertModal.tsx`, nouveau champ `transport_proposals[].seen_by_author`) ; un badge « New » sur la carte du besoin persiste tant que la proposition n'a pas été vue par ce chemin (`transportProposalWallRows` dans `Entraide.tsx`, même mécanisme `wallUnread.ts` que les autres badges « New »). **Ce cycle (#429-433)** : un besoin Transport se ferme désormais automatiquement dès qu'un horaire est validé (`status="ferme"` posé par `validateTransportLeg`) avec un badge « Validée » sur la proposition retenue — corrige au passage un bug introduit par ce changement (#430) où le popup d'alerte des propositions non vues revenait indéfiniment sur un besoin déjà fermé (`unseenProposalsFor` ignore désormais tout besoin dont `status !== "ouvert"`) ; le mur d'Entraide affiche la photo/avatar réelle de la personne qui s'occupe d'un besoin (single-claimant et chaque jour de couverture relais) à la place du « 👤 » générique, avec repli sur les initiales prénom+nom si aucune photo (`initials()` dans `lib/visitorRoster.ts`, corrige un premier essai qui n'affichait que la 1ère lettre du prénom) ; les besoins Courses à plusieurs contributeurs affichent désormais une rangée d'avatars groupés au-dessus de la phrase « X et Y s'en occupent » (`claimerAvatarRow`, `courseContributorsList()`) ; les listes de courses de la tuile Ma semaine utilisent une palette élargie de couleurs par contributeur (au-delà de 2) au lieu du schéma vert/orange binaire. **Cycle #435-438** : un popup de rappel d'échéance du jour (`components/TaskDueTodayAlertModal.tsx`, `lib/dueTodayAlerts.ts`) alerte désormais à la connexion — et à chaque retour au premier plan tant que le besoin n'est pas fait — pour chaque besoin repas/affaires/administratif/autre pris en charge par l'utilisateur dont l'échéance tombe le jour même (« C'est fait » ouvre la sheet de confirmation existante via `?openDone=1`), et pour chaque personne ayant coché ≥1 article d'une liste de courses due le jour même (ouvre directement l'aperçu de la liste via `?openShoppingList=1`) ; le bouton « ✏️ Modifier le besoin » est désormais visible pour l'auteur de n'importe quel besoin, pas seulement l'admin (#436) ; « Je m'en occupe » sur une liste de courses recoche de nouveau automatiquement les articles restants (comportement retiré en v1.10, réintroduit #436) et ajouter un article à une liste « Fait » la rouvre désormais systématiquement en « ouvert » (#436-437) ; `ShoppingListModal` regroupe les articles par contributeur et se resynchronise en direct avec le besoin affiché (#437) ; un bouton « ✓ Fait » (mur + popup de rappel) permet à toute personne ayant coché ≥1 article — ou à la preneuse/preneur formel — de cocher le reste à son nom et refermer le besoin, même en Historique, pour rattraper une liste restée bloquée par une prise en charge antérieure à la réintroduction du cochage automatique ; « Je m'en occupe » reste visible sur une liste « pris_en_charge » incomplète, avec un avertissement rouge et un badge « Pris en charge / partiellement » sur 2 lignes (#438).
- **Ma semaine** *(nouvel onglet, cycle #424-427)* : dernière position de la 2ᵉ barre d'onglets (Calendrier / Créneaux / Nuits / Infos / Partager / **Ma semaine**), visible admin + visiteur, masqué intervenant, ouvert par défaut à la connexion — composant partagé `components/MyWeekScreen.tsx`. Tuile « 🟠 Mon agenda » : visites/nuitées de la semaine fusionnées chronologiquement avec les trajets transport sur lesquels la personne est engagée, navigation directe vers l'écran d'origine au tap ; corrige au passage l'affichage des accompagnants au format texte libre `companion_firstnames` pré-`group_id`, invisibles jusque-là (`PlanningDuJourBlock`, `SoinsPeriodBlock`). **Ce cycle (#429)** : le groupe « Aujourd'hui » de l'agenda est désormais entouré d'un liseré vert pour le distinguer visuellement des autres jours de la semaine. Tuile « 🟢 Mes engagements » : cartes détaillées « Besoin {catégorie} / date / Publié par / statut » et tri chronologique pour les relais (#427) ; barre de progression relais réutilisant `components/RelaisDayProgress.tsx` avec une variante **admin 2 couleurs** (vert/rouge) distincte de la variante **visiteur 3 couleurs** (vert/orange/rouge) déjà utilisée ailleurs, photo/avatar du contributeur sous chaque case jour (repli sur les initiales prénom+nom depuis #433, corrige un premier essai à une seule lettre — voir #433 plus bas). Photo de profil admin dénormalisée dans `patient_spaces.admin_photo_url` (voir « Connu, non bloquant ») pour être visible du visiteur dans cette tuile — **ce cycle (#429)** corrige un bug où cette photo ne se resynchronisait jamais après un changement côté admin (le contexte `SpaceContext.tsx` ne relisait pas la colonne). **Ce cycle (#429)** : les listes de courses affichées ici passent d'un schéma binaire vert (moi) / orange (quelqu'un d'autre) à une **palette de couleurs par contributeur** (`courseContributorColors()`, `COURSE_PALETTE`/`PASTEL_COLORS`) quand plus de 2 personnes participent. **Ce cycle (#431)** : le filtre de cette tuile ne retient plus que les besoins où l'utilisateur est réellement **engagé** (pris en charge, contribué aux courses, ou couvert un jour de relais) — un besoin simplement publié par l'utilisateur mais pris en charge par quelqu'un d'autre n'apparaît plus ici (il reste visible dans « Mes besoins », Mon Compte) ; un avatar (photo ou initiale) du preneur en charge est désormais affiché pour les besoins hors relais/courses (repas, affaires, administratif, autre, transport). **Cycle #435** : correctif du filtre semaine — la tuile affichait un besoin engagé quelle que soit sa semaine d'échéance au lieu de le borner à la semaine affichée.
- **Nouvelles/Soutien** : indicateur « New » (barre bleue = identité connectée, badge/cadre rouge = non-lu — deux mécanismes indépendants depuis #371), fiabilité en arrière-plan (filet `AppState`), permissions admin/visiteur harmonisées.
- **Calendrier/Visites** : `HomeCalendarScreen` commun admin/visiteur, Mensuel/Hebdo (Hebdo Premium), réservations récurrentes, regroupement par créneau, cascade accompagnant sur modification.
- **Mon Compte** : Mes souvenirs (page personnelle), Mes documents (courriers pré-remplis), Mes engagements de relais, Mes alertes (avec Archives repliables), Mon code PIN.
- **Rôle Intervenant** : masqué derrière `INTERVENANT_ROLE_ENABLED=false` depuis #291 (pas supprimé — code archivé dans `Développement V2/`), Premium.
- **Identité visuelle (logo)** : **Cycle #420-421**, sur remontée utilisateur (contours en escalier visibles sur un screenshot). PR #420 : réencodage de `assets/icon.png`, `icon-sans-512.png`, `splash.png` avec anti-aliasing propre sur les silhouettes des personnages (dilatation de couleur + downsample/upsample pour éviter tout halo) ; `adaptive-icon.png` volontairement non touché (le lissage y créait un halo gris visible autour de la marge de sécurité transparente Android, non vérifiable sans device — limite connue). PR #421 : suite à #420, l'anneau décoratif superposé à la photo du patient (`icon-sans-512.png`, utilisé par `SpaceHeader.tsx` uniquement quand une photo patient existe) a son calendrier+horloge descendu d'environ 50px pour commencer entre les bras des deux personnages au lieu de les chevaucher, dégageant davantage la photo au centre. **Ce cycle (#423)** : le décalage de 50px de #421 s'est révélé excessif — espace vide visible entre les mains et le calendrier, et chevauchement avec le bras violet (tache violette dans le cadran, signalée par l'utilisateur comme « pixelisation »). Réduit à 20px, régénéré proprement depuis l'état pré-#421 (`icon-sans-512.png` + `logo.png` mis à jour). Aucun changement de code, aucune migration pour ces 3 PR — uniquement des assets binaires ; pas de section dédiée dans `Documentation Fonctionnalités.docx` (organisé par écran/fonctionnalité, pas par asset visuel), non modifiée pour ce point.

**Connu, non bloquant :**
- Branche distante `feat/entraide-creer-checklist` (PR #140, déjà mergée) porte un commit orphelin (`e9472c9`) — nettoyage jamais confirmé, sans conséquence fonctionnelle.
- Trois implémentations distinctes du désengagement/désinscription coexistent volontairement (`performUnclaim` dans `Entraide.tsx`, `lib/taskDisengage.ts` dans les deux `account.tsx`) — convention du repo, à garder en tête si une future demande vise à tout unifier.
- **Migrations manuelles au statut d'application non reconfirmé** : `supabase/migrations/20260909_personal_checklist_context_widen2.sql` (#417, **nouveau ce cycle** — élargit la contrainte CHECK `personal_checklist_items_checklist_context_check` aux 4 nouveaux contextes `telesurveillance`/`materiel_medical`/`aide_domicile_menage`/`portage_repas` ; sans cette exécution manuelle, la publication privée d'une de ces 4 sous-checklists échoue encore en production avec l'erreur "violates check constraint"), `20260821_visitor_profiles_relation.sql` (#278), `20260827_task_relais_coverage.sql` (#305), `20260830_deleted_content_seen.sql` (#337), `20260901_claimed_bought_at.sql` (#354), `20260901_visitor_claim_reset_pin.sql` (#366), `20260902_pin_reset_requests_resolved_at.sql` (#367). **Confirmées appliquées** : `20260827_reservation_change_history_seen.sql`, `20260902_freemium_7day_window.sql` (dont la fonction trigger avait le bug `column "timestamp"`, corrigé par #386), `20260904_rgpd_purge_premium_upgrade_extension.sql`, `20260910_patient_spaces_admin_photo.sql` (#427, ajoute `patient_spaces.admin_photo_url` — confirmée exécutée par l'utilisateur le 10/09/2026, nécessaire pour que la photo admin s'affiche côté visiteur dans « Ma semaine »). Le job pg_cron `check-freemium-expiry-daily` n'a toujours pas tourné sur un espace réellement expiré en conditions réelles — comportement de bout en bout (email + `cap_email_sent_at`) à surveiller au premier espace qui atteint sa 7ᵉ journée.
- **Ruleset GitHub `main-protection`** : le check de statut "Vercel" a été retiré manuellement par l'utilisateur (nécessaire suite à #375) — Claude Code ne peut pas modifier les rulesets lui-même (`gh api ... --method PUT` bloqué par le classifieur de sécurité auto-mode).
- **`adaptive-icon.png`** (icône adaptative Android) : volontairement non retouché par #420 — l'anti-aliasing y créerait un halo gris visible autour de sa marge de sécurité transparente, effet non vérifiable sans device réel. Limite connue, laissée de côté.
- **`logo.png`** (racine du dépôt, ajouté par ce cycle) : copie de `assets/icon-sans-512.png` (version finale post-#421), à la demande explicite de l'utilisateur — référence rapide / usage externe (site web, docs), n'est pas un asset consommé par le code de l'app.
- **`CHECKLISTS_SUGGEREES.md`** (inventaire complet du contenu des checklists, à la racine du dépôt) daté 09/09/2026 mais **non régénéré depuis l'ajout du sous-menu « Soin à domicile »** (#416) : sa checklist n°3 reflète encore l'ancien contenu plat (6 items), sans les 4 nouveaux modèles ni la structure en sous-menu — à régénérer si son usage redevient actif.
- **Documentation à jour** : `Documentation/Documentation Fonctionnalités.docx` synchronisée jusqu'à la PR #438 (**V1.36**, 13/09/2026, livrée par ce cycle documentaire de rattrapage #435-438) — trois cellules existantes complétées par ajout de phrase, sans nouvelle sous-section ni renumérotation : « Cycle de vie d'un besoin » (popup de rappel d'échéance du jour, « Modifier le besoin » ouvert à tout auteur) et « Liste de courses » (cochage auto au claim réintroduit, liste éditable après « Fait », regroupement par contributeur dans l'aperçu, rappel d'échéance courses, bouton « ✓ Fait » + badge « Pris en charge / partiellement ») en 5.9 Entraide ; « Mes engagements » en 5.18 Ma semaine (correctif filtre semaine). `PRD_AvecToi_v1_4.md` **passé de v1.34 à v1.35** : nouveau bloc changelog en tête (7 entrées), §3.8 Entraide amendé en profondeur (3 nouveaux bullets + réécriture complète du bullet Courses, incluant la réconciliation explicite avec la note historique v1.9→v1.10 sur le cochage automatique) et §3.13 Ma semaine amendé (aucune nouvelle section — ce cycle enrichit des capacités déjà documentées, sous le seuil « portée produit » du critère 5ter).

**En cours / pas commencé (reporté, à reprendre sur demande) :**
- `fix/souvenirs-rls-and-news-delete` : branche stale, 2 migrations RLS jamais exécutées en prod (`AUDIT_RLS_TAILLE_CODE_MORT.md`).
- Isolation Supabase (séparer l'instance prod du site web historique) : plan dans `ISOLATION_SUPABASE.md`, à réévaluer main­tenant que l'ancien site Vercel qui la motivait a été supprimé (#375) — ne pas présumer le plan caduc sans le redemander à l'utilisateur.
- `docs/spec-web-upgrade` : en attente d'une décision utilisateur.
- Points 13-14 (EAS Build APK signé, fiche Play Store) : pas commencés.
- Site marketing `avectoi.care` (dépôt séparé, Next.js, Infomaniak) : toujours pas scaffoldé. DNS/Resend opérationnels pour `support@avectoi.care`, PRD revu, reste à confirmer les accès Infomaniak pour le déploiement Git.
- Gate Intervenant (§3.9 docx) toujours en `Alert` bloquante, non harmonisé avec le style masquage/`ConfirmModal` introduit par #377-378 — à signaler si une future PR touche ce chantier.
- Nettoyage code mort post-assistant « Publier » (`!editTask &&` dans `taskForm`), à reproposer explicitement sur demande.
- `CHECKLISTS_SUGGEREES.md` à régénérer pour refléter le sous-menu « Soin à domicile » et les 4 nouveaux modèles (voir « Connu, non bloquant » ci-dessus).
- `AUDIT_RLS_TAILLE_CODE_MORT.md`, `PRD_ClaudeCode_Site_avectoi_care_v3.md`, `eslint.config.js`, `AvecToi-Site/` : fichiers non suivis présents dans l'arbre de travail, non touchés par ce cycle, jamais stagés (exclusion permanente).

## Historique cumulé
- Jusqu'au 17/08/2026 (PR #7-224) : socle V1 complet (auth, calendrier/créneaux/PIN, Souvenirs, Nouvelles, Entraide/Soutien, thèmes, notifications/emails), refonte intervenant Phase A/B, RGPD 90j + purge auto + sauvegarde schéma, checklists perso, EAS Update automatisé, panneau admin Calendrier + permissions Nouvelles, planning intervenants multi-espaces.
- 17/08 → 27/08/2026 (PR #225-301) : "Mes souvenirs", checklists proche-aidant guidées (11 modèles), Mes documents (courriers pré-remplis auto), liste de courses temps réel, besoin de relais ponctuel, réservations récurrentes, lien visiteur-patient, RGPD 60j+30j, retrait du rôle Intervenant en V1 (masqué, archivé V2), calendrier `HomeCalendarScreen` unifié admin/visiteur.
- 27/08 → 30/08/2026 (PR #303-345) : chantier SOS Relais (répartition par sous-périodes, alertes centralisées « Mes alertes »), assistant « Publier » Entraide en popups chaînées, Transport (lien Maps, propositions, validation), badges de non-lu (cadre orange puis indicateur « New »), une dizaine de correctifs UX groupés après tests utilisateur.
- 31/08 → 02/09/2026 (PR #346-371) : indicateur « New » stabilisé (fiabilité `AppState` en arrière-plan), tag Urgent auto à J+3, reconnaissance visiteur cross-device par PIN serveur (`visitor_profiles`), reset PIN admin + self-service visiteur, badge « New »/liseret bleu découplés en deux mécanismes indépendants.
- 02/09 → 05/09/2026 (PR #373-387) : cap Freemium repositionné en essai glissant de 7 jours, nettoyage de l'ancien MVP Vercel mort du dépôt, gating Freemium/Premium aligné sur le site (7,99 €, purge RGPD différenciée par plan), flux « Mot de passe oublié ? » (fix définitif via `flowType: "pkce"`), correctif du bug bloquant `column "timestamp" does not exist` sur toute réservation de Visite.
- 04/09 → 08/09/2026 (PR #390-411, narrative PR-par-PR non détaillée dans ce fichier — voir GitHub) : chantier co-administration temporaire complet — désignation depuis une proposition de relais, acceptation email + code, tableau de bord « Besoins SOS »/« Co-Administrateurs », RPC serveur dédiées avec restrictions RGPD/facturation, badge « New » sur propositions non traitées, barre de progression relais introduite dans Mon Compte.
- 08/09 → 09/09/2026 (PR #412-414) : barre de progression relais réutilisée sur 3 emplacements (Mon Compte, tableau de bord co-admin, mur d'Entraide) via `components/RelaisDayProgress.tsx` partagé ; accès « Paramètres Co-Administrateur » stabilisé après 4 tentatives (#405-410) en onglet caché du groupe `(visitor)` plutôt qu'un overlay manuel ; correctif d'affichage (troncature de date sur les propositions de relais déjà faites).
- 09/09/2026 (PR #416-417) : bibliothèque de checklists passée à 15 modèles — la carte « 🏠 Soin à domicile » (Entraide et Mon Compte → Mes Checklists) ouvre un sous-menu de 5 situations (ménage, portage de repas, télésurveillance, matériel médical, mise en place générale) au lieu de publier directement son contenu ; correctifs de navigation post-publication et de contrainte CHECK en base pour la publication privée des 4 nouveaux contextes.
- 09/09/2026 (PR #418, #420-421) : parité d'affichage des liens officiels multiples entre le mur d'Entraide et Mon Compte → Mes Checklists (PR #418, complète le mécanisme introduit par erreur d'attribution lors du cycle #416-417) ; qualité visuelle du logo — anti-aliasing des contours des personnages (PR #420) puis repositionnement du calendrier de l'anneau décoratif pour dégager la photo du patient (PR #421).
- 09/09 → 10/09/2026 (PR #423-427) : correctif v2 du repositionnement du logo (#423, décalage réduit de 50px à 20px après retour utilisateur) ; nouvel onglet « 🗓️ Ma semaine » (agenda de la semaine fusionnant visites/nuitées/transport, engagements Entraide détaillés avec courses colorées et barre de progression relais, photo admin dénormalisée pour le visiteur) ; popup d'alerte + badge « New » sur les propositions de transport non vues.
- 12/09/2026 (PR #429-433) : fermeture automatique + badge « Validée » sur un besoin Transport validé, liseré vert « Aujourd'hui » de Mon agenda, couleurs par contributeur pour les listes de courses, correctif de resynchronisation de `admin_photo_url` (#429) ; correctif du popup d'alerte transport qui revenait indéfiniment sur un besoin déjà fermé (#430) ; filtre « Mes engagements » resserré à engagé-seulement + avatar du preneur en charge, mur d'Entraide « 👤 » remplacé par la photo/avatar réelle (#431) ; avatars groupés pour les besoins Courses à plusieurs contributeurs (#432) ; correctif du repli d'avatar affichant désormais les initiales prénom+nom au lieu de la seule lettre du prénom (#433).
- 12/09 → 13/09/2026 (PR #435-438) : popup de rappel d'échéance du jour pour repas/affaires/administratif/autre pris en charge et pour les listes de courses partiellement cochées, correctif du filtre semaine « Mes engagements » (#435) ; texte du popup affiné, « Modifier le besoin » ouvert à tout auteur, cochage automatique au claim courses réintroduit (retiré en v1.10, jugé erroné à l'époque), liste rouverte en « ouvert » après ajout d'un article sur une liste « Fait » (#436) ; popup simplifié + reproposé à chaque reconnexion, alerte d'échéance étendue aux courses, badge « partiellement » persistant après fermeture auto, `ShoppingListModal` regroupé par contributeur et resynchronisé en direct (#437) ; bouton « ✓ Fait » (popup + mur, utilisable même en Historique, pour rattraper une liste restée bloquée) et badge « Pris en charge / partiellement » (2 lignes) avec avertissement rouge sur une liste `pris_en_charge` incomplète (#438).

## 1. Objectif de la session
Quatre PR distinctes, déjà mergées par l'utilisateur avant le début de cette session documentaire (confirmations explicites successives : « ok testé, mergé », « mergé »), livrées sur deux demandes utilisateur (rappel d'échéance + fix filtre semaine, puis « bouton Fait + statut partiel » suite à un cas réel bloqué signalé par l'utilisateur) :
- **PR #435** (`feat/alerte-echeance-engagement-rappel`) : deux changements groupés — nouveau popup `TaskDueTodayAlertModal.tsx` affiché à la connexion pour chaque besoin repas/affaires/administratif/autre pris en charge par l'utilisateur dont l'échéance (`date_limite`) tombe le jour même (« C'est fait » ouvre la sheet de confirmation existante d'`Entraide.tsx` via `?openDone=1`, même mécanisme que `RelaisAlertModal`) ; correctif de la tuile « Mes engagements » qui affichait un besoin engagé quelle que soit sa semaine d'échéance au lieu de le filtrer sur la semaine affichée.
- **PR #436** (`fix/rappel-texte-courses-liste-editable`) : quatre changements groupés — texte du popup de rappel enrichi (titre + catégorie du besoin) ; `ShoppingListModal` : ajouter un article à une liste déjà « Fait » repasse le besoin en « pris_en_charge »/« ouvert » selon les cas (même logique que le décochage) ; `handleClaim` (courses) : « Je m'en occupe » recoche automatiquement les articles restants et les attribue au preneur — réintroduit ce comportement, qui avait été explicitement retiré en v1.10 (jugé erroné à l'époque) ; le bouton « ✏️ Modifier le besoin » (et son raccourci par appui long) devient visible pour l'auteur de n'importe quel besoin, pas seulement l'admin sur ses propres besoins.
- **PR #437** (`fix/popup-reconnexion-courses-groupage`) : cinq changements groupés — texte du popup simplifié (titre seul) et popup reproposé à chaque reconnexion (`AppState` "active") tant que le besoin n'est pas fait, plus seulement une fois par session ; ajouter un article à une liste « Fait » repasse désormais **toujours** le besoin en « ouvert » (prise en charge effacée), au lieu du comportement à deux issues de #436 ; nouveau système d'alerte du jour J pour les courses (`lib/dueTodayAlerts.ts` : `fetchDueTodayCourses`) — chaque personne ayant coché ≥1 article reçoit un rappel listant ses propres articles, bouton ouvrant directement l'aperçu de la liste ; « … partiellement » persiste désormais aussi une fois le besoin auto-fermé (statut « ferme »), pas seulement « ouvert » ; `ShoppingListModal` resynchronisé en direct avec la tâche live et regroupé par contributeur (une photo par personne, ordre alphabétique, « moi » en premier).
- **PR #438** (`fix/courses-fait-partiel`) : suite à un signalement utilisateur (une prise en charge posée par une visiteuse — Silvia Baupin — sur une liste de courses n'avait jamais coché les articles restants ni refermé le besoin, resté visible en Historique) — nouveau bouton « ✓ Fait » sur le popup de rappel d'échéance et sur la carte du mur, accessible à toute personne ayant coché ≥1 article ou étant la preneuse/preneur formel, y compris sur un besoin déjà fermé/en Historique, pour cocher directement le reste de la liste à son nom et refermer le besoin — sert à la fois de fonctionnalité et de filet de rattrapage pour les listes restées bloquées par une prise en charge antérieure à la réintroduction du cochage automatique (#436) ; « Je m'en occupe » reste désormais aussi visible sur un besoin « pris_en_charge » dont la liste n'est pas intégralement cochée ; dans ce cas, avertissement rouge « Certains articles ne sont pas encore pris en charge » sur le bloc du besoin et badge de statut « Pris en charge / partiellement » sur 2 lignes.

## 2. État actuel
- Code : PR #435-438 mergées, aucune migration SQL dans ce cycle — confirmé, aucune tâche de code restante identifiée. Diagnostic du cas Silvia Baupin : très probablement une donnée historique antérieure au cochage automatique réintroduit par #436 (la logique de `handleClaim` était déjà correcte en l'état du code lu pendant #438), le bouton « ✓ Fait » sert de réparation manuelle pour ce cas précis et tout autre cas similaire déjà en base.
- `Documentation/Documentation Fonctionnalités.docx` : V1.35 → **V1.36** (13/09/2026). Aucune nouvelle sous-section — trois cellules existantes complétées par une phrase : « Cycle de vie d'un besoin » et « Liste de courses » (5.9 Entraide), « Mes engagements » (5.18 Ma semaine, correctif filtre semaine).
- `PRD_AvecToi_v1_4.md` : **v1.34 → v1.35**. Nouveau bloc changelog en tête (7 entrées) ; §3.8 Entraide amendé (3 nouveaux bullets — « Modifier le besoin » ouvert à tout auteur, rappel d'échéance du jour, et bullet Courses entièrement réécrit avec réconciliation explicite de la note historique v1.9→v1.10 qui indiquait le cochage automatique comme « retiré car jugé erroné », désormais marquée « réintroduit en v1.35 à la demande explicite de l'utilisateur ») ; §3.13 Ma semaine amendé (correctif filtre semaine) — aucune nouvelle section, sous le seuil « portée produit » du critère 5ter.
- `Handoff/handoff.md` : ce fichier — en-tête, « État global » (bullets Entraide/Ma semaine amendés, ligne Documentation à jour), « Historique cumulé » complété d'une ligne, section détaillée réécrite.

## 3. Fichiers concernés
- `components/Entraide.tsx` (#436, #437, #438) — `handleClaim` courses recoche automatiquement + attribue les articles restants (#436) ; « ✏️ Modifier le besoin » visible pour tout auteur (#436) ; nouveaux helpers `courseListIncomplete()`, `courseFaitEligible()`, `markCoursesFait()`, `confirmCoursesFait()` ; badge de statut « Pris en charge / partiellement » (2 lignes) et avertissement rouge sur liste `pris_en_charge` incomplète ; bouton « ✓ Fait » sur la carte courses ; « Je m'en occupe » étendu aux besoins `pris_en_charge` avec liste incomplète (#438).
- `components/TaskDueTodayAlertModal.tsx` (#435, #436, #437, #438) — nouveau composant (#435) : popup à la connexion pour repas/affaires/administratif/autre pris en charge dû le jour même, deep-link `?openDone=1` ; texte enrichi puis simplifié, reproposé à chaque `AppState` "active" (#436-437) ; variante courses avec `?openShoppingList=1` (#437) ; nouveau bouton « ✓ Fait » + état `identity` (#438).
- `lib/dueTodayAlerts.ts` (#435, #437) — nouveau fichier (#435) : `fetchDueTodayCommitments` (repas/affaires/administratif/autre, comparaison pin+prénom+nom sauf sentinel admin) ; `fetchDueTodayCourses` (#437) — un besoin par personne ayant coché ≥1 article, liste des articles attribuée.
- `components/MyWeekScreen.tsx` (#435) — filtre semaine de la tuile « Mes engagements » corrigé pour borner à la semaine affichée.
- `components/ShoppingListModal.tsx` (#436, #437) — ajouter un article à une liste « Fait » rouvre le besoin (issue variable en #436, toujours « ouvert » en #437) ; regroupement par contributeur + resynchronisation live avec la tâche affichée (#437).
- `Documentation/Documentation Fonctionnalités.docx`, `PRD_AvecToi_v1_4.md` — voir §2.

## 4. Ce qui a échoué
Rien côté code. Côté process : la branche de travail de #438 avait déjà sa PR mergée au moment où un commit supplémentaire y a été ajouté par erreur — récupéré en committant sur cette branche stale puis en `cherry-pick`ant le commit sur une branche fraîche depuis `origin/main` avant d'ouvrir la PR (même pattern de récupération que documenté dans `avectoi_stacked_pr_merge_order_trap`, appliqué ici de façon proactive).

## 5. Prochaine étape
Aucune tâche de code en attente identifiée par l'utilisateur — le cycle est clos une fois cette documentation committée/poussée. **Non testé sur un appareil réel pendant cette session** (pas d'accès `adb`/émulateur dans cet environnement) : #435-438 vérifiées par relecture de code et `npx tsc --noEmit` uniquement côté Claude ; l'utilisateur a lui-même confirmé le fonctionnement de bout en bout après chaque merge. **Cas Silvia Baupin** : à vérifier par l'utilisateur que le bouton « ✓ Fait » débloque effectivement le besoin resté coincé en Historique signalé au départ de la demande #438. Committer les 3 documents (docx + PRD + ce handoff) sur une branche `docs/` neuve (depuis `origin/main`), pousser, ouvrir la PR.

**⚠️ Rappels à transmettre à l'utilisateur (reportés depuis les cycles précédents, statuts inchangés sauf mention contraire) :**
1. `supabase/migrations/20260909_personal_checklist_context_widen2.sql` (#417) — à exécuter manuellement via le Dashboard Supabase (SQL Editor) ; sans ça, publier en privé une checklist « ménage », « portage de repas », « télésurveillance » ou « matériel médical » échoue encore avec une erreur de contrainte CHECK. Inchangé, toujours non confirmé.
2. `supabase/migrations/20260821_visitor_profiles_relation.sql` (#278) — statut d'application non confirmé, inchangé.
3. `supabase/migrations/20260827_task_relais_coverage.sql` (#305) — statut d'application non reconfirmé explicitement, inchangé.
4. Symptôme (b) du cycle #307-308 — alertes SOS relais qui semblaient disparaître — reste seulement instrumenté (logs d'erreur), jamais conclusivement corrigé, inchangé.
5. `supabase/migrations/20260830_deleted_content_seen.sql` (#337) — statut d'application non confirmé, inchangé.
6. `supabase/migrations/20260901_claimed_bought_at.sql` (#354) — statut d'application non confirmé, inchangé.
7. Nettoyage du code mort post-assistant « Publier » (`!editTask &&` dans `taskForm`) — à reproposer explicitement sur demande, inchangé.
8. Le « Modifier » visible par PIN partagé sur une réservation d'autrui reste un comportement explicitement voulu (cycle #350), pas un bug — inchangé.
9. Le badge « New » ne se réinitialise qu'à un vrai redémarrage complet de l'app — inchangé.
10. L'Urgent automatique à J+3 persiste après échéance mais reste borné à ~60s par `closeOverdue` — inchangé.
11. `supabase/migrations/20260901_visitor_claim_reset_pin.sql` (#366) et `20260902_pin_reset_requests_resolved_at.sql` (#367) — statut d'application non confirmé, inchangé.
12. Badge « New » et liseret bleu sont deux mécanismes indépendants (`NewIndicator` `{isNew, mine}`) depuis #371 — inchangé.
13. `visitorSession.ts` (AsyncStorage) n'est qu'un cache de confort depuis #358, pas la source de vérité de l'identité visiteur — inchangé.
14. `supabase/migrations/20260902_freemium_7day_window.sql` (#374, corrigée #386) confirmée appliquée, mais le job `check-freemium-expiry-daily` n'a toujours pas tourné sur un espace réellement expiré en conditions réelles — comportement de bout en bout (email + `cap_email_sent_at`) à surveiller en priorité au premier espace qui atteint sa 7ᵉ journée — inchangé.
15. Le plan `ISOLATION_SUPABASE.md` mériterait d'être réévalué maintenant que l'ancien site Vercel qui partageait l'instance a été supprimé (#375) — ne pas présumer qu'il est caduc sans le redemander à l'utilisateur — inchangé.
16. Le site marketing `avectoi.care` n'est toujours pas scaffoldé — inchangé.
17. Le gate Intervenant (§3.9 docx), toujours Premium, garde l'ancien comportement d'`Alert` bloquante — non harmonisé avec les deux styles de gate (masquage / `ConfirmModal`) introduits par #377-378 — inchangé.
18. `avectoi://auth/reset-password` dépend du flux PKCE (`flowType: "pkce"`, #383) — toute autre feature future utilisant un lien email Supabase Auth en bénéficie implicitement — inchangé.
19. Toute nouvelle colonne temporelle sur `reservations` doit utiliser `created_at` (le nom réel en base) — `timestamp` n'a jamais existé et a causé le bug corrigé par #386 — vérifier `supabase/schema_backups/schema_snapshot.sql` en cas de doute plutôt que de supposer — inchangé.
20. `CHECKLISTS_SUGGEREES.md` (inventaire du contenu des checklists, à la racine du dépôt) n'a pas été régénéré depuis l'ajout du sous-menu « Soin à domicile » (#416) — sa section « Soin à domicile » reflète encore l'ancien contenu plat, sans les 4 nouveaux modèles ni la structure en sous-menu — inchangé.
21. `assets/adaptive-icon.png` n'a reçu aucun des correctifs de logo (#420 anti-aliasing, #421/#423 repositionnement) — l'anti-aliasing y créerait un halo gris visible autour de sa marge de sécurité transparente Android, non vérifiable sans device. À réévaluer si l'utilisateur confirme le halo sur un build réel. Inchangé.
22. `logo.png` à la racine du dépôt est une copie statique de `assets/icon-sans-512.png` au moment de chaque commit (resynchronisée par #423) — elle ne se met pas à jour automatiquement si l'asset source est retouché par une future PR ; à resynchroniser manuellement si besoin. Inchangé.
23. `TransportProposalAlertModal.tsx` (#427) ne marque une proposition « vue » que sur une action explicite (« Voir les propositions »), jamais à la simple fermeture du popup — même convention que `RelaisAlertModal.tsx`. À garder en tête si une future demande vise à « simplifier » ce comportement : c'est un choix délibéré, pas un oubli.
24. **Nouveau** : depuis #429, `validateTransportLeg` pose `status="ferme"` dès qu'un horaire est validé — un besoin Transport validé ne reste plus jamais « ouvert ». `unseenProposalsFor` (#430) en dépend explicitement (`status !== "ouvert"` ⇒ ignoré) : toute future logique qui filtre/liste les besoins Transport par statut doit tenir compte de cette fermeture automatique, ce n'est plus un état atteint uniquement par action manuelle de l'auteur.
25. **Nouveau** : `lib/visitorRoster.ts` porte désormais deux façons de résoudre une identité en photo — `loadPhotoRoster(spaceId)` (#431, utilisé par `Entraide.tsx`) et l'équivalent inline pré-existant dans `MyWeekScreen.tsx` (jamais refactoré vers le helper partagé). Les deux doivent être tenues à jour en parallèle si la source de la photo change (ex. nouveau champ, nouvelle table) — à unifier si une future demande touche encore à cette zone.

Points en suspens à reproposer sur demande : nettoyage du code mort post-assistant « Publier » (rappel 7), nettoyage de la branche distante `feat/entraide-creer-checklist` (commit orphelin `e9472c9`), harmonisation du gate Intervenant (rappel 17), réévaluation d'`ISOLATION_SUPABASE.md` (rappel 15), scaffold du site marketing `avectoi.care`, déploiement `avectoi-site` sur Infomaniak (bloqué sans les accès de l'utilisateur), rebase de `fix/souvenirs-rls-and-news-delete`, décision `docs/spec-web-upgrade`, isolation Supabase, taille de l'app/audit code mort (`AUDIT_RLS_TAILLE_CODE_MORT.md`), régénération de `CHECKLISTS_SUGGEREES.md` (rappel 20).
